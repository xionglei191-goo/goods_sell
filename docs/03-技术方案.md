# 华启商城 — 技术方案

## 1. 技术栈总览

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Next.js | 15.x | App Router，全栈React框架 |
| 语言 | TypeScript | 5.x | 全栈类型安全 |
| UI组件 | shadcn/ui | latest | 基于Radix UI的组件库 |
| 样式 | Tailwind CSS | 4.x | 原子化CSS |
| 状态管理 | Zustand | 5.x | 轻量级状态管理 |
| 数据获取 | TanStack Query | 5.x | 服务端数据缓存 |
| 数据库 | PostgreSQL | 16.x | 关系型数据库 |
| ORM | Prisma | 6.x | 类型安全的数据库操作 |
| 认证 | NextAuth.js | 5.x | Auth.js |
| 图表 | Recharts | 2.x | React图表库 |
| 地图 | 高德地图 JS API | 2.0 | 地图展示与定位 |
| AI | 统一大模型网关 / DashScope / DeepSeek | Anthropic 兼容 API | 大模型客服 |
| 支付 | 微信支付 | v3 | 在线支付 |
| 图片存储 | 本地/阿里云OSS | - | 产品图片存储 |
| 表单 | React Hook Form + Zod | - | 表单验证 |

## 2. 系统架构

```
┌──────────────────────────────────────────────┐
│                   用户端                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 商城H5   │ │ 管理后台 │ │ 经销商后台   │  │
│  │ (Mobile) │ │ (PC/Pad) │ │ (Mobile/PC)  │  │
│  └────┬─────┘ └────┬─────┘ └──────┬───────┘  │
└───────┼─────────────┼──────────────┼──────────┘
        │             │              │
┌───────▼─────────────▼──────────────▼──────────┐
│              Next.js 15 App Router             │
│  ┌──────────────────────────────────────────┐  │
│  │  Middleware (Auth + Role Check)          │  │
│  ├──────────────────────────────────────────┤  │
│  │  Server Components (SSR/RSC)            │  │
│  │  Server Actions (Mutations)             │  │
│  │  API Routes (Webhooks/External)         │  │
│  ├──────────────────────────────────────────┤  │
│  │  Features Layer                         │  │
│  │  ┌────────┐┌────────┐┌────────┐         │  │
│  │  │Products││Orders  ││Finance │ ...     │  │
│  │  └────────┘└────────┘└────────┘         │  │
│  ├──────────────────────────────────────────┤  │
│  │  Prisma ORM                             │  │
│  └──────────────────────────────────────────┘  │
└──────────────────┬─────────────────────────────┘
                   │
        ┌──────────▼──────────┐
        │   PostgreSQL 16     │
        └─────────────────────┘
```

## 3. 项目目录结构

```
goods_sell/
├── docs/                       # 项目文档
├── prisma/
│   ├── schema.prisma           # 数据库模型
│   ├── seed.ts                 # 演示数据
│   └── migrations/             # 迁移文件
├── public/
│   ├── images/                 # 静态图片
│   └── icons/                  # 图标
├── src/
│   ├── app/
│   │   ├── (auth)/             # 认证页面（登录/注册）
│   │   ├── (dashboard)/        # 管理后台
│   │   │   ├── layout.tsx      # 后台布局
│   │   │   ├── page.tsx        # 仪表盘首页
│   │   │   ├── products/       # 产品管理
│   │   │   ├── inventory/      # 库存管理
│   │   │   ├── orders/         # 订单管理
│   │   │   ├── customers/      # 客户管理
│   │   │   ├── finance/        # 财务管理
│   │   │   ├── dealers/        # 经销商管理
│   │   │   ├── sales/          # 销售管理
│   │   │   ├── map/            # 地图管理
│   │   │   └── settings/       # 系统设置
│   │   ├── (shop)/             # 线上商城
│   │   │   ├── layout.tsx      # 商城布局
│   │   │   ├── page.tsx        # 商城首页
│   │   │   ├── catalog/        # 产品目录
│   │   │   ├── product/[id]/   # 产品详情
│   │   │   ├── cart/           # 购物车
│   │   │   ├── checkout/       # 结算
│   │   │   ├── my-orders/      # 我的订单
│   │   │   ├── account/        # 账户中心
│   │   │   ├── ai-chat/        # AI客服
│   │   │   └── fun/            # 趣味互动
│   │   ├── (dealer)/           # 经销商端
│   │   │   ├── layout.tsx
│   │   │   ├── incoming/       # 待接订单
│   │   │   ├── my-orders/      # 我的订单
│   │   │   └── settlement/     # 结算中心
│   │   └── api/                # API路由
│   │       ├── auth/
│   │       ├── webhooks/
│   │       └── ai/
│   ├── components/
│   │   ├── ui/                 # shadcn/ui组件
│   │   ├── layout/             # 布局组件
│   │   ├── charts/             # 图表组件
│   │   └── shared/             # 共享组件
│   ├── features/               # 业务逻辑层
│   │   ├── auth/
│   │   ├── products/
│   │   ├── inventory/
│   │   ├── orders/
│   │   ├── customers/
│   │   ├── finance/
│   │   ├── dealers/
│   │   ├── shop/
│   │   ├── ai/
│   │   └── marketing/
│   ├── lib/
│   │   ├── prisma.ts           # Prisma客户端
│   │   ├── auth.ts             # 认证配置
│   │   ├── map.ts              # 地图工具
│   │   └── utils.ts            # 工具函数
│   ├── hooks/                  # 全局Hooks
│   └── types/                  # 全局类型
├── .env.example
├── .env
├── package.json
├── tailwind.config.ts
├── tsconfig.json
├── next.config.ts
├── todo.md
└── README.md
```

## 4. 关键技术决策

### 4.1 全栈 Next.js（非前后端分离）
- 利用 Server Components 减少客户端 JS 体积
- Server Actions 处理数据变更，无需写 REST API
- 统一部署，降低运维复杂度

### 4.2 PostgreSQL + Prisma
- Decimal 类型精确处理金额
- JSONB 类型存储灵活数据（用户画像标签等）
- Prisma 提供类型安全和自动迁移

### 4.3 移动优先响应式
- 第一阶段不做原生APP，用响应式Web覆盖手机端
- 重点优化微信内置浏览器体验
- 后续可通过 Taro/uni-app 转小程序

## 5. 部署架构（后续）

```
用户 → Nginx (反向代理/SSL) → Next.js (PM2管理) → PostgreSQL
                             → 静态资源 CDN
```
