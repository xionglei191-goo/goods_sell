# 华启酒饮数字渠道平台 — 全功能测试方案

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 华启酒饮数字渠道平台 |
| 文档名称 | 全功能测试方案 |
| 文档版本 | V1.24 |
| 编写日期 | 2026-04-30 |
| 适用阶段 | 上线前验收、封闭试运营前验收、版本发布回归 |
| 适用对象 | 产品负责人、实施人员、测试人员、开发人员、运营人员、业务负责人 |

## 当前执行进度标记

当前推进到：已完成“最新代码复核 + 自动化质量门禁 + 测试 schema 初始化 + API smoke + 数据库业务闭环 + 多角色权限 E2E + 生产 build/start smoke + 九批浏览器/本地模拟逐项验收 + 正式环境签收清单整理 + AI 代办助手代码复核 + 真实 AI provider 48 tools 全量覆盖 + 六角色浏览器抽验 + AI 下单/库存/接单/拒单/新品推送/管理员调价/财务开票/仓管履约浏览器写流程补充回归 + 第三方边界 smoke + Chrome/Edge 桌面与移动兼容修复复测 + AI 审计自由文本脱敏回归 + 销售员管理页/产品编辑页 RBAC 漏测修复 + RBAC 路由矩阵 smoke + 产品详情价格字段权限修复 + 字段级权限 smoke + 全量收尾回归 + 业务/财务/仓管/经销商模拟签收 smoke + AI 固定词条分角色显示与客户购买历史误规划修复 + AI Planner 候选召回强化 + 系统设置上线检查中心 + 系统本体完整度矩阵 + 运营验收检查 + 正式上线验收记录模板 + AI 能力目录全页面覆盖 + UI 暖色审计补强 + 系统完整度验收表 + 商城客户侧关键审计 + admin 等价 AI 工具边界回归”。P0 主链路、账号权限变更链路、AI tools 权限/确认/审计、真实 provider 工具规划、WRITE/HIGH_RISK 确认卡、RBAC 越权拦截、字段级权限、第三方 mock/边界能力、大部分 P1 运营能力和四类业务角色模拟验收在本地独立测试 schema 中已基本闭环；本轮 `test:ai-tools` 复跑结果为 `AI tools smoke passed: 75 tools`，`test:agent-capabilities` 复跑结果为 `71 capabilities, 79 pages`；`npm run check:system` 已升级为 14 个系统模块 × 6 个完整度维度的矩阵检查，覆盖真实入口、真实操作、权限、审计、异常处理和验证证据；但该命令仍属于自动门禁，最终系统完整度必须结合 `docs/15-系统完整度验收表.md` 的真实角色和真实操作证据；本轮补齐了商城下单、询价、取消、确认收货、地址和个人资料变更审计，并固定 admin 等价 AI 工具边界回归；`npm run check:operational` 当前为 `WARNING 5 / BLOCKER 0`，表示业务签收、价格复核、库存盘点、账号权限复核、真实设备、真实支付、小程序体验版、税控联调和备份恢复演练仍需人工留痕；正式公开上线 readiness 仍按 production 口径执行，微信小程序、微信公众号、微信支付、税控电子发票和酒类经营资质缺失为 `check:launch` 外部硬配置 BLOCKER，不计入程序完整度缺陷。封闭试运营可用 `LAUNCH_READINESS_MODE=trial` 降级第三方真实配置要求。

状态说明：

| 状态 | 含义 |
|------|------|
| 已完成 | 已在本轮本地测试环境执行并通过 |
| 已修复并复测 | 测试中发现问题，已修改代码或配置并完成复测 |
| 部分完成 | 已用自动化或数据库闭环覆盖核心路径，但仍缺少真实环境或人工逐项验收 |
| 待执行 | 尚未在正式测试环境执行 |
| 环境限制 | 当前本机或第三方条件不满足，需要正式测试环境补测 |

| 阶段 | 当前状态 | 说明 |
|------|----------|------|
| 最新代码复核 | 已完成 | 已按当前代码复核 RBAC、`/forbidden`、微信 API、smoke 脚本、构建命令和测试库要求 |
| 静态质量门禁 | 已完成 | `npx.cmd tsc --noEmit`、`npm.cmd run lint`、`npm.cmd run test:rbac`、`npm.cmd audit --audit-level=moderate` 均通过 |
| 测试数据隔离 | 部分完成 | 本机无 `CREATE DATABASE` 权限，已使用 `goods_sell_test` schema 完成隔离验证；正式验收仍应创建独立测试库 |
| Phase 6 只读 smoke | 已完成 | 仓储、配送、票据、设置、用户、日志等只读检查通过 |
| Phase 5 API smoke | 已完成 | 小程序首页、目录、购物车鉴权、登录、分享、公众号菜单鉴权通过 |
| 路由 smoke | 已完成 | 公开页、未登录保护页、后台/购物车/经销商端跳转规则通过 |
| 小程序下单 API E2E | 已完成 | 登录、地址、购物车、结算、下单、订单列表、分享链路通过 |
| 数据库全功能闭环 | 已完成 | 覆盖订单支付出库、经销商申请审核、分单接单/拒单、询价报价转订单、优惠券、新品推送、渠道冲突、收款、审计日志 |
| 多角色登录与权限 E2E | 已修复并复测 | 管理员、销售员、仓管、财务、消费者、经销商登录与授权/未授权入口验证通过；本轮浏览器补测发现仓管可打开 `/dashboard/salespeople`，以及非管理员可直达 `/dashboard/products/{id}/edit`，已补路由规则、RBAC smoke 路由矩阵和浏览器复测；新增 `test:field-permissions`，固定验证产品价格字段和销售员客户/订单/经销商数据归属边界 |
| API 异常与边界 smoke | 已完成 | 匿名访问、非法支付回调、非法结算、无效商品等边界通过 |
| 生产构建与启动 | 已修复并复测 | 已将 `package.json` 的 `build` 改为 `next build`；`npm.cmd run build` 与 `npm.cmd run start -- -p 3200` smoke 通过 |
| P0 人工全功能逐项验收 | 本地测试 schema 基本完成 | 已追加九批浏览器/本地模拟验收，完成登录权限、商城浏览加购、客户地址新增修复、取消订单、购物车询价、场景询价、报价生成、报价转订单、商品新增/编辑/上下架、库存入出库、盘点确认、采购入库、后台开单、订单确认发货、经销商接单/拒单/库存上报、赊账收款核销、开票、经销商申请审核/驳回、后台用户创建/重置/禁用、RBAC 专项、核心页面运行时扫描、生产启动 smoke 和交付检视入口复核等主要 P0 项 |
| P1 运营能力验收 | 本地测试 schema 基本完成 | 已追加浏览器模拟验收，完成优惠券创建/定向发放、新品推送创建与打开/咨询/试饮事件、客户画像刷新、经销商策略配置、渠道冲突创建与处理、AI 对话效果、微信配置入口与公众号菜单 Mock；真实微信链路和设备兼容仍需补测 |
| AI 代办助手复核 | 已修复并复测 | 已复核 `/api/ai/assistant` SSE、`/api/ai/assistant/confirm`、`/api/ai/quick-prompts`、74 个 tools、角色权限过滤、WRITE/HIGH_RISK 确认卡、HMAC token、审计脱敏、三端气泡入口、全站能力目录和上线/完整度检查；本轮修复供应商入口、客户配送订单和公共法律页能力覆盖，`test:ai-tools` 与 `test:agent-capabilities` 均通过 |
| 兼容性与设备验收 | 本地 Chrome/Edge 已完成 | 新增 `test:browser-compat`，最新复跑 Chrome/Edge 桌面 1366 与移动 390 共 68 个页面检查通过；覆盖管理员 `/dashboard/salespeople`、产品详情/编辑页，以及仓管产品详情字段级价格权限；微信内置浏览器、小程序体验版和真实移动设备仍需正式环境签收 |
| 真实微信支付/回调 | 本地边界已完成，真实支付待正式环境 | 新增 `test:third-party`，最新 runId=`TPMQNBDI88`，覆盖 mock 预支付、成功回调、重复回调幂等、少付拦截、非法回调；真实小额支付仍需微信支付测试环境 |
| 模拟运营演练 | 本地测试 schema 已完成，真实人员签收待确认 | 新增 `test:role-acceptance`，已用虚拟客户、订单、商品、配送、收款、库存、经销商和渠道冲突完成业务/财务/仓管/经销商模拟签收；真实负责人仍需按本记录做书面确认 |

## 2. 测试目标

本方案用于验证华启酒饮数字渠道平台在正式运营前是否具备可用、可控、可追溯的上线条件。

测试目标包括：

1. 验证客户从进入商城、提交需求、下单、支付、履约到售后的主流程可闭环。
2. 验证公司内部商品、库存、采购、订单、配送、财务和对账流程可正常使用。
3. 验证经销商分单、接单、拒单、库存上报和结算流程符合渠道保护策略。
4. 验证线索、询价、报价、推广码、新品推送和 AI 互动能支持运营获客。
5. 验证权限、日志、合规提示、异常处理和数据口径满足试运营要求。
6. 形成上线准入结论，明确必须修复项、可延期项和试运营风险。

## 3. 测试原则

- 先测主链路，再测边界和异常。
- 先测 P0 运营阻断问题，再测 P1/P2 体验与运营问题。
- 所有涉及金额、库存、分单、收款和客户资料的流程必须有可追溯记录。
- 测试数据应与真实业务接近，但不得混入真实客户隐私数据。
- 测试通过不代表直接全面运营，建议先进入封闭试运营。
- 写数据测试必须使用独立测试库，禁止在生产库或当前业务库执行 seed、Phase 5、下单、支付模拟、库存、财务和推广码相关用例。

## 4. 最新代码复核结果

### 4.1 复核范围

本次复核基于 2026-04-30 最新代码，重点核对以下内容：

- `src/app` 路由结构。
- `scripts` 下现有 smoke 脚本。
- `package.json` 测试与构建命令。
- RBAC 权限、`/forbidden` 页面、消费者/经销商/内部角色入口控制。
- 微信小程序与公众号 API。
- 仓储、配送、票据、系统设置、操作日志等只读 smoke 查询。
- AI 代办助手：悬浮气泡、SSE API、确认 API、tool registry、planner、executor、审计脱敏、上线 readiness。

### 4.2 已完成的第一批测试记录

| 批次 | 命令 | 结果 | 说明 |
|------|------|------|------|
| 静态检查 | `npx.cmd tsc --noEmit` | 通过 | TypeScript 类型检查通过 |
| 静态检查 | `npm.cmd run lint` | 通过 | ESLint 无报错 |
| RBAC smoke | `npm.cmd run test:rbac` | 通过 | 输出 `RBAC smoke passed` |
| 构建检查 | `npm.cmd run build` | 通过 | Next.js 生产构建通过 |
| 依赖审计 | `npm.cmd audit --audit-level=moderate` | 通过 | 输出 `found 0 vulnerabilities` |
| AI tools smoke | `npm.cmd run test:ai-tools` | 通过 | 输出 `AI tools smoke passed: 75 tools` |
| 系统本体完整度 | `npm.cmd run check:system` + `docs/15-系统完整度验收表.md` | 自动门禁通过后仍需人工证据 | 14 个模块 × 6 个维度矩阵检查作为基础门禁；最终结论必须记录入口、操作、权限、审计、异常和证据；域名、微信、小程序、支付、税控、酒类资质不计入程序缺陷 |
| 运营验收 | `npm.cmd run check:operational` | 可运行，需人工签收 | 检查业务负责人签收、价格复核、库存盘点、账号权限复核、真实设备、真实支付、小程序体验版、税控联调和备份恢复演练 |
| 上线 readiness | `npm.cmd run check:launch` | 可运行，当前有外部硬配置阻塞 | 脚本默认按正式公开上线口径输出 readiness 报告；当前环境结果为 `BLOCKER`，微信小程序、微信公众号、微信支付、税控电子发票和酒类经营资质未完整配置，这些不作为程序完整度缺陷 |
| 只读 smoke | 临时加载 `.env.local` 后执行 `npm.cmd run test:phase6` | 通过 | 输出 `Phase 6 smoke passed { totalSku: 33, deliveryRows: 3, payments: 3, users: 4, logs: 3 }` |
| 测试 schema 初始化 | `DATABASE_URL` 指向 `schema=goods_sell_test` 后执行 `npx.cmd prisma migrate reset --force` | 通过 | 本机账号无 `CREATE DATABASE` 权限，改用独立 schema；迁移与 seed 成功，seed 商品数 33 |
| 测试 schema 只读 smoke | `DATABASE_URL` 指向 `schema=goods_sell_test` 后执行 `npm.cmd run test:phase6` | 通过 | 输出 `Phase 6 smoke passed { totalSku: 33, deliveryRows: 0, payments: 0, users: 4, logs: 0 }` |
| API smoke | 测试服务 `http://localhost:3100` 指向 `schema=goods_sell_test` 后执行 `npm.cmd run test:phase5` | 通过 | 输出 `Phase 5 smoke passed` |
| 路由 smoke | 测试服务 `http://localhost:3100` 指向 `schema=goods_sell_test` 后访问公开页和未登录保护页 | 通过 | `/` 跳转 `/shop`，商城公开页返回 200，后台/购物车/经销商端未登录访问跳转登录 |
| API 下单 E2E | 临时将 `.env.local` 的 `DATABASE_URL` 指向 `schema=goods_sell_test`，启动 `http://localhost:3100` 后执行小程序登录、地址、购物车、结算、下单、订单列表、分享 | 通过 | 输出 `API_MINI_FLOW_PASS`；订单为 `PENDING_PAYMENT`，未支付前不扣库存 |
| 数据库全功能演练 | `DATABASE_URL` 指向 `schema=goods_sell_test` 后执行客户、订单支付出库、经销商申请审核、分单接单/拒单、询价报价转订单、优惠券、新品推送、渠道冲突、收款和审计日志闭环 | 通过 | 输出 `DB_FULL_FUNCTION_FLOW_PASS {"orders":3,"leads":1,"inquiries":1,"quotes":1,"conflicts":2,"auditLogs":1,"convertedPushes":1}` |
| 多角色登录与权限 E2E | 临时将 `.env.local` 的 `DATABASE_URL` 指向 `schema=goods_sell_test`，通过 NextAuth credentials 登录管理员、销售员、仓管、财务、消费者、经销商，并访问授权/未授权入口 | 通过 | 输出 `AUTH_ROLE_FLOW_PASS {"accounts":["ADMIN","SALESPERSON","WAREHOUSE","FINANCE","CONSUMER","DEALER"]}` |
| API 异常与边界 smoke | 测试商品详情、匿名访问地址/订单/预支付/AI、非法支付回调、匿名结算等边界 | 通过 | 输出 `API_BOUNDARY_SMOKE_PASS`；未登录与非法回调均按预期返回 401 或 400 |

### 4.3 复核结论

- 原测试方案与当前功能模块总体匹配。
- 最新代码新增或强化了 RBAC、`/forbidden`、`test:rbac`，必须纳入 P0 验收。
- 独立 `tsx` 脚本不会自动加载 `.env.local`，执行 Phase 6 等数据库脚本时需要显式加载环境变量。
- `test:phase5` 会模拟微信登录和分享记录，属于写数据测试，必须在独立测试库和测试服务上执行。
- 当前本机 PostgreSQL 账号无 `CREATE DATABASE` 权限时，可使用同一数据库下的独立 schema 作为临时测试隔离方案，例如在 `DATABASE_URL` 增加 `?schema=goods_sell_test`。该方案不得替代正式上线前的独立测试库验收。

## 5. 测试范围

### 5.1 必测范围

| 模块 | 测试内容 | 优先级 |
|------|----------|--------|
| 登录与权限 | 注册、登录、退出、角色菜单和接口权限 | P0 |
| 商品管理 | 商品、分类、品牌、图片、价格、上下架、阈值 | P0 |
| 库存管理 | 入库、出库、盘点、库存流水、安全库存预警 | P0 |
| 采购管理 | 供应商、采购单、到货入库 | P1 |
| 商城端 | 首页、目录、详情、购物车、结算、地址、订单 | P0 |
| 场景化入口 | 宴席配酒、企业团购、门店补货、经销商/业务员入口 | P0 |
| 线索询价报价 | 线索生成、分派、跟进、询价、报价、转订单 | P0 |
| 订单管理 | 手动开单、订单详情、状态流转、导出 | P0 |
| 分单与渠道保护 | 小单经销商承接、大单公司统筹、拒单转派、分单记录 | P0 |
| 经销商端 | 待接订单、我的订单、库存上报、结算、推广、线索 | P0 |
| 配送管理 | 发货、配送中、送达、订单联动 | P1 |
| 财务管理 | 应收、收款登记、核销、对账单、票据 | P0 |
| 营销运营 | 优惠券、活动、新品推送、客户分层、经销商分层 | P1 |
| AI互动 | AI选品、趣味互动、画像、线索沉淀 | P1 |
| 微信生态 | 小程序接口、公众号菜单、支付预下单、支付回调 | P0/P1 |
| 系统设置 | 业务配置、用户管理 | P0 |
| 操作日志 | 关键操作记录、筛选、追溯 | P0 |
| 合规提示 | 酒类禁售、适度饮酒、个人信息授权 | P0 |
| RBAC 专项 | `/forbidden`、角色菜单、路由权限、错误角色 action 拦截 | P0 |

### 5.2 暂不作为上线阻断的范围

- 大规模并发压测。
- 原生 App 兼容测试。
- 第三方 ERP 深度对接。
- AI 直播。
- 多仓库调拨。
- 完整自动化测试覆盖。

## 6. 测试环境

| 环境项 | 建议 |
|--------|------|
| 应用环境 | 独立测试环境或本地验收环境 |
| 数据库 | 使用独立 PostgreSQL 测试库，禁止连接正式生产库或当前业务库 |
| 微信支付 | 优先使用沙箱或模拟支付，真实支付仅做小额验证 |
| 微信小程序 | 使用体验版或开发版 |
| 公众号 | 使用测试公众号或测试菜单 |
| AI服务 | 使用测试密钥，限制调用额度 |
| 地图服务 | 使用测试 Key，确认域名白名单 |
| 浏览器 | Chrome、Edge、Safari/微信浏览器抽测 |
| 设备 | PC、手机浏览器、微信内置浏览器 |

### 6.1 独立测试库准备与禁止事项

上线前全功能测试必须使用独立测试库。建议流程：

1. 新建 PostgreSQL 测试库，例如 `goods_sell_test`。
2. 复制 `.env.local` 为测试环境配置，并确认 `DATABASE_URL` 指向测试库。
3. 仅对测试库执行 Prisma 迁移和 seed。
4. 验证 seed 后至少包含 4 个内部账号、33 个 SKU、经销商、订单、收款、日志和系统配置数据。
5. 启动测试服务时使用测试环境变量。
6. 使用测试库运行 Phase 5、商城下单、库存、收款、对账、推广码、经销商接单等写数据测试。

如果本机账号没有 `CREATE DATABASE` 权限，可以临时使用独立 schema 执行开发期 smoke：

```powershell
# 在原 DATABASE_URL 基础上增加 ?schema=goods_sell_test
npx.cmd prisma migrate reset --force
```

独立 schema 只适合开发期隔离验证。正式封闭试运营前，仍应由数据库管理员创建独立测试库。

禁止事项：

- 禁止在生产库执行 `prisma db seed`。
- 禁止在当前业务库执行 Phase 5、支付模拟、下单、库存、财务、推广码等写数据测试。
- 禁止使用真实客户手机号、地址、企业税号或个人隐私数据。
- 禁止把测试库账号密码写入公开仓库。

### 6.2 Windows PowerShell 命令注意事项

在 Windows PowerShell 中，`npm.ps1` 和 `npx.ps1` 可能被执行策略拦截。测试命令统一使用：

```powershell
npm.cmd run lint
npx.cmd tsc --noEmit
```

执行独立 `tsx` 数据库脚本前，需要显式加载环境变量。示例：

```powershell
Get-Content .env.local | ForEach-Object {
  if ($_ -match '^\s*([^#][A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)$') {
    $v = $matches[2].Trim()
    if (($v.StartsWith('"') -and $v.EndsWith('"')) -or ($v.StartsWith("'") -and $v.EndsWith("'"))) {
      $v = $v.Substring(1, $v.Length - 2)
    }
    [Environment]::SetEnvironmentVariable($matches[1], $v, 'Process')
  }
}
npm.cmd run test:phase6
```

## 7. 测试执行批次与命令

### 7.1 第一轮：静态质量门禁

执行状态：已完成。

| 顺序 | 命令 | 通过标准 |
|------|------|----------|
| 1 | `npx.cmd tsc --noEmit` | 无类型错误 |
| 2 | `npm.cmd run lint` | ESLint 无错误 |
| 3 | `npm.cmd run test:rbac` | 输出 `RBAC smoke passed` |
| 4 | `npm.cmd run build` | Next.js 生产构建成功 |
| 5 | `npm.cmd audit --audit-level=moderate` | 无中高风险生产依赖漏洞，或有明确接受说明 |

### 7.2 第二轮：只读 smoke

执行状态：已完成。

| 顺序 | 命令 | 通过标准 |
|------|------|----------|
| 1 | 临时加载测试环境变量后执行 `npm.cmd run test:phase6` | 仓储、配送、票据、设置、用户、日志查询通过 |

Phase 6 只做查询和未登录重定向验证，仍要求指向测试库，避免误读生产数据。

### 7.3 第三轮：API smoke

执行状态：已完成。

执行条件：
- 测试服务已启动。
- `DATABASE_URL` 指向独立测试库。
- 可接受写入模拟微信客户、购物车/分享相关测试记录。

命令：

```powershell
npm.cmd run dev
npm.cmd run test:phase5
```

通过标准：
- 小程序首页 API 返回 200。
- 小程序目录 API 返回 200。
- 未登录购物车 API 返回 401。
- 模拟微信登录返回 token。
- 已登录购物车 API 返回 200。
- 分享记录 API 返回 200。
- 未登录后台同步公众号菜单返回 403。

### 7.4 第四轮：P0 全功能闭环

执行状态：部分完成。已完成自动化 API E2E、数据库业务闭环、多角色权限 E2E 和异常边界 smoke；仍需在正式独立测试库和人工验收环境中按 P0-001 到 P0-064 逐项签收。

按本方案 P0-001 到 P0-064 执行。每条用例必须记录：
- 执行账号。
- 测试数据。
- 实际结果。
- 通过/失败/阻塞。
- 订单号、线索号、询价单号、报价单号或截图。

### 7.5 第五轮：P1 运营能力

执行状态：部分完成。优惠券、新品推送、渠道冲突等核心数据闭环已验证；客户/经销商分层、真实微信链路、AI 交互体验仍需在正式测试环境人工验收。

按 P1-001 到 P1-012 执行。若存在第三方接口不可用，应记录为阻塞，并说明测试替代方式或人工验收方式。

## 8. 测试账号建议

| 角色 | 账号建议 | 说明 |
|------|----------|------|
| 管理员 | admin_test | 全局配置和验收 |
| 销售员A | sales_a_test | 负责宴席、团购和客户跟进 |
| 销售员B | sales_b_test | 用于权限隔离和推广码归因 |
| 仓管 | warehouse_test | 入库、出库、盘点和配送 |
| 财务 | finance_test | 收款、核销、对账和开票 |
| 经销商A | dealer_a_test | 城区经销商，正常接单 |
| 经销商B | dealer_b_test | 城区经销商，用于拒单转派 |
| 经销商C | dealer_c_test | 跨区域或超半径经销商 |
| 普通客户 | customer_test | 普通商城下单 |
| 宴席客户 | banquet_test | 大单询价 |
| 企业客户 | company_test | 团购询价和开票 |
| 门店客户 | store_test | 门店补货 |

测试账号密码应在测试环境单独配置，不应写入公开仓库。

## 9. 测试数据准备

### 9.1 商品数据

至少准备以下商品：

| 类型 | 数量 | 说明 |
|------|------|------|
| 白酒 | 5 | 覆盖高价、低价、畅销、礼盒 |
| 啤酒 | 3 | 适合小单和门店补货 |
| 饮料 | 5 | 宴席搭配和普通购买 |
| 食品 | 3 | 组合销售和普通商品 |
| 缺货商品 | 1 | 用于库存不足测试 |
| 下架商品 | 1 | 用于不可购买测试 |

每个商品应配置：
- SKU、名称、分类、品牌、单位、规格。
- 成本价、批发价、零售价。
- 库存、安全库存。
- 商品图片。
- 大单阈值。

### 9.2 经销商数据

至少准备 3 个经销商：

| 经销商 | 配置 | 用途 |
|--------|------|------|
| 经销商A | 距离客户近、库存充足、正在接单 | 正常分单 |
| 经销商B | 距离客户近、允许接单、可拒单 | 拒单转派 |
| 经销商C | 超出服务半径或不允许跨区 | 分单排除 |

每个经销商应配置：
- 门店名称、联系人、手机号。
- 经纬度、区域、服务半径。
- 是否接单。
- 最小/最大接单金额。
- 价格等级。
- 可接品牌。
- 拒单规则。
- 门店库存。

### 9.3 客户与地址数据

至少准备：
- 城区普通客户地址。
- 接近经销商 A 的客户地址。
- 接近经销商 B 的客户地址。
- 超出经销商服务半径的客户地址。
- 宴席酒店地址。
- 企业收货地址。
- 门店补货地址。

### 9.4 业务数据

至少准备：
- 2 个供应商。
- 2 张采购单。
- 3 张优惠券。
- 2 个营销活动。
- 3 个业务员推广码。
- 3 个经销商门店码。
- 5 条客户标签。
- 3 条新品推送任务。

## 10. 缺陷等级

| 等级 | 定义 | 处理要求 |
|------|------|----------|
| S1 阻断 | 系统无法登录、下单、收款、库存错误、数据丢失、越权访问、支付错误 | 上线前必须修复 |
| S2 严重 | 主流程可走但结果错误、分单错误、财务口径错误、订单状态异常 | 上线前必须修复或有明确人工兜底 |
| S3 一般 | 单个功能异常、不影响核心运营、有替代操作 | 可进入试运营，但需排期修复 |
| S4 建议 | 文案、样式、交互优化、低频体验问题 | 不阻断上线 |

## 11. 上线准入标准

满足以下条件才建议进入封闭试运营：

1. 所有 P0 测试用例通过率达到 100%。
2. 不存在未关闭的 S1 缺陷。
3. S2 缺陷已修复，或业务负责人确认有人工兜底方案。
4. 商品、库存、经销商、价格和客户测试数据通过抽查。
5. 小单经销商承接、大单公司报价、拒单转派三条主链路通过。
6. 收款、应收、对账和订单金额口径一致。
7. 操作日志可追溯关键操作。
8. 客户侧合规提示和个人信息授权可见。
9. 已完成一次模拟运营演练。
10. 已明确试运营范围、参与人员、应急联系人和回滚方案。

## 12. P0 主链路测试用例

### 12.1 登录与权限

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-001 | 管理员登录 | 使用管理员账号登录后台 | 登录成功，进入仪表盘 |
| P0-002 | 销售员登录 | 使用销售员账号登录后台 | 登录成功，仅看到授权菜单和名下数据 |
| P0-003 | 仓管登录 | 使用仓管账号登录后台 | 可进入库存、仓库和配送相关页面 |
| P0-004 | 财务登录 | 使用财务账号登录后台 | 可进入财务、应收、收款和对账页面 |
| P0-005 | 经销商登录 | 使用经销商账号进入经销商端 | 可查看待接订单、库存上报、结算和推广 |
| P0-006 | 客户登录 | 使用客户账号进入商城端 | 可查看个人订单、地址和购物车 |
| P0-007 | 越权访问 | 销售员尝试访问用户管理或系统设置 | 被拒绝或看不到操作入口 |
| P0-008 | 退出登录 | 任意角色退出登录后访问后台页面 | 跳转登录或无权限访问 |

### 12.2 商品、库存与采购

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-009 | 新增商品 | 管理员新增商品并填写价格、库存和阈值 | 商品保存成功，可在列表和商城展示 |
| P0-010 | 编辑商品 | 修改商品价格、图片和安全库存 | 保存成功，前后台展示一致 |
| P0-011 | 下架商品 | 将商品设为下架 | 商城不可购买，后台仍可查看 |
| P0-012 | 入库 | 对某商品执行采购入库 | 库存增加，生成库存流水 |
| P0-013 | 出库 | 对某商品执行订单出库或手工出库 | 库存减少，生成库存流水 |
| P0-014 | 库存不足 | 购买缺货或库存不足商品 | 禁止提交或提示库存不足 |
| P0-015 | 库存预警 | 将商品库存降至安全库存以下 | 库存预警可见 |
| P0-016 | 盘点确认 | 创建盘点单并确认差异 | 库存调整为实盘数量，生成盘点记录 |

### 12.3 商城下单

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-017 | 浏览首页 | 客户进入 `/shop` | 首页场景入口、分类、推荐商品正常展示 |
| P0-018 | 商品搜索筛选 | 客户进入商品目录搜索和筛选 | 商品结果正确 |
| P0-019 | 商品详情 | 进入商品详情页 | 图片、价格、规格、库存、购买入口正常 |
| P0-020 | 加入购物车 | 添加商品到购物车并修改数量 | 数量和金额正确 |
| P0-021 | 地址管理 | 新增、编辑、设为默认地址 | 地址保存成功，结算页可选择 |
| P0-022 | 提交普通订单 | 客户从购物车结算提交小单 | 订单创建成功，金额正确 |
| P0-023 | 订单详情 | 客户查看我的订单详情 | 商品、金额、状态、地址正确 |
| P0-024 | 取消订单 | 客户取消未履约订单 | 状态更新为已取消 |

### 12.4 场景化入口、线索、询价、报价

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-025 | 宴席询价 | 提交桌数、预算、日期、地址和联系方式 | 生成线索和询价单 |
| P0-026 | 企业团购 | 提交企业团购、开票和配送需求 | 生成团购询价 |
| P0-027 | 门店补货 | 提交门店类型、补货品类和预算 | 生成补货线索或询价 |
| P0-028 | 推广码归因 | 通过业务员推广码提交线索 | 线索关联正确业务员和推广码 |
| P0-029 | 经销商码归因 | 通过经销商门店码提交需求 | 线索关联正确经销商 |
| P0-030 | 线索分派 | 管理员将线索分派给销售员 | 线索状态和负责人更新 |
| P0-031 | 创建报价 | 销售员基于询价创建报价单 | 报价金额、内容和有效期正确 |
| P0-032 | 报价转订单 | 将已接受报价转为订单 | 订单创建成功，关联报价和客户 |

### 12.5 分单与经销商端

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-033 | 小单分给经销商 | 客户下小额普通订单，地址靠近经销商A | 订单路由为经销商，生成分单记录 |
| P0-034 | 大单公司统筹 | 客户提交超过阈值订单或团购订单 | 不直接分给经销商，进入公司/业务员处理 |
| P0-035 | 经销商接单 | 经销商A在待接订单中接单 | 订单分单状态为已接，订单进入待履约 |
| P0-036 | 经销商拒单 | 经销商B拒绝待接订单 | 分单状态为拒绝，可转派或回公司 |
| P0-037 | 超半径排除 | 客户地址超出经销商服务半径 | 该经销商不应被匹配为优先履约方 |
| P0-038 | 库存不足排除 | 经销商库存不足 | 不应优先分派给该经销商 |
| P0-039 | 经销商库存上报 | 经销商修改商品库存 | 库存上报成功，后台可见 |
| P0-040 | 经销商推广统计 | 通过经销商推广码产生线索或订单 | 扫码、线索、订单统计更新 |

### 12.6 订单、配送、财务

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-041 | 手动开单 | 销售员或管理员为客户手动创建订单 | 订单创建成功，来源为手动 |
| P0-042 | 订单确认 | 后台确认已支付或待确认订单 | 状态更新正确 |
| P0-043 | 发货 | 仓管对订单执行发货 | 配送状态更新，库存扣减符合规则 |
| P0-044 | 送达完成 | 配送完成后更新订单 | 订单进入已送达或已完成 |
| P0-045 | 收款登记 | 财务为赊账订单登记回款 | 支付记录生成，应收减少 |
| P0-046 | 核销订单 | 回款关联订单核销 | 订单已收金额和客户余额正确 |
| P0-047 | 对账单 | 按客户和时间段生成对账单 | 订单、收款、未收金额一致 |
| P0-048 | 开票记录 | 为订单或收款登记发票 | 发票信息保存并可查询 |

### 12.7 日志与合规

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-049 | 操作日志 | 新增商品、修改订单、登记收款 | 日志记录操作人、模块、动作和摘要 |
| P0-050 | 酒类提示 | 客户进入酒类商品或商城关键页面 | 可见未成年人禁售和适度饮酒提示 |
| P0-051 | 个人信息授权 | 客户提交手机号、地址或偏好 | 有授权提示或同意记录 |
| P0-052 | 敏感数据权限 | 非财务角色访问对账或收款详情 | 无权限或仅可查看授权范围 |

### 12.8 RBAC 专项

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P0-053 | 未登录访问后台订单 | 未登录访问 `/dashboard/orders` | 跳转 `/login?callbackUrl=/dashboard/orders` |
| P0-054 | 消费者访问后台 | 消费者访问 `/dashboard` | 跳转 `/forbidden` |
| P0-055 | 经销商访问后台 | 经销商访问 `/dashboard` | 跳转 `/forbidden` |
| P0-056 | 经销商访问购物车 | 经销商访问 `/shop/cart` | 跳转 `/forbidden` |
| P0-057 | 销售员菜单过滤 | 销售员登录后台查看菜单 | 不显示库存、采购、仓储、财务、票据、设置、日志菜单 |
| P0-058 | 仓管菜单过滤 | 仓管登录后台查看菜单 | 只显示产品只读、库存、采购、订单履约、仓储、配送等菜单 |
| P0-059 | 财务菜单过滤 | 财务登录后台查看菜单 | 显示订单/客户只读、财务、票据、销售报表，不显示仓储和渠道经营写操作入口 |
| P0-060 | 消费者注册 | 注册消费者账号 | 注册成功后可登录并进入 `/shop` |
| P0-061 | 经销商申请 | 提交经销商申请 | 创建 `DEALER_JOIN` 线索，账号不可登录 |
| P0-062 | 管理员审核通过经销商 | 管理员审核通过经销商申请 | 创建 `Dealer` 档案，申请线索变为 `CONVERTED`，经销商可登录 `/dealer/incoming` |
| P0-063 | 管理员驳回经销商 | 管理员驳回经销商申请 | 申请线索变为 `LOST`，账号仍不可登录 |
| P0-064 | 错误角色 action 拦截 | 错误角色调用库存、财务或设置 action | 返回 `UNAUTHORIZED` 或无权限错误 |

## 13. P1 运营能力测试用例

| 编号 | 用例 | 步骤 | 预期结果 |
|------|------|------|----------|
| P1-001 | 优惠券创建 | 创建满减券并设置有效期和数量 | 优惠券保存成功 |
| P1-002 | 优惠券领取 | 客户领取可用优惠券 | 客户优惠券列表展示 |
| P1-003 | 优惠券使用 | 客户结算时使用优惠券 | 应付金额正确扣减 |
| P1-004 | 新品推送 | 按客户标签创建推送 | 推送记录生成，状态可更新 |
| P1-005 | 推送转化 | 标记打开、点击、转化 | 数据统计正确 |
| P1-006 | 客户分层 | 查看客户列表分层筛选 | 分层结果符合订单和画像数据 |
| P1-007 | 经销商分层 | 查看经销商动态分层 | 接单、推广、库存、冲突指标展示 |
| P1-008 | AI选品 | 客户使用 AI 询问宴席配酒 | AI 返回合理建议，并可沉淀线索 |
| P1-009 | 趣味互动 | 客户参与测试或签到 | 记录互动结果、积分或标签 |
| P1-010 | 渠道冲突 | 创建跨区、拒单或投诉台账 | 可分派责任人、更新状态、关闭 |
| P1-011 | 微信小程序首页 | 调用小程序首页接口 | 返回分类、商品和推荐数据 |
| P1-012 | 公众号菜单 | 保存或发布公众号菜单配置 | 菜单配置成功或提示明确 |

## 14. 异常与边界测试

| 编号 | 场景 | 测试点 | 预期结果 |
|------|------|--------|----------|
| E-001 | 手机号重复 | 注册或新增客户使用已存在手机号 | 提示手机号已存在 |
| E-002 | 商品库存为 0 | 客户购买无库存商品 | 禁止购买或提示缺货 |
| E-003 | 优惠券过期 | 结算使用过期优惠券 | 不可使用 |
| E-004 | 地址缺少经纬度 | 提交订单后无法地图匹配 | 可回退公司仓库或提示补全地址 |
| E-005 | 没有可用经销商 | 所有经销商关闭接单或超半径 | 转公司仓库处理 |
| E-006 | 经销商频繁拒单 | 超过每日拒单上限 | 生成渠道冲突或降低优先级 |
| E-007 | 支付回调重复 | 同一支付通知重复进入 | 不重复增加收款，不重复改库存 |
| E-008 | 订单取消后发货 | 已取消订单执行发货 | 禁止发货 |
| E-009 | 已完成订单退款 | 对已完成订单发起退款 | 状态进入退款流程或提示需人工处理 |
| E-010 | 非本人订单 | 客户访问他人订单详情 | 禁止访问 |
| E-011 | 销售员访问他人客户 | 销售员查看非名下客户 | 无权限或只读受限 |
| E-012 | AI服务超时 | AI接口无法返回 | 前端提示稍后再试，不影响其他功能 |

## 15. 兼容性测试

| 端 | 环境 | 测试重点 |
|----|------|----------|
| PC后台 | Chrome、Edge | 表格、筛选、表单、弹窗、导出、图表 |
| 手机浏览器 | Chrome、Safari | 商城首页、目录、购物车、结算、场景页 |
| 微信浏览器 | 微信内置浏览器 | 授权、下单、支付、分享、页面适配 |
| 小程序 | 开发版/体验版 | 登录、首页、目录、购物车、订单接口 |

兼容性验收重点：
- 页面无明显错位。
- 表单按钮可点击。
- 长文本不溢出。
- 商品图片正常加载。
- 移动端结算、地址和订单流程可完成。

## 16. 性能与稳定性抽测

正式压测可后续单独执行，但试运营前建议完成以下抽测：

| 场景 | 目标 |
|------|------|
| 后台首页加载 | 常规数据量下可正常打开 |
| 商品目录加载 | 100 个商品以内响应可接受 |
| 订单提交 | 不出现重复订单或卡死 |
| 库存扣减 | 并发小批量下不出现负库存 |
| 导出订单 | 1000 条以内可导出或有明确等待提示 |
| AI 对话 | 超时有提示，不阻塞页面 |

## 17. 模拟运营演练

上线前建议组织一次 2-4 小时模拟运营演练。

### 17.1 参与人员

- 管理员 1 人。
- 销售员 2 人。
- 仓管 1 人。
- 财务 1 人。
- 经销商 2-3 家。
- 测试客户 5-10 人。
- 开发或运维 1 人值守。

### 17.2 演练剧本

1. 普通客户下小单，经销商 A 接单并完成配送。
2. 普通客户下小单，经销商 B 拒单，系统转派或转公司处理。
3. 宴席客户提交大单询价，销售员创建报价并转订单。
4. 企业客户提交团购询价，要求开票和分批配送。
5. 门店客户提交补货需求，业务员跟进并转订单。
6. 仓管执行入库、出库和盘点。
7. 财务登记一笔现金收款、一笔转账收款、一笔赊账核销。
8. 运营创建优惠券并让客户使用。
9. 运营执行一次新品推送并标记转化。
10. 管理员处理一条渠道冲突并关闭。

### 17.3 演练通过标准

- 每个剧本均能完成闭环。
- 金额、库存、订单状态和客户归属无明显错误。
- 操作人员能理解自己的入口和待办。
- 所有异常有人工兜底方案。
- 演练问题已记录负责人和修复计划。

## 18. 测试记录模板

| 字段 | 填写说明 |
|------|----------|
| 用例编号 | 如 P0-001 |
| 测试模块 | 登录、订单、库存等 |
| 测试人员 | 执行人 |
| 测试时间 | 年月日 时分 |
| 测试环境 | 本地、测试服、体验版等 |
| 测试数据 | 使用的账号、商品、订单号等 |
| 执行结果 | 通过/不通过/阻塞 |
| 实际结果 | 记录真实表现 |
| 缺陷等级 | S1/S2/S3/S4 |
| 责任人 | 修复或确认人 |
| 修复状态 | 未处理/处理中/已修复/已验证/延期 |
| 备注 | 其他说明 |

## 19. 缺陷记录模板

| 字段 | 填写说明 |
|------|----------|
| 缺陷编号 | BUG-YYYYMMDD-001 |
| 标题 | 简短描述问题 |
| 所属模块 | 订单、库存、财务等 |
| 严重等级 | S1/S2/S3/S4 |
| 复现步骤 | 清晰列出操作步骤 |
| 预期结果 | 应该发生什么 |
| 实际结果 | 实际发生什么 |
| 影响范围 | 影响角色、流程、数据 |
| 截图/日志 | 页面截图、控制台、服务端日志 |
| 责任人 | 开发或业务确认人 |
| 修复版本 | 对应发布版本 |
| 验证结论 | 通过/不通过 |

## 20. 试运营建议

全功能测试通过后，建议不要立即全面开放，而是先进入封闭试运营：

| 阶段 | 范围 | 时间 | 目标 |
|------|------|------|------|
| 内部试用 | 公司内部人员 | 1-2 天 | 熟悉系统、发现明显问题 |
| 小范围试运营 | 2-3 家经销商、10-30 个客户 | 3-7 天 | 验证真实下单、分单、收款和配送 |
| 扩大试运营 | 10 家以内经销商、100 个以内客户 | 1-2 周 | 验证运营承接能力和异常处理 |
| 正式运营 | 全部目标客户和经销商 | 根据前期结果决定 | 稳定运营和数据复盘 |

试运营期间应每日复盘：
- 新增线索、询价、订单数量。
- 小单经销商承接率。
- 拒单和渠道冲突。
- 库存准确率。
- 收款和应收差异。
- 客户反馈。
- 系统错误和慢接口。

## 21. 上线风险清单

| 风险 | 影响 | 建议 |
|------|------|------|
| 商品价格错误 | 直接造成损失或渠道投诉 | 上线前双人复核价格 |
| 库存不准 | 超卖、缺货、配送失败 | 上线前盘点核心商品 |
| 分单规则不准 | 经销商不满或客户体验差 | 先小范围验证经销商半径和政策 |
| 财务口径不一致 | 对账困难 | 财务参与测试并确认口径 |
| 微信支付配置错误 | 无法付款或回调失败 | 先做小额真实支付验证 |
| 客户隐私授权不足 | 合规风险 | 已内置 `/privacy`、`/terms` 和注册授权勾选；正式上线前仍需法务/业务复核签收 |
| 酒类营销文案违规 | 监管和品牌风险 | 上线前人工审核页面和 AI 话术 |
| 运营人员不熟悉 | 真实订单处理延误 | 做角色培训和演练 |
| 缺少应急方案 | 问题扩大化 | 准备人工接单、手工对账和回滚方案 |

## 22. 测试结论模板

测试完成后，应形成测试结论：

```text
测试版本：
测试时间：
测试环境：
测试负责人：

用例总数：
通过数量：
失败数量：
阻塞数量：

S1 缺陷数量：
S2 缺陷数量：
S3 缺陷数量：
S4 缺陷数量：

是否满足封闭试运营条件：
是否允许正式运营：

必须修复项：
可延期项：
人工兜底方案：
业务负责人确认：
技术负责人确认：
```

建议结论分为三类：

| 结论 | 含义 |
|------|------|
| 通过 | 可进入封闭试运营或正式发布 |
| 有条件通过 | 存在非阻断问题，但有人工兜底和修复计划 |
| 不通过 | 存在 S1/S2 阻断问题，不建议上线 |

## 23. 本轮测试结论（2026-04-30）

| 项目 | 结论 |
|------|------|
| 静态质量门禁 | 通过 |
| RBAC smoke | 通过 |
| 构建检查 | 通过 |
| 依赖审计 | 通过，0 vulnerabilities |
| Phase 5 API smoke | 通过 |
| Phase 6 运维 smoke | 通过 |
| 路由 smoke | 通过 |
| API 小程序下单 E2E | 通过 |
| 数据库全功能演练 | 通过 |
| 多角色登录与权限 E2E | 通过 |
| API 异常与边界 smoke | 通过 |
| 生产构建与启动 smoke | 通过 |
| 生产构建脚本复核 | 已将 `package.json` 的 `build` 从 `next build --turbopack` 调整为 `next build`，避免 `next start` 启动 Turbopack 产物时报 `routesManifest.dataRoutes is not iterable` |
| 收尾门禁复测 | 通过；`npx.cmd tsc --noEmit`、`npm.cmd run lint`、`npm.cmd run test:rbac` 均通过 |
| 测试隔离 | 已使用 `goods_sell_test` schema；`.env.local` 已恢复为原 `goods_sell` 数据库 |
| 遗留限制 | 本机账号无 `CREATE DATABASE` 权限，正式封闭试运营前仍建议创建独立测试库；真实微信支付回调、小程序体验版、人工浏览器逐项验收需在正式测试环境执行 |

本轮结论：当前代码通过可自动化执行的上线前质量门禁、API smoke、路由 smoke、数据库业务闭环测试以及生产 `build/start` smoke。建议进入封闭试运营准备阶段，但正式运营前仍需完成真实微信环境与人工角色验收。

### 23.1 生产启动复核记录

- 初始发现：使用原 `npm.cmd run build` 生成 Turbopack 产物后，执行 `npm.cmd run start -- -p 3200` 会在启动阶段报错 `routesManifest.dataRoutes is not iterable`，属于上线前必须修复的问题。
- 修复动作：将默认构建脚本改为稳定生产构建 `next build`。
- 复测结果：`npm.cmd run build` 通过；临时将 `.env.local` 指向 `goods_sell_test` schema、将认证 URL 指向 `http://localhost:3200` 并开启 `AUTH_TRUST_HOST=true` 后，`npm.cmd run start -- -p 3200` 通过生产 smoke。
- 覆盖端点：`/api/wechat/mini/home`、`/api/wechat/mini/catalog`、`/`、`/shop`、`/shop/catalog`、`/login`、`/register`、`/forbidden`、`/dashboard/orders`、`/shop/cart`、`/dealer/incoming`、`/api/wechat/mini/cart`、`/api/wechat/mini/orders`。
- 环境恢复：生产 smoke 后已停止本地 3200 服务，`.env.local` 已恢复为原 `goods_sell` 数据库配置。

### 23.2 浏览器模拟逐项验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 数据准备：已对 `goods_sell_test` schema 执行 Prisma migration reset 和 seed。
- 当前状态：本轮浏览器模拟已完成 39 项，其中 P0/RBAC 32 项、P1 7 项，均通过。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| P0-001 | 管理员登录后台 | 通过 | 登录后可访问 `/dashboard` |
| P0-002 | 销售员登录后台 | 通过 | 登录后可访问 `/dashboard/orders` |
| P0-003 | 仓管登录后台 | 通过 | 登录后可访问 `/dashboard/warehouse` |
| P0-004 | 财务登录后台 | 通过 | 登录后可访问 `/dashboard/finance` |
| P0-005 | 消费者登录商城 | 通过 | 登录后可访问 `/shop` |
| P0-006 | 经销商登录经销商端 | 通过 | 登录后可访问 `/dealer/incoming` |
| P0-007 | 销售员越权访问用户管理 | 通过 | 访问 `/dashboard/settings/users` 被重定向到 `/forbidden` |
| P0-008 | 退出登录后访问后台 | 通过 | 退出后访问 `/dashboard` 被重定向到登录页 |
| P0-017 | 商城首页与合规提示 | 通过 | 首页可打开，显示个人信息用途、未成年人禁售、适度饮酒等提示 |
| P0-018 | 商品目录与筛选入口 | 通过 | `/shop/catalog` 显示 33 件商品及酒类/食品/饮料分类筛选 |
| P0-019 | 商品详情页 | 通过 | 可打开可口可乐商品详情页 |
| P0-020 | 加购进入购物车 | 通过 | 点击商品加购后，购物车显示对应商品 |
| P0-021 | 购物车与结算入口 | 通过 | `/shop/cart` 显示购物车与结算入口 |
| P0-022 | 结算页入口 | 通过 | `/shop/checkout` 可打开并显示结算/订单相关信息 |
| P0-023 | 我的订单入口 | 通过 | `/shop/my-orders` 可访问并显示订单相关内容 |
| P0-025 | 宴席配酒入口 | 通过 | `/shop/scenes/banquet` 可打开 |
| P0-026 | 企业团购入口 | 通过 | `/shop/scenes/group-buy` 可打开 |
| P0-027 | 门店补货入口 | 通过 | `/shop/scenes/restock` 可打开 |
| P0-028 | 渠道/经销商入口 | 通过 | `/shop/channel` 可打开 |
| P0-041 | 后台订单页面 | 通过 | 管理员可访问 `/dashboard/orders` |
| P0-044 | 配送管理页面 | 通过 | 管理员可访问 `/dashboard/delivery` |
| P0-047 | 对账单页面 | 通过 | 管理员可访问 `/dashboard/finance/statements` |
| P0-048 | 票据/收款相关页面 | 通过 | 管理员可访问 `/dashboard/receipts` |
| P0-049 | 操作日志页面 | 通过 | 管理员可访问 `/dashboard/logs` |
| P0-052 | 非财务角色访问财务受限 | 通过 | 销售员访问 `/dashboard/finance/payments` 被重定向到 `/forbidden` |
| P0-053 | 未登录访问后台订单 | 通过 | 未登录访问 `/dashboard/orders` 被重定向到 `/login?callbackUrl=/dashboard/orders` |
| P0-054 | 消费者访问后台 | 通过 | 消费者访问 `/dashboard` 被重定向到 `/forbidden` |
| P0-055 | 经销商访问后台 | 通过 | 经销商访问 `/dashboard` 被重定向到 `/forbidden` |
| P0-056 | 经销商访问购物车 | 通过 | 经销商访问 `/shop/cart` 被重定向到 `/forbidden` |
| P0-057 | 销售员权限过滤 | 通过 | 可访问订单；访问仓储、财务、设置被拒绝 |
| P0-058 | 仓管权限过滤 | 通过 | 可访问仓储和库存；访问财务、设置被拒绝 |
| P0-059 | 财务权限过滤 | 通过 | 可访问财务和收款；访问仓储、设置被拒绝 |
| P1-001 | 优惠券页面 | 通过 | 管理员可访问 `/dashboard/marketing/coupons` |
| P1-002 | 新品推送页面 | 通过 | 管理员可访问 `/dashboard/product-pushes` |
| P1-003 | 客户分层/客户页面 | 通过 | 管理员可访问 `/dashboard/customers` |
| P1-004 | 经销商页面 | 通过 | 管理员可访问 `/dashboard/dealers` |
| P1-005 | 渠道冲突页面 | 通过 | 管理员可访问 `/dashboard/channel-conflicts` |
| P1-006 | AI 互动页面 | 通过 | 可访问 `/shop/fun` |
| P1-011 | 微信配置页面 | 通过 | 管理员可访问 `/dashboard/wechat` |

### 23.3 第一批后待继续项

| 范围 | 状态 | 说明 |
|------|------|------|
| P0-009 至 P0-016 | 第二批已部分完成 | 商品新增/编辑/上下架、入库、出库、库存预警和盘点入口已在第二批完成；盘点确认仍需继续 |
| P0-024 | 待继续 | 取消订单需要先通过浏览器创建可取消订单 |
| P0-029 至 P0-040 | 第二批已部分完成 | 线索/询价/报价页面入口、经销商端库存和推广入口已验证；完整询价报价转订单、分单接单/拒单仍需继续 |
| P0-042 至 P0-046 | 第二批已完成 | 订单确认、发货、配送页面联动、赊账订单、收款登记与核销已在第二批完成 |
| P0-050 至 P0-051 | 部分完成 | 合规提示已在首页验证；个人信息授权提交记录仍需结合具体表单继续验收 |
| P0-060 至 P0-064 | 第二批已部分完成 | 消费者注册、经销商申请、管理员审核通过/驳回已完成；错误角色 action 拦截仍需继续 |
| P1-007 至 P1-010、P1-012 | 待继续 | 客户/经销商分层配置写流程、AI 对话效果、渠道冲突处理写流程、微信小程序/公众号真实接口仍需补测 |
| 真实微信支付与小程序体验版 | 环境限制 | 需要微信测试号、体验版或沙箱/小额真实支付环境 |

### 23.4 第二批浏览器模拟逐项验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：后台写流程、库存写入、订单写入、财务核销、注册与经销商审核。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| P0-009 | 新增商品 | 通过 | 通过浏览器新增 `浏览器验收商品379698`，产品列表可见 |
| P0-010 | 编辑商品 | 通过 | 通过浏览器进入编辑页，将新增商品零售价改为 `¥16.00`，列表可见 |
| P0-011 | 商品上下架 | 通过 | 点击新增商品 `上架/下架` 按钮后，列表状态正常更新 |
| P0-012 | 入库 | 通过 | 在 `/dashboard/inventory/stock-in` 提交 `浏览器验收入库`，库存流水页可见入库记录 |
| P0-013 | 出库 | 通过 | 在 `/dashboard/inventory/stock-out` 提交 `浏览器验收出库`，库存流水页可见出库记录 |
| P0-014 | 库存流水 | 通过 | `/dashboard/inventory/records` 可查看入库/出库流水 |
| P0-015 | 库存预警 | 通过 | `/dashboard/inventory` 显示库存总览与预警商品数 |
| P0-016 | 盘点入口 | 通过 | `/dashboard/warehouse` 显示仓储作业、新建盘点和库存预警入口；盘点确认仍需继续执行 |
| P0-035 | 经销商待接订单页 | 通过 | 经销商账号可访问 `/dealer/incoming` |
| P0-039 | 经销商库存页 | 通过 | 经销商账号可访问 `/dealer/stock` |
| P0-040 | 经销商推广统计 | 通过 | 经销商账号可访问 `/dealer/promotion` |
| P0-041 | 手动开单 | 通过 | 后台手动创建订单 `HQ20260430000001` |
| P0-042 | 订单确认 | 通过 | 对 `HQ20260430000001` 点击确认，订单状态流转正常 |
| P0-043 | 订单发货 | 通过 | 对 `HQ20260430000001` 点击发货，订单进入配送相关状态 |
| P0-044 | 配送页面联动 | 通过 | `/dashboard/delivery` 可查看配送相关信息 |
| P0-045 | 赊账订单待核销 | 通过 | 创建赊账订单 `HQ20260430000002` 后，收款登记页出现待核销订单 |
| P0-046 | 收款登记与核销 | 通过 | 在收款登记页填写 `¥75.00` 并确认收款后，待核销订单清除 |
| P0-060 | 消费者注册 | 通过 | 注册测试消费者 `13908554224` 后进入商城 |
| P0-061 | 经销商申请 | 通过 | 提交测试经销商申请 `13808579049`，线索池、经销商审核区、客户列表均可见 |
| P0-062 | 经销商审核通过 | 通过 | 管理员审核通过 `13808579049` 后，该账号可登录 `/dealer/incoming` |
| P0-063 | 经销商申请驳回 | 通过 | 提交测试经销商申请 `13708646301` 后管理员驳回，该账号无法进入经销商端 |

本批备注：

- 商品编辑和上下架在浏览器中已实际执行；早期封装脚本中出现过定位超时，但后续直接定位复测通过。
- 手动开单、赊账订单、收款核销均写入 `goods_sell_test` schema。
- 本批未执行真实支付；微信支付仍保留为正式测试环境补测项。

### 23.5 当前剩余浏览器验收项

| 范围 | 状态 | 说明 |
|------|------|------|
| P0-024 | 第三批已完成 | 消费者创建订单 `HQ20260430000003` 后在“我的订单”取消成功 |
| P0-029 至 P0-032 | 第四批已完成 | 场景询价、线索生成、询价后台可见、报价生成已完成；第四批通过购物车询价生成带商品明细的询价，并完成报价转订单 |
| P0-033 至 P0-038 | 第三批已完成主要动作 | 已构造经销商待接单测试数据，通过浏览器完成接单与拒单；拒单后出现转派/待接变化 |
| P0-050 至 P0-051 | 第三批已完成 | 首页合规提示和场景表单个人信息/酒类合规授权勾选均已验证 |
| P0-064 | 第三批已完成 | 已通过 `npm.cmd run test:rbac` 补充 action/权限 smoke，输出 `RBAC smoke passed` |
| P1-007 至 P1-010、P1-012 | 第三批已部分完成 | AI 对话效果、渠道冲突创建与处理已完成；微信小程序/公众号真实接口、客户/经销商分层配置深度写流程仍需补测 |
| 兼容性与设备 | 待继续 | Edge/Chrome/微信内置浏览器/移动端/小程序体验版仍需正式环境补测 |

### 23.6 第三批浏览器模拟逐项验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：取消订单、询价报价、经销商接拒单、盘点确认、AI 对话、渠道冲突处理、RBAC action 级补充。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| P0-024 | 客户取消订单 | 通过 | 消费者结算生成 `HQ20260430000003`，在 `/shop/my-orders` 点击“取消订单”后状态变化可见 |
| P0-029 | 场景线索生成 | 通过 | 宴席配酒表单提交后，后台线索池可见宴席线索 |
| P0-030 | 场景询价提交 | 通过 | 宴席配酒表单提交后，后台询价单 `XJ2026043000001` 可见 |
| P0-031 | 创建报价 | 通过 | 后台报价页基于 `XJ2026043000001` 创建报价单 `BJ2026043000001`，金额 `¥4,888.00` |
| P0-032 | 报价转订单 | 阻塞 | `BJ2026043000001` 显示“缺少商品明细”，页面禁用“转订单”；需补商品明细后复测 |
| P0-033 | 小单经销商分单数据 | 通过 | 在测试 schema 构造两笔 `HQBROWSER...` 经销商待接单用于浏览器接拒单验收 |
| P0-035 | 经销商接单 | 通过 | 经销商端 `/dealer/incoming` 显示待接单，点击“接单”后订单进入 `/dealer/my-orders` |
| P0-036 | 经销商拒单 | 通过 | 对另一笔待接单点击“拒单”后，待接列表发生转派/待接变化 |
| P0-037 | 拒单后转派 | 通过 | 拒单后页面不再保持原两笔待接单状态，系统产生转派/剩余待接变化 |
| P0-050 | 酒类合规提示 | 通过 | 首页和 AI/询价页面显示未成年人禁售、适度饮酒、广告边界提示 |
| P0-051 | 个人信息授权 | 通过 | 宴席询价表单要求勾选个人信息与酒类合规授权后提交 |
| P0-064 | 错误角色 action 拦截 | 通过 | 补跑 `npm.cmd run test:rbac`，输出 `RBAC smoke passed` |
| P1-006 | AI 对话效果 | 通过 | `/shop/ai-chat` 输入宴席预算问题后，返回结构化建议，并提供“提交询价”入口 |
| P1-009 | 渠道冲突创建 | 通过 | 在 `/dashboard/channel-conflicts` 新增“浏览器验收渠道冲突”，台账可见 |
| P1-010 | 渠道冲突处理 | 通过 | 将渠道冲突状态改为“已解决”，填写处理备注并保存后状态可见 |
| P0-016 | 盘点确认 | 通过 | 在 `/dashboard/warehouse` 新建盘点，进入盘点详情并点击“确认盘点并调整库存”成功 |

本批备注：

- 经销商接单/拒单的前置待接单由测试脚本写入 `goods_sell_test` schema，浏览器负责执行实际接单和拒单动作。
- 报价转订单的初始阻塞不是运行错误，而是页面业务规则阻止：第三批场景表单询价缺少商品明细，按钮禁用；第四批已用购物车询价补测通过。
- 微信真实支付、小程序体验版和公众号真实菜单发布仍需正式微信测试环境。

### 23.7 第四批浏览器模拟逐项验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：补测第三批阻塞的“报价转订单”商品明细闭环。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| P0-030 | 购物车询价提交 | 通过 | 消费者 `13900139001` 在商品详情页加购 `可口可乐 330ml*24`，结算页选择“宴席配酒”并提交询价，生成 `XJ2026043000002` |
| P0-031 | 基于购物车询价创建报价 | 通过 | 管理员在报价单页基于 `XJ2026043000002` 创建报价单 `BJ2026043000002`，金额 `¥68.00` |
| P0-032 | 报价转订单 | 通过 | `BJ2026043000002` 的“转订单”按钮可用，点击后进入订单详情，生成订单 `HQ20260430000006` |
| P0-032-A | 转订单明细校验 | 通过 | 订单详情显示商品 `可口可乐 330ml*24`、SKU `HQ-SODA-001`、数量 `1`、金额 `¥68.00`，备注关联报价单和询价单 |
| P0-032-B | 转订单支付记录校验 | 通过 | 订单详情显示转账收款记录 `¥68.00`，订单状态为已支付 |

本批备注：

- 第三批阻塞项已复测通过。业务规则要求报价转订单必须有商品明细；通过购物车结算页生成的询价满足该条件。
- 场景表单直接提交的询价仍适合作为线索/人工报价入口；若未来希望这类询价也可一键转订单，需要在报价前补齐商品明细。

### 23.8 第五批浏览器模拟逐项验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：引入虚拟供应商、客户地址、优惠券、新品推送、渠道政策、开票等数据，继续暴露正式运营前问题。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| P0-016-FIX | 客户已有地址时新增地址 | 已修复并复测 | 发现 `AddressManager` 已有地址时点击“新增地址”不显示表单；已修复创建态判断和地址列表刷新，复测新增 `虚拟验收客户 13977778888` 地址成功 |
| P0-012/P0-013 | 采购收货入库 | 通过 | 新增供应商 `虚拟验收供应商905101`，创建采购单 `PO20260430954346`，完成收货入库并进入已完成状态 |
| P0-039 | 经销商库存上报 | 通过 | 经销商 `验收烟酒店9049` 在 `/dealer/stock` 上报 `康师傅冰红茶 500ml*15` 门店库存 `12`，汇总显示已上报 1、可售库存 12 |
| P0-048 | 开具电子发票 | 通过 | 在 `/dashboard/receipts` 对订单 `HQ20260430000006` 开具 Mock 发票 `FP20260430000001`，购方为 `虚拟验收客户发票抬头` |
| P0-049 | 操作日志与微信消息日志 | 通过 | 操作日志页可访问；微信页新增 `official_menu MOCKED` 菜单日志 |
| P1-001 | 优惠券创建 | 通过 | 新建 `浏览器验收满减券980389`，满 `¥60.00` 减 `¥8.00`，总量 `20` |
| P1-002 | 优惠券定向发放 | 通过 | 优惠券按标签发放，发放统计变化；普通客户看不到“潜在经销商”标签券，符合定向投放逻辑 |
| P1-003 | 客户画像刷新 | 通过 | 客户 `张阿姨` 详情页点击“刷新画像”，生成宴席客户、企业团购、偏好饮料/酒类等画像标签和趋势/偏好数据 |
| P1-004 | 经销商渠道政策配置 | 通过 | 经销商 `验收烟酒店9049` 保存最低金额 `¥50.00`、最高金额 `¥5,000.00`、优先级 `7`、每日拒单上限 `2`、允许跨区和可口可乐品牌限制 |
| P1-007 | 经销商结算中心 | 通过 | 经销商端 `/dealer/settlement` 可访问，显示本月完成订单、订单金额和结算金额 |
| P1-008 | 经销商线索中心 | 通过 | 经销商端 `/dealer/leads` 可访问，显示门店线索统计和已转化申请线索 |
| P1-012 | 新品推送创建与事件复盘 | 通过 | 创建 10 条 `电解质水 500ml*15` 新品推送，记录打开、咨询、试饮事件后，复盘打开率和咨询/试饮指标同步更新 |
| 微信-001 | 公众号菜单 Mock 同步 | 通过 | 未配置公众号密钥时点击“同步公众号菜单”，页面提示已写入模拟菜单日志，消息日志新增 `official_menu MOCKED` |
| 设置-001 | 系统业务参数保存 | 通过 | 在 `/dashboard/settings` 保存安全库存、大小单界线、配送半径、赊账账期等参数，构建复测通过 |
| 权限-人工确认 | 后台用户创建/重置/禁用 | 待人工确认 | 页面可访问并显示创建、重置、禁用入口；因属于账号权限持久变更，需负责人现场确认后再执行最后提交动作 |

本批发现与处理：

- 已修复客户地址新增缺陷：已有地址时新增表单不展开，且保存后本地列表不立即同步。修改文件：`src/features/shop/AddressManager.tsx`。
- 收尾门禁已复测通过：`npx.cmd tsc --noEmit`、`npm.cmd run lint`、`npm.cmd run test:rbac`、`npm.cmd run build`。
- 真实微信支付、小程序体验版、公众号真实菜单发布仍依赖第三方正式测试环境，本批只完成 Mock 和边界能力验证。

### 23.9 第六批浏览器模拟与 Smoke 复核记录

执行环境：

- 测试方式：Codex in-app browser 页面扫描 + 本地 smoke 脚本复核。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：补跑 Phase 5/Phase 6、依赖审计，并对商城、后台、经销商端核心页面做运行时错误扫描。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| Smoke-Phase5 | 微信小程序 API smoke | 通过 | 显式加载 `.env.local` 并设置 `PHASE5_BASE_URL=http://localhost:3300` 后，`npm.cmd run test:phase5` 输出 `Phase 5 smoke passed` |
| Smoke-Phase6 | 运维只读 smoke | 通过 | 显式加载 `.env.local` 并设置 `PHASE6_BASE_URL=http://localhost:3300` 后，输出 `Phase 6 smoke passed { totalSku: 34, deliveryRows: 5, payments: 5, users: 4, logs: 19 }` |
| Audit-001 | 依赖审计 | 通过 | `npm.cmd audit --audit-level=moderate` 输出 `found 0 vulnerabilities` |
| Browser-Core-Shop | 商城核心页面运行时扫描 | 通过 | `/shop`、`/shop/catalog`、商品详情、购物车、结算、账户、地址、我的订单、优惠券、AI 助手均可打开并出现正确标题，无 `Application error` |
| Browser-Core-Dashboard | 后台核心页面运行时扫描 | 通过 | `/dashboard`、订单、客户、经销商、新品推送、票据、微信生态、系统设置均可打开并出现正确标题，无 `Application error` |
| Browser-Core-Dealer | 经销商核心页面运行时扫描 | 通过 | `/dealer/incoming`、`/dealer/my-orders`、推广、线索、库存、结算均可打开并出现正确标题，无 `Application error` |
| Env-Notice | Smoke 脚本环境加载要求 | 已确认 | 直接运行独立 `tsx` smoke 不会自动加载 `.env.local`，Phase 5 还会默认访问 3000；正式执行必须显式加载环境并指定 `PHASE5_BASE_URL`/`PHASE6_BASE_URL` |

本批备注：

- Browser console 中仍可查询到第五批修复前的历史 `prompt() is not supported` 错误日志；修复后地址新增已复测通过，本批核心页面未出现新的 `Application error`。
- in-app browser 不能完全替代 Chrome、Edge、微信内置浏览器和小程序体验版；真实设备兼容仍需正式环境补测。
- 后台用户创建、重置、禁用属于持久账号权限变更，已暂停在最终提交前，等待负责人确认。

### 23.10 第七批后台用户账号变更验收记录

执行环境：

- 测试方式：Codex in-app browser 浏览器模拟。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 本批重点：在测试 schema 中执行后台用户创建、登录、越权拦截、重置密码、禁用、禁用后登录失败验证。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| User-001 | 创建后台销售账号 | 通过 | 管理员在 `/dashboard/settings/users` 创建 `虚拟验收销售736531`，账号 `13877736531`，角色为销售 |
| User-002 | 新销售账号登录 | 通过 | 使用 `13877736531 / staff123` 登录后可访问 `/dashboard/orders` |
| User-003 | 新销售账号越权拦截 | 通过 | 新销售账号访问 `/dashboard/settings/users` 被重定向到 `/forbidden` |
| User-004 | 重置后台账号密码 | 通过 | 管理员对 `虚拟验收销售736531` 点击“重置”，页面提示密码已重置为 `admin123` |
| User-005 | 禁用后台账号 | 通过 | 管理员对 `虚拟验收销售736531` 点击“禁用”，用户列表显示该账号为禁用态并提供“启用”入口 |
| User-006 | 禁用账号登录拦截 | 通过 | 使用 `13877736531 / admin123` 登录后仍停留在登录页，未进入后台 |
| User-007 | 账号变更门禁复测 | 通过 | `npx.cmd tsc --noEmit`、`npm.cmd run lint`、`npm.cmd run test:rbac` 均通过 |

本批备注：

- 本批仅在 `goods_sell_test` schema 中创建和禁用虚拟后台用户，不影响当前业务库。
- 用户管理动作均会写入操作日志，覆盖账号创建、密码重置和禁用审计要求。

### 23.11 第八批最终本地回归与生产启动复核记录

执行环境：

- 测试方式：本地命令回归 + 生产构建产物启动 smoke。
- 测试地址：开发服务 `http://localhost:3300`，生产启动 smoke `http://localhost:3200`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema，完成后恢复原业务库配置。
- 本批重点：在已完成浏览器逐项验收后，补跑质量门禁、Phase 5/Phase 6、生产构建和生产启动入口，确认当前修改没有引入回归。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| Final-001 | TypeScript 类型检查 | 通过 | `npx.cmd tsc --noEmit` 通过 |
| Final-002 | Lint 检查 | 通过 | `npm.cmd run lint` 通过 |
| Final-003 | RBAC 专项 smoke | 通过 | `npm.cmd run test:rbac` 输出 `RBAC smoke passed` |
| Final-004 | 依赖安全审计 | 通过 | `npm.cmd audit --audit-level=moderate` 输出 `found 0 vulnerabilities` |
| Final-005 | 微信小程序 API smoke | 通过 | 在 `http://localhost:3300` 显式加载测试环境变量后，`npm.cmd run test:phase5` 输出 `Phase 5 smoke passed` |
| Final-006 | 运维只读 smoke | 通过 | 在 `http://localhost:3300` 显式加载测试环境变量后，`npm.cmd run test:phase6` 输出 `Phase 6 smoke passed { totalSku: 34, deliveryRows: 5, payments: 5, users: 5, logs: 22 }` |
| Final-007 | 生产构建 | 通过 | `npm.cmd run build` 通过；构建命令已使用标准 `next build` |
| Final-008 | 生产启动 | 通过 | `npm.cmd run start -- -p 3200` 可正常启动，`next start` 输出 Ready |
| Final-009 | 生产公开入口 | 通过 | `/shop`、`/login`、`/forbidden` 返回 `200` |
| Final-010 | 生产微信 API 入口 | 通过 | `/api/wechat/mini/home`、`/api/wechat/mini/catalog` 返回 `200` |
| Final-011 | 生产未登录鉴权 | 通过 | `/dashboard/orders`、`/dealer/incoming` 未登录返回 `307` 重定向 |
| Final-012 | 生产 API 鉴权边界 | 通过 | `/api/wechat/mini/cart` 未登录返回 `401`，`POST /api/wechat/official/menu` 未授权返回 `403` |

本批备注：

- PowerShell `Invoke-WebRequest` 在本机 probe 时出现客户端空引用异常，已改用 `curl.exe` 读取 HTTP 状态码，生产服务本身正常。
- 本批完成后需关闭 3200/3300 测试服务，并将 `.env.local` 恢复到原业务库连接，避免后续误连测试 schema。
- 本地可执行范围内，当前版本已完成上线前主要 P0/P1 模拟验收；真实微信、小程序体验版、多浏览器/移动设备兼容和业务人员签收仍需在正式测试环境执行。

### 23.12 第九批交付检视入口可视化复核记录

执行环境：

- 测试方式：Codex in-app browser 当前浏览器页复核。
- 测试地址：`http://localhost:3300`。
- 数据隔离：临时将 `.env.local` 指向 `goods_sell_test` schema 并启动开发服务。
- 本批重点：在用户当前打开的浏览器环境中，确认交付检视常用入口可正常渲染，不出现空白页或 `Application error`。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| Handoff-001 | 登录页可视化入口 | 通过 | `/login` 返回标题 `华启商城`，页面包含登录/华启相关内容，无 `Application error` |
| Handoff-002 | 商城入口 | 通过 | `/shop` 可打开，页面包含华启/商品相关内容，无 `Application error` |
| Handoff-003 | 后台未登录保护 | 通过 | `/dashboard/orders` 未登录时跳转到 `/login?callbackUrl=%2Fdashboard%2Forders`，无错误页 |
| Handoff-004 | 经销商未登录保护 | 通过 | `/dealer/incoming` 未登录时跳转到 `/login?callbackUrl=%2Fdealer%2Fincoming`，无错误页 |
| Handoff-005 | 无权限页 | 通过 | `/forbidden` 可打开，页面包含无权/返回相关内容，无 `Application error` |

本批备注：

- 本批不新增业务数据，仅验证当前浏览器和本地服务的交付检视入口状态。
- 该轮用于方便项目负责人打开浏览器快速检视，不替代真实 Chrome、Edge、微信内置浏览器、小程序体验版兼容验收。

### 23.13 当前剩余正式验收项

| 范围 | 状态 | 说明 |
|------|------|------|
| 报价转订单 | 第四批已完成 | 购物车询价 `XJ2026043000002` -> 报价 `BJ2026043000002` -> 订单 `HQ20260430000006` 已通过浏览器闭环 |
| 微信小程序体验版 | 环境限制 | API smoke 已通过；仍需要小程序开发版或体验版环境做端内真实验收 |
| 微信支付/回调 | 环境限制 | 模拟支付和非法回调边界已通过；仍需要沙箱或小额真实支付验证 |
| 公众号菜单发布 | 环境限制 | Mock 菜单同步已通过；真实发布需要测试公众号或正式公众号权限 |
| 后台用户账号变更 | 第七批已完成 | 在测试 schema 中完成虚拟后台账号创建、登录、越权拦截、密码重置、禁用和禁用后登录拦截 |
| AI 代办助手真实联调 | 本地测试 schema 已完成 | `test:ai-provider`、`test:ai-tools`、`test:ai-runtime`、`test:ai-full-provider`、类型检查、Lint、生产构建已通过；真实 AI provider 47 tools 全量规划/确认/执行/审计在 `goods_sell_test` 通过，正式测试库仍需业务人员签收 |
| 兼容性验收 | 部分完成 | in-app browser 核心页面运行时扫描已通过；仍需要 Chrome、Edge、微信内置浏览器、移动端、小程序体验版逐项查看 |
| 业务人员签收 | 待执行 | 仓管、财务、销售、经销商和运营负责人需按正式测试库结果签字确认 |

### 23.14 正式环境签收清单

本节用于承接本地模拟验收之后的正式测试环境验收。以下事项必须在独立正式测试库或试运营测试环境执行，不得直接使用生产业务库。

| 签收项 | 责任角色 | 执行环境 | 通过标准 | 当前状态 |
|--------|----------|----------|----------|----------|
| 小程序体验版首页/目录/详情/购物车/订单 | 运营负责人、测试人员 | 微信开发者工具或体验版小程序 | 页面可打开，商品、价格、库存、购物车、订单状态与后台一致 | 待正式环境执行 |
| 小程序授权登录与手机号绑定 | 运营负责人、测试人员 | 微信体验版小程序 | 授权流程可完成，重复登录不会生成异常重复客户，授权提示清晰 | 待正式环境执行 |
| 微信支付与支付回调 | 财务负责人、测试人员 | 微信支付沙箱或小额真实支付 | 支付成功后订单状态、收款记录、日志一致；重复/非法回调不会重复入账 | 待正式环境执行 |
| 微信退款或异常支付兜底 | 财务负责人、测试人员 | 微信支付沙箱或小额真实支付 | 异常订单有明确人工处理入口和财务核对记录 | 待正式环境执行 |
| 公众号菜单真实发布 | 运营负责人 | 测试公众号或正式公众号灰度 | 菜单发布成功，菜单入口跳转正确，失败时有日志和错误提示 | 待正式环境执行 |
| Chrome 兼容验收 | 测试人员 | Windows Chrome 最新稳定版 | 后台、商城、经销商端核心页面无布局错位和运行时错误 | 待正式环境执行 |
| Edge 兼容验收 | 测试人员 | Windows Edge 最新稳定版 | 后台、商城、经销商端核心页面无布局错位和运行时错误 | 待正式环境执行 |
| 微信内置浏览器兼容 | 运营负责人、测试人员 | iOS/Android 微信内置浏览器 | 商城、授权、询价、合规提示、订单页可正常使用 | 待正式环境执行 |
| 移动端 H5 兼容 | 测试人员 | iOS Safari、Android Chrome | 商品浏览、购物车、地址、订单、AI 助手无明显遮挡、溢出或不可点击 | 待正式环境执行 |
| 服务协议与隐私政策 | 业务负责人、法务负责人 | `/terms`、`/privacy`、注册页 | 协议页面可访问，注册必须勾选授权，文本与经营主体、支付、税控和数据留存政策一致 | 基础页面已内置，待正式签收 |
| 消费者/经销商注册授权 | 运营负责人、渠道负责人 | 注册页 | 消费者注册、经销商申请均有服务协议和隐私政策勾选；经销商申请资料进入审核流程 | 基础流程已内置，待正式签收 |
| 仓管业务签收 | 仓管负责人 | 正式测试库后台 | 商品、库存入出库、盘点、采购收货、配送记录可完成并可追溯 | 待签收 |
| 财务业务签收 | 财务负责人 | 正式测试库后台 | 收款、赊账核销、发票、对账金额一致，异常款项有处理路径 | 待签收 |
| 销售业务签收 | 销售负责人 | 正式测试库后台 | 客户、询价、报价、后台开单、跟进记录可支撑日常销售 | 待签收 |
| 经销商业务签收 | 经销商代表、渠道负责人 | 经销商端 | 接单、拒单、库存上报、线索、结算中心符合渠道协作要求 | 待签收 |
| 运营业务签收 | 运营负责人 | 正式测试库后台 | 优惠券、新品推送、客户画像、渠道冲突、微信消息日志可运营 | 待签收 |
| AI 代办助手签收 | 管理员、销售、仓管、财务、经销商、客户代表 | 正式测试库全端 | 气泡可用，权限过滤正确，确认卡可执行，越权拦截，审计日志完整 | 待签收 |

正式环境签收准入结论：

- 若以上签收项中存在未关闭 S1 缺陷，不得进入试运营。
- 若存在 S2 缺陷，必须完成修复，或形成负责人确认的人工兜底方案后方可试运营。
- 真实微信支付、真实公众号菜单和小程序体验版验收未完成前，不建议面向真实客户全面运营；可先限定内部员工、指定经销商和少量测试客户做封闭试运营。
- 业务签收完成后，应冻结测试版本、备份测试数据和验收记录，再安排上线窗口。

### 23.15 Phase 11 AI Tools 回归记录

执行环境：

- 测试方式：本地命令回归。
- 测试范围：AI tool registry、权限过滤、参数校验、确认流程、越权场景和上线检查。
- 本批重点：确认 AI 悬浮气泡背后的 tools 能力已经接入主工作目录，且不会绕过角色权限执行。

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| AI-001 | AI tools smoke | 通过 | `npm.cmd run test:ai-tools` 输出 `AI tools smoke passed: 47 tools` |
| AI-002 | TypeScript 类型检查 | 通过 | `.\node_modules\.bin\tsc.cmd --noEmit` 通过 |
| AI-003 | Lint 检查 | 通过 | `npm.cmd run lint` 通过 |
| AI-004 | 生产构建 | 通过 | `npm.cmd run build` 通过 |
| AI-005 | 客户自然语言下单 | 已自动化覆盖核心逻辑 | “我要下单 1 箱剑兰春”应生成商品、数量、地址、价格确认卡，确认后提交订单或询价 |
| AI-006 | 老板查销售员业绩 | 已自动化覆盖核心逻辑 | “这个月张军业绩怎么样”应调用 `salesperson_performance`，返回销售额、订单数、客户数、回款和排名 |
| AI-007 | 管理员写操作确认 | 已自动化覆盖核心逻辑 | 调价、入库、收款等 WRITE tool 必须先返回确认卡 |
| AI-008 | 高风险二次确认 | 已自动化覆盖核心逻辑 | 禁用账号、重置密码、系统参数等 HIGH_RISK tool 必须校验确认文字和 signed token |
| AI-009 | 越权拦截 | 已自动化覆盖核心逻辑 | 客户不能查他人订单，经销商不能查他人门店，非 ADMIN 不能改系统设置 |
| AI-010 | 上线检查脚本 | 可运行，当前阻塞 | `npm.cmd run check:launch` 正常输出 readiness 报告；当前环境为 `BLOCKER`，`READY 4 / WARNING 5 / BLOCKER 1`，阻塞项为酒类经营资质未配置 |

正式环境补测要求：

- 使用管理员、销售员、仓管、财务、经销商和消费者账号分别打开 AI 气泡，确认只出现权限内能力。
- 在正式测试库执行一次客户下单确认、管理员调价确认、仓管入库确认、财务应收查询和经销商待接单查询。
- 检查 `AuditLog` 中是否记录 WRITE/HIGH_RISK tool 的操作者、对象、参数摘要、结果摘要和失败原因。
- AI 生成的酒类推荐与营销文案仍需遵守未成年人禁售、适度饮酒和酒类广告边界。

### 23.16 AI 代办助手代码复核记录

复核对象：

- 前端入口：`src/features/ai/AiFloatingBubble.tsx`、`src/features/shop/ShopShell.tsx`、`src/components/layout/DashboardShell.tsx`、`src/features/dealer/DealerShell.tsx`。
- 服务端入口：`src/app/api/ai/assistant/route.ts`、`src/app/api/ai/assistant/confirm/route.ts`。
- 工具执行层：`src/features/ai/assistant-service.ts`、`src/features/ai/tools/context.ts`、`executor.ts`、`planner.ts`、`registry.ts`、`audit.ts`、`provider.ts`。
- 上线检查：`src/features/system/launch-readiness.ts`、`scripts/launch-readiness.ts`。

复核结论：

| 范围 | 结论 | 测试影响 |
|------|------|----------|
| 三端 AI 气泡入口 | 商城登录用户、后台用户、经销商端均接入 `AiFloatingBubble`；未登录商城不显示气泡 | 正式验收需分别使用消费者、经销商、管理员、销售、仓管、财务账号打开气泡 |
| Assistant API | `/api/ai/assistant` 使用 SSE 返回 `delta/card/done/error`，空消息返回 400，未登录由上下文抛出 401 并以 SSE error 呈现 | 需补测未登录、空消息、普通查询、写操作确认卡和服务端异常提示 |
| Confirm API | `/api/ai/assistant/confirm` 统一执行待确认 tool，校验 toolName、args、确认 token 和高风险确认文字 | 需补测缺 token、过期/篡改 token、参数变化、确认文字错误和重复确认 |
| Tool 权限 | `getAiToolContext()` 绑定当前登录用户，`canUseAiTool()` 按角色和 permission 过滤，动态权限由 `resolvePermission` 补充 | 需补测角色越权：客户查后台、经销商查他人门店、销售查非名下客户、财务执行履约 |
| 写操作风险分级 | READ/DRAFT 直接返回，WRITE/HIGH_RISK 必须先生成确认卡；HIGH_RISK 默认要求输入“确认执行” | 需补测调价、入库、收款、发券、重置密码、禁用账号、系统配置等确认链路 |
| 审计与脱敏 | tool 调用、待确认、确认执行、失败均写操作日志，并对 password、token、secret 等字段脱敏 | 需在正式测试库核对 `AuditLog` 中有记录且无明文密码/API key/token |
| AI provider | 未配置 provider 时使用内置规则规划；配置 `AI_BASE_URL`/`AI_API_KEY` 后可调用 Anthropic-compatible `/messages` | 当前本地未配置真实 provider，复杂口语规划需正式测试密钥联调 |
| 上线 readiness | `check:launch` 可检查数据库、认证、AI、地图、微信、支付、税控、酒类资质，并支持 `production/trial/demo` 口径 | 当前 production 报告为 `BLOCKER`，微信小程序、微信公众号、微信支付、税控电子发票和酒类经营资质未完整配置；封闭试运营可显式使用 `LAUNCH_READINESS_MODE=trial` |
| 系统本体完整度 | `check:system` 按后台、商城、经销商端、AI、权限、订单、库存、财务、营销、微信生态、日志、配置中心、报表、地图配送 14 个模块，逐项检查真实入口、真实操作、权限、审计、异常处理和验证证据 | 自动报告只作为门禁；最终需按 `docs/15-系统完整度验收表.md` 留存真实角色、真实操作和日志证据，外部硬配置和人工运营签收不计入程序完整度 |
| 运营验收 | `check:operational` 可检查业务签收、价格复核、库存盘点、账号权限复核、真实设备、真实支付、小程序体验版、税控联调和备份恢复演练 | 当前报告为 `WARNING`，需按正式上线验收记录模板人工留痕 |

本轮补跑命令：

| 命令 | 结果 |
|------|------|
| `npx.cmd tsc --noEmit` | 通过 |
| `npm.cmd run test:ai-tools` | 通过，输出 `AI tools smoke passed: 47 tools` |
| `npm.cmd run check:launch` | 可运行，当前输出 `Launch readiness: BLOCKER` |
| `npm.cmd run lint` | 通过 |
| `npm.cmd run build` | 通过 |

新增正式测试要求：

- AI 代办助手不得作为绕过菜单权限的入口；所有 AI tool 的可见性和执行结果必须与角色权限一致。
- 所有 WRITE/HIGH_RISK tool 必须先展示确认卡，不得直接写库。
- 高风险操作必须验证确认文字和签名 token；token 过期、篡改、跨用户、跨角色、参数变化均应拒绝。
- 对 AI 模型返回的 tool plan 必须只执行已注册 tool，未知 tool 或参数不完整应失败并记录审计。
- 正式运营前必须配置酒类经营资质；未配置时 `check:launch` 的 BLOCKER 不可忽略。
- 真实 AI provider 联调时需限制测试密钥额度，并验证超时、非 JSON、空响应、401/429/5xx 等异常提示不会阻塞主业务页面。

### 23.17 AI 代办助手全流程测试记录

执行环境：

- 测试方式：本地命令门禁 + API/SSE smoke + Codex in-app browser 三端模拟验收。
- 测试库：临时将 `.env.local` 指向 `goods_sell_test` schema。
- 测试地址：`http://localhost:3300`。
- 测试模型：未接入真实 AI provider，使用内置规则 planner 和已注册 AI tools。
- 数据原则：仅使用虚拟客户、虚拟订单、虚拟商品、虚拟经销商和虚拟员工；不连接生产业务库。

本轮完成进度：

| 批次 | 状态 | 说明 |
|------|------|------|
| 自动化门禁 | 通过 | `npx.cmd tsc --noEmit`、`npm.cmd run lint`、`npm.cmd run test:ai-tools`、`npm.cmd run test:rbac`、`npm.cmd run build` 均通过 |
| 上线检查 | 阻塞项已确认 | `npm.cmd run check:launch` 可运行，当前输出 `Launch readiness: BLOCKER`；阻塞项为酒类经营资质未配置，另有 AI provider、微信、小程序、支付、税控等 WARNING |
| API 基础行为 | 通过 | 空消息返回 400；未登录 SSE 返回 401 error；未登录 confirm 被拒绝；普通 READ 返回 `delta/card/done` |
| 多角色登录 | 通过 | 管理员、销售员、仓管、财务、消费者、经销商账号均可通过本地认证登录 |
| 三端 AI 入口 | 通过 | 后台 `/dashboard`、商城 `/shop`、经销商端 `/dealer/incoming` 均显示“打开 AI 助手”气泡 |
| 浏览器交互 | 通过 | 后台 AI 对话框可展开、收起、输入问题并返回 readiness 结果卡；本轮未发现新增 console error |
| 审计与脱敏 | 通过 | AI tool 成功、失败、越权、确认执行均写入 `AuditLog`；近 30 条 AI 日志未发现 `password`、`confirmationToken`、`secret`、`apiKey` 明文字段 |

用例记录：

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| AI-E2E-001 | 未登录调用 `/api/ai/assistant` | 通过 | SSE 返回 401 error，不泄露业务数据 |
| AI-E2E-002 | 空消息调用 `/api/ai/assistant` | 通过 | 返回 400 |
| AI-E2E-003 | confirm 缺少登录态/参数 | 通过 | 被 401/400 拦截 |
| AI-E2E-004 | 消费者查商品 | 通过 | `查一下青岛经典啤酒` 返回商品结果卡 |
| AI-E2E-005 | 消费者自然语言下单 | 通过 | `我要下单 1 箱青岛经典啤酒` 先生成确认卡，确认后创建测试订单，最近订单包含 `HQ20260430000008` |
| AI-E2E-006 | 管理员查销售员业绩 | 通过 | `这个月李明业绩怎么样` 返回经营摘要结果卡 |
| AI-E2E-007 | 管理员上线 readiness | 通过 | `现在上线还差什么配置` 返回 `BLOCKER` 结果卡，列出酒类经营资质阻塞和 5 个提醒项 |
| AI-E2E-008 | 管理员禁用虚拟员工高风险操作 | 通过 | 直接 tool 生成确认卡；错误确认文字被拒绝；输入确认文字后成功执行并写审计 |
| AI-E2E-009 | 管理员自然语言调价 | 失败 S2 | `把香脆薯片组合装涨价 1 块` 未生成确认卡，planner 只提取到 `productQuery`，提示缺少新价格或调整金额 |
| AI-E2E-010 | 管理员调价确认 token | 失败 S2 | `把香脆薯片组合装价格改成 16` 可生成确认卡，但确认时报“确认凭证无效”；原因是 token payload 中可出现 `undefined`，导致稳定序列化结果不是合法 JSON |
| AI-E2E-011 | 仓管自然语言入库 | 失败 S2 | `给香脆薯片组合装入库 2 件` 被 planner 解析为 `productQuery=给香脆薯片组合装入库`，商品匹配失败，未生成确认卡 |
| AI-E2E-012 | 仓管入库 executor/confirm | 通过 | 使用明确 SKU/商品参数直接生成确认卡，确认后库存与库存流水变化 |
| AI-E2E-013 | 财务查回款摘要 | 通过 | 财务账号 `查一下财务回款` 返回结果卡 |
| AI-E2E-014 | 财务登记收款高风险确认 | 通过 | 创建虚拟赊销订单 `HQAI-FIN-15348961`，错误确认文字返回 400；正确确认后登记收款 ¥1.00，订单已收金额变为 1 |
| AI-E2E-015 | 经销商查待接单 | 通过 | 经销商账号 `查一下待接订单` 返回自己的待接单结果卡 |
| AI-E2E-016 | 经销商上报库存 | 通过 | 经销商确认后将 `HQ-BEER-001` 门店库存上报为 8，可在 `DealerStock` 追溯 |
| AI-E2E-017 | 经销商接单确认 | 通过 | 经销商确认后接单 `HQ20260430000008`，路由状态变为 `ACCEPTED` |
| AI-E2E-018 | 篡改确认 args | 通过 | 修改 `routingId` 后确认返回 400：确认内容已变化 |
| AI-E2E-019 | 跨用户使用确认 token | 通过 | 管理员使用经销商确认卡执行接单返回 403 |
| AI-E2E-020 | 经销商越权查后台经营总览 | 通过 | 经销商直接调用 `business_overview` 被 403 拦截并写入越权审计 |
| AI-E2E-021 | 浏览器后台 AI UI | 通过 | `/dashboard` 气泡可见，对话框可打开、放大、收起，readiness SSE 结果卡正常展示 |
| AI-E2E-022 | 浏览器商城 AI UI | 通过 | 消费者登录后 `/shop` 气泡可见，不遮挡底部导航核心入口 |
| AI-E2E-023 | 浏览器经销商 AI UI | 通过 | 经销商登录后 `/dealer/incoming` 气泡可见，不影响接单/拒单按钮展示 |

本轮发现的问题：

- S2：规则 planner 对“涨价 1 块”等相对调价表达解析不完整，应补充 `adjustRetailPrice`/`adjustWholesalePrice` 提取规则，并保持与绝对改价表达一致的确认卡流程。
- S2：确认 token 的 `stableStringify()` 对对象属性值为 `undefined` 时会生成非法 JSON，导致部分 WRITE tool 确认卡无法执行。建议在签名前过滤 `undefined` 字段，或将其规范化为 `null`。
- S2：规则 planner 对“给某商品入库 2 件”未清理前缀“给”和后缀“入库”，导致商品匹配失败。建议扩展 `cleanProductQuery()` 的动词/介词清理规则，并补充仓管自然语言回归用例。
- S3：财务自然语言“给某客户某订单登记收款 1 元”当前被规则 planner 命中为经营总览，未进入 `finance_register_payment`；executor 和 confirm 链路可用，但自然语言入口需要补规则。
- 正式环境待补测：真实 AI provider JSON plan、超时、401/429/5xx、小程序体验版、真实微信支付/回调、公众号真实菜单发布、多浏览器和真实移动设备兼容。

当前结论：

- AI 代办助手的三端入口、SSE 回复、确认卡安全链路、权限拦截、审计日志和多角色 READ 场景已完成本地全流程验收。
- 本轮发现 3 个影响自然语言写操作可用性的 S2 问题和 1 个 S3 规则覆盖问题。建议修复 S2 后再把 AI 写操作开放给运营人员试用；在修复前，可仅开放 READ 类助手能力和已验证的显式参数/菜单式确认流程。
- 上线 readiness 在 production 口径下仍为 `BLOCKER`，微信小程序、微信公众号、微信支付、税控电子发票和酒类经营资质未完整配置前不建议面向真实客户全面运营。

### 23.18 AI 代办助手缺陷修复回归记录

修复范围：

- `src/features/ai/tools/planner.ts`：补充相对调价、绝对改价、仓管入库商品名清理、财务登记收款自然语言规划。
- `src/features/ai/tools/executor.ts`：确认 token 签名前过滤对象中的 `undefined` 字段，避免生成非法 JSON。
- `scripts/ai-tools-smoke.ts`：新增 AI planner 回归断言，覆盖调价、入库和财务收款自然语言。

回归结果：

| 用例编号 | 原问题 | 回归状态 | 实际结果 |
|----------|--------|----------|----------|
| AI-FIX-001 | `把剑兰春涨价 5 块` 只命中调价工具但未提取调整金额 | 已修复 | `test:ai-tools` 断言 `adjustRetailPrice === 5` 通过 |
| AI-FIX-002 | `把剑兰春价格改成 16` 生成确认卡后 token payload 可能含 `undefined` | 已修复 | token payload 可被 JSON 解析，`undefined` 字段已被过滤 |
| AI-FIX-003 | `给香脆薯片组合装入库 2 件` 商品名被解析为 `给香脆薯片组合装入库` | 已修复 | `test:ai-tools` 断言 tool 为 `inventory_stock_in`，商品名为 `香脆薯片组合装`，数量为 `2` |
| AI-FIX-004 | 财务自然语言登记收款被命中为经营总览 | 已修复 | `给13900139001的订单HQAI-FIN-15348961登记收款1元` 命中 `finance_register_payment`，并提取手机号、订单号和金额 |

本轮回归命令：

| 命令 | 结果 |
|------|------|
| `npm.cmd run test:ai-tools` | 通过，输出 `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 |
| `npm.cmd run lint` | 通过 |
| `npm.cmd run build` | 通过 |
| token payload 解析脚本 | 通过，`tokenJsonValid: true`，且 `adjustRetailPrice` 未出现在签名 payload 中 |

剩余说明：

- 直接在独立脚本中执行调价 handler 会触发 Next `revalidateTag` 的请求上下文限制；本轮以 planner smoke 和确认 token payload 解析验证修复点。完整“确认后调价页面可见”仍建议通过浏览器/API request 作用域在测试服务中复测。
- 本轮未改变 `check:launch` 结论；酒类经营资质仍是正式上线阻塞项。

### 23.19 AI 代办助手运行时修复回归记录

执行环境：

- 测试方式：本地 3300 测试服务 + HTTP 登录态 + `/api/ai/assistant` SSE + `/api/ai/assistant/confirm`。
- 数据隔离：服务进程使用 `goods_sell_test` schema；执行前通过 `/dashboard/orders` 页面确认可见测试订单 `HQAI-FIN-15348961`。
- 测试账号：管理员 `admin`、仓管 `13800138002`、财务 `13800138003`。

运行时回归结果：

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| AI-RUNTIME-001 | 管理员自然语言绝对调价 | 通过 | `把青岛经典啤酒价格改成72` 命中 `admin_update_product_price`，生成确认卡，确认后返回“商品价格已更新”结果卡 |
| AI-RUNTIME-002 | 仓管自然语言入库 | 通过 | `给青岛经典啤酒入库 1 件` 命中 `inventory_stock_in`，生成确认卡，确认后返回“入库成功”结果卡 |
| AI-RUNTIME-003 | 财务自然语言登记收款 | 通过 | `给13900139001的订单HQAI-FIN-15348961登记收款1元` 命中 `finance_register_payment`，生成 HIGH_RISK 确认卡 |
| AI-RUNTIME-004 | 财务高风险错误确认文字 | 通过 | 输入错误确认文字返回 400：高风险操作需要输入“确认执行”后才能继续 |
| AI-RUNTIME-005 | 财务高风险正确确认文字 | 通过 | 输入正确确认文字后登记收款 ¥1.00，返回“收款已登记”结果卡 |
| AI-RUNTIME-006 | 确认 token payload | 通过 | 生成确认卡的签名 payload 可被 JSON 解析，`undefined` 字段不会写入 payload |
| AI-RUNTIME-007 | AI 审计敏感字段 | 通过 | 新生成的确认卡审计日志中不再出现 `password`、`confirmationToken`、`secret`、`apiKey` 等敏感字段名；敏感字段已从 AI 审计对象中移除 |

补充修复：

- `src/features/ai/tools/audit.ts`：AI 审计脱敏策略由“保留敏感键并写 `[REDACTED]`”调整为“直接移除敏感键”，避免日志字段名暴露确认凭证或密钥语义。
- `scripts/ai-tools-smoke.ts`：同步更新脱敏断言，确认 `password` 与 `confirmationToken` 字段被移除，普通字段保留。

本轮回归命令：

| 命令 | 结果 |
|------|------|
| `npm.cmd run test:ai-tools` | 通过 |
| `npm.cmd run test:rbac` | 通过，输出 `RBAC smoke passed` |
| `npx.cmd tsc --noEmit` | 通过 |
| `npm.cmd run lint` | 通过 |
| `npm.cmd run build` | 通过 |
| `npm.cmd run check:launch` | 可运行，当前仍输出 `Launch readiness: BLOCKER`；阻塞项为酒类经营资质未配置 |

当前结论：

- 23.17 中记录的 AI 写操作自然语言解析问题、确认 token `undefined` 问题、AI 审计敏感字段问题均已完成代码修复和运行时回归。
- 剩余正式环境补测仍包括真实 AI provider、微信支付/回调、公众号真实菜单、小程序体验版、多浏览器与真实移动设备兼容。

### 23.20 真实 AI Provider 同步与 think 模式准备记录

执行目标：

- 将远端服务器已配置的 AI provider 信息同步到本地 `.env.local`，用于后续真实 AI 全量测试。
- 开启 AI provider smoke 命令，验证真实模型能按系统要求返回工具规划 JSON。
- 启用 think 模式请求参数，但不展示模型内部思考过程，只验收最终响应与业务行为。

同步结果：

| 项目 | 状态 | 说明 |
|------|------|------|
| 远端配置读取 | 已完成 | 从部署机 `/data/goods_sell` 读取 AI 相关环境变量 |
| 本地 `.env.local` 更新 | 已完成 | 已同步 `AI_BASE_URL`、`AI_API_KEY`、`AI_MODEL` |
| think 模式开关 | 已完成 | 已配置 `AI_THINKING_ENABLED=true` 和 `AI_THINKING_BUDGET_TOKENS=1024` |
| 密钥保护 | 已完成 | 终端和文档仅记录变量名、配置状态和值长度，不记录明文密钥 |
| 临时文件清理 | 已完成 | 同步过程使用的临时文件已删除 |

新增验证命令：

| 命令 | 目的 | 状态 |
|------|------|------|
| `npm.cmd run test:ai-provider` | 调用真实 AI provider，验证最小工具规划请求能命中 `system_launch_readiness` | 通过，输出 `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |

正式全量测试补充要求：

- 后续浏览器全量测试必须在真实 provider 开启时重复 23.17 和 23.19 的核心场景。
- 重点观察复杂中文口语是否优于内置规则 planner，并记录错误 tool、缺参数、非 JSON、超时和权限误判。
- 若 provider 返回非 JSON、未知 tool 或越权 tool，系统必须友好失败，不得执行写操作。
- 所有真实 AI 写操作仍必须经过确认卡、签名 token、角色权限和高风险确认文字校验。
- 本项目不记录或展示模型内部推理链路；think 模式仅作为 provider 能力开关和质量验证条件。

### 23.21 真实 AI Provider 浏览器全流程验收记录

执行环境：

- 测试方式：Codex in-app browser 模拟三端操作 + 本地 3300 测试服务 + `goods_sell_test` schema。
- AI provider：远端真实 provider，同步到本地 `.env.local`；`AI_THINKING_ENABLED=true`，`AI_THINKING_BUDGET_TOKENS=1024`。
- 测试地址：`http://localhost:3300`。
- 测试账号：管理员 `admin`、销售员 `13800138001`、仓管 `13800138002`、财务 `13800138003`、消费者 `13900139001`、经销商 `13900139101`。
- 数据原则：仅写入测试 schema；未使用生产库；未记录 AI 密钥明文。

本轮自动化与服务准备：

| 项目 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-provider` | 通过 | 真实 provider 命中 `system_launch_readiness`，think 模式为 enabled |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| 3300 测试服务 | 通过 | Next dev 服务使用 `.env.local` 和 `goods_sell_test` 启动 |

浏览器用例记录：

| 用例编号 | 角色/端 | 验收项 | 状态 | 实际结果 |
|----------|---------|--------|------|----------|
| AI-REAL-001 | 管理员/后台 | `现在上线还差什么配置` | 通过 | 返回上线就绪检查卡片，状态 `BLOCKER`，酒类经营资质为阻塞项 |
| AI-REAL-002 | 管理员/后台 | `把青岛经典啤酒价格改成73` | 通过 | 生成“确认商品调价”卡片，点击“确认调价”后返回“商品价格已更新” |
| AI-REAL-003 | 仓管/后台 | `给青岛经典啤酒入库 1 件` | 通过 | 生成“确认入库”卡片，点击“确认入库”后返回“入库成功” |
| AI-REAL-004 | 财务/后台 | `给13900139001的订单HQAI-FIN-15348961登记收款1元` | 通过 | 生成二次确认卡；错误确认文字按钮保持禁用，输入“确认执行”后登记成功 |
| AI-REAL-005 | 经销商端 | `查一下待接订单` | 通过 | 返回 2 个待接订单，仅展示当前经销商范围内订单 |
| AI-REAL-006 | 经销商端 | `把青岛经典啤酒门店库存上报为9` | 失败 S2 | 真实 AI 误规划为 `search_products`，返回“没有找到相关商品”，未生成库存上报确认卡 |
| AI-REAL-007 | 经销商端 | `上报 HQ-BEER-001 门店库存 9 件` | 失败 S2 | 即使给出 SKU，真实 AI 仍误规划为 `search_products`，未生成确认卡 |
| AI-REAL-008 | 经销商端 | `接单 HQ20260430000007` | 失败 S2 | 真实 AI 误规划为 `dealer_incoming_orders`，只返回待接订单列表，未生成接单确认卡 |
| AI-REAL-009 | 消费者/商城 | `查一下青岛经典啤酒` | 失败 S3 | 真实 AI 误规划/参数不完整，返回“没有找到相关商品”；商城页面本身可见该商品 |
| AI-REAL-010 | 消费者/商城 | `我要下单 1 箱青岛经典啤酒` | 通过 | 生成“确认提交订单”卡片，确认后生成订单 `HQ20260430000013` |
| AI-REAL-011 | 销售员/后台 | `这个月我的业绩怎么样` | 通过 | 返回销售一部-李明业绩卡片：销售额、订单数、名下客户、回款、排名可见 |
| AI-REAL-012 | 销售员/后台 | `现在上线还差什么配置` | 失败 S2 | 后端请求返回 200，但浏览器助手停留在“正在处理...”；审计显示真实 AI 实际执行了 `business_overview`，未按意图命中 readiness/权限拒绝 |

数据库与审计核对：

| 核对项 | 状态 | 结果 |
|--------|------|------|
| 商品调价 | 通过 | `HQ-BEER-001` 零售价为 `73` |
| 消费者下单 | 通过 | 订单 `HQ20260430000013` 已生成，客户为张阿姨，金额 `73`，订单状态 `PAID` |
| 库存写操作 | 通过 | 入库成功卡片返回，业务审计存在“库存/入库”记录 |
| AI 写操作审计 | 通过 | 调价、入库、收款、下单均存在“AI助手”确认/完成记录 |
| 审计敏感字段 | 通过 | 近 30 条审计日志未发现 `password`、`confirmationToken`、`secret`、`apiKey`、`authorization` 明文字段 |
| 浏览器控制台 | 通过 | 本轮关键页面未发现新增 console error |

本轮新增问题：

- S2：真实 AI provider 对经销商库存上报意图识别不稳定，`把青岛经典啤酒门店库存上报为9` 和 `上报 HQ-BEER-001 门店库存 9 件` 均误规划为 `search_products`。建议在 provider system prompt 中提高 `dealer_report_stock` 的示例优先级，并在 planner fallback 中对“上报/门店库存/SKU”做硬规则兜底。
- S2：真实 AI provider 对经销商接单意图识别不稳定，`接单 HQ20260430000007` 误规划为 `dealer_incoming_orders`。建议增加 `dealer_accept_routing`/`dealer_reject_routing` 的订单号示例和后端 plan 校正规则。
- S2：销售员询问 readiness 时，真实 AI 执行了 `business_overview`，浏览器 UI 停留在“正在处理...”。建议排查 SSE `done/error` 事件处理，并在 provider plan 后增加“意图-工具一致性”校验；销售员不应通过 AI 获得管理员上线 readiness。
- S3：消费者商品查询 `查一下青岛经典啤酒` 未命中商品，但下单语句能命中。建议补充商品查询参数清洗或真实 provider 示例。

当前结论：

- 真实 AI provider 已可用于后台/商城/经销商三端 READ 与部分 WRITE/HIGH_RISK 流程。
- 管理员调价、仓管入库、财务高风险收款、消费者下单已完成浏览器端真实 AI 闭环验收。
- 经销商写操作和部分短句查询仍存在真实 provider 规划偏差，建议修复 S2 后再开放经销商 AI 写操作给试运营用户。
- `check:launch` 结论未改变：酒类经营资质仍是正式上线阻塞项。

### 23.22 真实 AI Provider 规划偏差修复记录

修复目标：

- 修复 23.21 暴露的经销商库存上报、经销商接单、销售员 readiness 误规划，以及消费者短句商品查询不命中的问题。
- 将真实 provider 容易误判的关键业务意图提升为本地 planner 硬规则兜底，避免写操作被误规划为只读查询。

代码修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增订单号、SKU、库存数解析；经销商拒单/接单优先于待接单查询；支持“上报 ... 库存”“门店库存”“SKU 库存”表达；消费者商品查询清理“查一下/查询/看看”等前缀；销售员询问上线配置时规划到 `system_launch_readiness`，由 executor 做权限拦截 |
| `src/features/ai/tools/registry.ts` | `dealer_accept_routing` 和 `dealer_reject_routing` 支持用订单号查找当前经销商自己的待接 routing，不再要求 AI 知道内部 `routingId` |
| `scripts/ai-tools-smoke.ts` | 新增回归断言：经销商库存上报、SKU 库存上报、订单号接单、消费者短句商品查询、销售员 readiness 权限拦截入口 |
| `scripts/ai-runtime-regression.ts` | 新增运行时回归：在测试 schema 中验证经销商库存上报确认卡、订单号接单确认卡、销售员 readiness 403 权限拦截 |

回归用例：

| 用例编号 | 原问题 | 回归状态 | 实际结果 |
|----------|--------|----------|----------|
| AI-REAL-FIX-001 | `把青岛经典啤酒门店库存上报为9` 误规划为 `search_products` | 已修复 | planner 命中 `dealer_report_stock`，`productQuery=青岛经典啤酒`，`stock=9` |
| AI-REAL-FIX-002 | `上报 HQ-BEER-001 门店库存 9 件` 误规划为 `search_products` | 已修复 | planner 命中 `dealer_report_stock`，`productQuery=HQ-BEER-001`，`stock=9` |
| AI-REAL-FIX-003 | `接单 HQ20260430000007` 误规划为 `dealer_incoming_orders` | 已修复 | planner 命中 `dealer_accept_routing`，并支持订单号作为确认入口 |
| AI-REAL-FIX-004 | 消费者 `查一下青岛经典啤酒` 查询不到商品 | 已修复 | planner 命中 `search_products`，查询词清理为 `青岛经典啤酒` |
| AI-REAL-FIX-005 | 销售员询问上线 readiness 被误规划为经营总览 | 已修复 | planner 命中 `system_launch_readiness`，运行时应由 executor 返回 403 权限拦截 |

回归命令：

| 命令 | 结果 |
|------|------|
| `npm.cmd run test:ai-tools` | 通过，输出 `AI tools smoke passed: 47 tools` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过，输出 `AI runtime regression passed` |
| `npm.cmd run test:ai-provider` | 通过，输出 `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| `npx.cmd tsc --noEmit` | 通过 |
| `npm.cmd run lint` | 通过 |
| `npm.cmd run build` | 通过 |

剩余验证：

- 建议下一轮再用 3300 测试服务和真实 AI provider 做浏览器回归，重点复测经销商“确认上报库存”“确认接单/拒单”和销售员 readiness 权限拦截卡片。
- 本轮未改变 `check:launch` 结论；酒类经营资质仍是正式上线阻塞项。

### 23.23 真实 AI 修复后浏览器回归记录

执行环境：

- 测试方式：Codex in-app browser + 本地 3300 测试服务。
- 测试库：`goods_sell_test` schema。
- AI provider：真实 provider，think 模式开启。
- 测试账号：经销商 `13900139101`、销售员 `13800138001`、消费者 `13900139001`。

补充前端修复：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/AiFloatingBubble.tsx` | SSE 返回 `error` 时将错误写入当前助手消息；流结束但无正文时落入完成态，避免一直显示“正在处理...” |
| `src/features/ai/AiChatClient.tsx` | 同步修复商城 AI 聊天页 SSE 空正文/错误消息展示 |

浏览器回归结果：

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| AI-BROWSER-FIX-001 | 经销商 `把青岛经典啤酒门店库存上报为9` | 通过 | 返回“确认上报库存”卡片，商品为青岛经典啤酒，库存为 9 |
| AI-BROWSER-FIX-002 | 经销商 `接单 HQ20260430000007` | 通过 | 返回“确认接单”卡片，订单号为 `HQ20260430000007`，金额 `¥72.00` |
| AI-BROWSER-FIX-003 | 销售员 `现在上线还差什么配置` | 通过 | 返回“无法执行/无权限查看或操作该事项”，原因显示权限不足；页面不再残留“正在处理...” |
| AI-BROWSER-FIX-004 | 消费者 `查一下青岛经典啤酒` | 通过 | 返回“找到 1 个相关商品”，结果包含青岛经典啤酒 500ml*12，价格 `¥73.00` |
| AI-BROWSER-FIX-005 | 控制台错误 | 通过 | 上述关键页面未发现新增 console error |

本轮结论：

- 23.21 中记录的经销商库存上报、经销商接单、销售员 readiness UI 卡住、消费者短句查询问题已完成浏览器回归。
- 经销商拒单仍建议在下一轮使用独立待接订单做确认卡和重匹配实测，避免消费当前已用于接单回归的订单。
- `check:launch` 结论未改变；酒类经营资质仍是正式上线阻塞项。

### 23.24 真实 AI 经销商拒单补充回归记录

执行环境：
- 测试方式：Codex in-app browser + 本地 3300 测试服务。
- 测试库：`goods_sell_test` schema。
- AI provider：真实 provider，think 模式开启。
- 测试账号：经销商 `13900139101`。
- 数据原则：仅使用测试 schema 中的虚拟待接订单；未使用生产库，未记录 AI 密钥明文。

浏览器回归结果：

| 用例编号 | 验收项 | 状态 | 实际结果 |
|----------|--------|------|----------|
| AI-BROWSER-FIX-006 | 经销商 `拒单 HQ20260430000013 原因 太远` | 通过 | 真实 AI 返回“确认拒单”卡片，展示订单号 `HQ20260430000013` 和原因“原因 太远”；点击“确认拒单”后返回“订单已拒绝并自动重匹配”和“已拒单”结果卡 |
| AI-BROWSER-FIX-007 | 经销商拒单确认链路 | 通过 | 拒单写操作必须先生成确认卡，未直接写库；确认后才执行拒单 handler |
| AI-BROWSER-FIX-008 | 经销商拒单订单号解析 | 通过 | `dealer_reject_routing` 可通过自然语言中的业务订单号解析当前经销商自己的待接 routing，不要求 AI 知道内部 routingId |
| AI-BROWSER-FIX-009 | 浏览器控制台 | 观察项 | 拒单执行链路未出现业务异常；控制台保留 1 条 Next dev HMR/RSC payload fetch 回退错误，来源为早前 `/shop` 热刷新，不影响本次拒单结果 |

补充自动化回归：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | 覆盖经销商库存上报、订单号接单、订单号拒单、销售员 readiness 403 等运行时链路 |
| `npm.cmd run test:ai-provider` | 通过 | 真实 provider 命中 `system_launch_readiness`，think 模式为 `enabled` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |
| `npm.cmd run check:launch` | 阻塞项符合预期 | 返回 `Launch readiness: BLOCKER`；阻塞项为 `ALCOHOL_BUSINESS_LICENSE_NO` / `ALCOHOL_LICENSE_NO` 未配置，微信小程序、公众号、微信支付、税控仍为 WARN |

本轮结论：
- 23.23 中保留的“经销商拒单确认卡和重匹配实测”已完成浏览器补充回归。
- 真实 AI + 本地硬规则兜底后，经销商库存上报、接单、拒单三条写流程均能进入确认卡，不再被误规划成只读查询。
- 经销商 AI 确认成功后页面自动刷新体验已在 23.35 修复并复测；当前以页面列表、AI 结果卡、数据库状态和审计日志共同作为验收依据。
- `check:launch` 结论未改变；酒类经营资质仍是正式上线阻塞项。

### 23.25 AI 浮窗移动端横向溢出补充回归记录

问题来源：
- 浏览器全流程测试重点放在 AI 是否能返回结果卡、确认卡、权限拦截和写操作闭环，未把移动窄屏下的横向溢出作为硬性验收项。
- AI 浮窗底部快捷提示使用横向滚动，消息气泡和嵌套卡片缺少 `min-width: 0`、`overflow-x-hidden` 和长文本换行约束，导致 390px 左右窄屏中出现横向滚动条。

修复范围：
| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/AiFloatingBubble.tsx` | 浮窗容器、消息区、消息气泡、结果卡、确认卡增加防横向溢出约束；快捷提示由横向滚动改为自动换行 |
| `src/features/ai/AiChatClient.tsx` | 商城 AI 聊天页同步增加防横向溢出约束；快捷问题由横向滚动改为自动换行 |

回归命令：
| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |

后续测试要求：
- AI 浮窗和 AI 聊天页在移动视口中必须增加截图或视觉检查，不只记录业务返回文本。
- 对所有 AI result/confirmation/suggestion 卡片增加“无横向滚动、长文本可换行、快捷提示不遮挡输入框”的 UI 验收项。
- 运营前每轮浏览器验收应至少覆盖：商城移动端、经销商移动端、后台桌面端三类 AI 入口。

### 23.26 AI 下单意图误命中经营总览补充回归记录

问题来源：
- 用户输入 `我要下单 1 箱剑兰春` 时，AI 返回了经营总览文案“本期销售、回款、毛利”，与下单意图完全不匹配。
- 原因是此前 smoke 只断言了消费者角色下的下单意图；员工/经销商上下文中，同一句快捷提示没有强断言。
- 员工侧 planner 缺少“购买/下单”高优先级分支，兜底逻辑可能落到 `business_overview`；真实 AI 规划偏差时也会被该兜底放大。

修复范围：
| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增跨角色 `isPurchaseIntent()`；消费者仍走 `customer_submit_order`；员工侧下单意图走 `orders_manual_order_draft`；经销商侧下单意图先走 `search_products`，不再落到 `business_overview` |
| `scripts/ai-tools-smoke.ts` | 新增同一句 `我要下单 1 箱剑兰春` 在消费者、管理员、经销商三个角色下的规划断言，明确禁止误命中经营总览 |

回归命令：
| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |

后续测试要求：
- 所有快捷提示必须按角色矩阵验收，不能只测某一个默认角色。
- AI 测试记录中必须增加“意图-工具一致性”字段：用户话术、当前角色、命中的 tool、是否允许、是否符合预期。
- 对下单、收款、调价、库存、接单/拒单这类核心意图，若命中无关 READ 工具，应按 S1/S2 缺陷记录，而不是视为普通回复偏差。

### 23.27 AI tool plan 意图一致性校验补充回归记录

问题来源：
- 23.26 的修复不能只针对 `我要下单 1 箱剑兰春` 单句做特判；真实 AI provider 仍可能在其他短句中把写操作误规划成经营总览、结算查询或商品查询。
- AI 代办助手需要在模型 plan 之后再做一次确定性校验，确保“用户原话核心意图”和最终执行 tool 类型一致。

修复范围：
| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增 `detectCoreIntent()` 和 `validateAiToolPlan()`；对下单、上线检查、收款登记、调价、经销商接单/拒单、门店库存上报、入库/出库、安全库存、订单状态操作等核心意图建立允许 tool 白名单 |
| `src/features/ai/assistant-service.ts` | 在规则 planner / 真实 AI model plan 之后统一调用 `validateAiToolPlan()`；若命中 tool 与核心意图不一致，先用本地确定性 planner 纠偏，仍不匹配则拒绝执行，避免错误 READ 结果冒充业务答案 |
| `scripts/ai-tools-smoke.ts` | 新增“模拟错误模型规划”断言：员工下单被误规划为 `business_overview` 时必须纠偏为 `orders_manual_order_draft`；经销商下单被误规划为 `dealer_settlement_summary` 时必须纠偏为 `search_products` |

覆盖意图矩阵：
| 核心意图 | 允许 tool |
|----------|-----------|
| 下单/购买 | `customer_submit_order`、`orders_manual_order_draft`、`search_products` |
| 上线/配置就绪检查 | `system_launch_readiness` |
| 收款/回款登记 | `finance_register_payment` |
| 商品调价 | `admin_update_product_price` |
| 经销商接单 | `dealer_accept_routing` |
| 经销商拒单 | `dealer_reject_routing` |
| 经销商门店库存上报 | `dealer_report_stock` |
| 入库/出库 | `inventory_stock_in`、`inventory_stock_out` |
| 安全库存 | `warehouse_update_safe_stock` |
| 订单确认/发货/送达/完成/取消 | `order_status_action` |

回归命令：
| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |

当前结论：
- 本轮不再是单句补丁，而是对高风险、高频 AI 工具调用增加“计划结果闸门”。
- 对未列入核心意图矩阵的开放式问答仍允许 AI 正常规划；后续测试如果再发现新的业务核心意图误命中，应先补入矩阵，再补自动化断言。
- 浏览器复测时，对任一 AI 回复都要记录“用户原话、角色、实际 tool、意图校验结果、业务页面追溯结果”，其中 tool 与意图不一致按 S1/S2 缺陷处理。

### 23.28 真实 AI Provider 全角色全 Tools 覆盖测试记录

执行目标：
- 使用真实 AI provider 作为必测路径，不再用规则 planner 或 direct executor 代表通过。
- `AI_THINKING_ENABLED=true`，provider smoke 必须输出 thinking enabled；不记录或展示模型内部思考过程。
- 使用本地 `goods_sell_test` schema，大量创建虚拟客户、订单、产品、经销商、优惠券、库存、发票和审计日志，不污染业务库。
- 47 个 AI tools 均按“自然语言输入 -> 真实 AI plan -> tool 执行/确认卡 -> 数据库断言 -> 审计断言”执行。

代码与脚本补充：

| 文件 | 变更 |
|------|------|
| `scripts/ai-full-provider-coverage.ts` | 新增真实 AI provider 全量覆盖脚本，强制校验 `goods_sell_test`、真实 provider、think 模式；47 个 tools 每个 1 条自然语言用例；WRITE/HIGH_RISK 验证确认卡、缺 token、篡改 token、跨用户 token、高风险确认文字；执行后校验数据库和 `AI助手` 审计日志 |
| `scripts/sync-ai-provider-env.ts` | 新增远端 AI provider 配置同步脚本，仅同步 `AI_*` / provider 兼容键，备份 `.env.local`，输出全程脱敏 |
| `src/features/auth/test-session.ts`、`src/features/auth/guards.ts`、`src/features/dealer/actions.ts`、`src/features/logs/audit.ts` | 新增仅在 `goods_sell_test` 生效的 AI 全量脚本测试会话，避免脚本环境无法调用浏览器会话；生产库或非测试 schema 不启用 |
| `src/lib/revalidate.ts` 及相关 actions | 在 AI 全量脚本测试会话下兼容 Next.js request scope 外的 `revalidatePath` / `revalidateTag`，避免测试脚本误报静态生成上下文错误 |
| `src/features/ai/tools/planner.ts`、`src/features/ai/tools/registry.ts` | 修复财务只提供订单号登记收款不命中问题；订单号可反查客户，仍保持 HIGH_RISK 二次确认 |
| `package.json` | 新增 `test:ai-full-provider` 与 `sync:ai-provider-env` 命令 |

本轮自动化执行记录：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-full-provider` | 通过 | 最新复跑 runId=`AI-FULL-MOL3YUB7`，`PASS=47 FAIL=0 BLOCKED=0`，RBAC unauthorized checks 通过；历史 runId=`AI-FULL-MOL0GMQI` 也通过 |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |
| `npm.cmd run check:launch` | 阻塞项符合预期 | 默认本地配置返回 `Launch readiness: BLOCKER`，阻塞项为酒类经营资质未配置 |
| 临时配置 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | 返回 `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0`；小程序、公众号、微信支付、税控仍为 WARNING |

真实 provider 47 tools 覆盖矩阵：

| 编号 | 角色 | 预期 tool | 自然语言话术 | 状态/断言 |
|------|------|-----------|--------------|-----------|
| AI-FULL-001 | 消费者 | `search_products` | 查一下测试商品的价格库存 | 通过，真实 provider 命中商品查询，返回 1 个相关商品 |
| AI-FULL-002 | 消费者 | `customer_context` | 我的默认地址和最近订单摘要怎么样 | 通过，仅返回当前客户上下文 |
| AI-FULL-003 | 消费者 | `customer_submit_order` | 我要下单 1 箱测试商品，微信支付 | 通过，先确认后创建订单 `HQ20260430000049`，库存和审计可追溯 |
| AI-FULL-004 | 消费者 | `customer_orders` | 查看我的最近订单和配送状态 | 通过，仅返回当前客户订单 |
| AI-FULL-005 | 消费者 | `customer_receivables` | 我现在有哪些待付款和欠款 | 通过，仅返回当前客户待付款 |
| AI-FULL-006 | 管理员 | `business_overview` | 这个月经营总览怎么样 | 通过，返回销售、回款、毛利、订单等经营摘要 |
| AI-FULL-007 | 管理员 | `salesperson_performance` | 这个月某销售员的业绩怎么样 | 通过，返回销售员业绩与排名 |
| AI-FULL-008 | 管理员 | `search_customers` | 查客户手机号的欠款和最近订单 | 通过，返回目标客户 |
| AI-FULL-009 | 管理员 | `admin_create_customer` | 新增客户、手机号、标签、归属销售员 | 通过，WRITE 确认后创建虚拟客户 |
| AI-FULL-010 | 管理员 | `admin_update_customer_profile` | 把客户信用额度改成 888 | 通过，WRITE 确认后客户资料变更 |
| AI-FULL-011 | 管理员 | `admin_assign_customer_salesperson` | 把客户归属给销售员 B | 通过，WRITE 确认后客户归属变更 |
| AI-FULL-012 | 销售员 | `admin_update_customer_tags` | 给名下客户追加标签 | 通过，销售员只能维护授权客户标签 |
| AI-FULL-013 | 管理员 | `product_operations_summary` | 查测试商品库存销量和毛利 | 通过，返回商品经营指标 |
| AI-FULL-014 | 财务 | `finance_summary` | 这个月财务应收和回款趋势怎么样 | 通过，返回应收和回款摘要 |
| AI-FULL-015 | 仓管 | `delivery_summary` | 查看配送摘要，待发货和配送中有多少 | 通过，返回配送摘要 |
| AI-FULL-016 | 销售员 | `channel_summary` | 查看渠道经营摘要、经销商线索和冲突情况 | 通过，返回渠道摘要 |
| AI-FULL-017 | 管理员 | `admin_update_product_price` | 把 SKU 测试商品零售价改成 81 | 通过，WRITE 确认后目标 SKU 价格变化 |
| AI-FULL-018 | 管理员 | `admin_update_product_status` | 把 SKU 测试商品下架 | 通过，WRITE 确认后目标 SKU 状态为 `INACTIVE` |
| AI-FULL-019 | 仓管 | `warehouse_update_safe_stock` | 把 SKU 的安全库存调整为 17 | 通过，WRITE 确认后安全库存变化 |
| AI-FULL-020 | 仓管 | `order_status_action` | 把订单发货 | 通过，WRITE 确认后订单状态变更 |
| AI-FULL-021 | 仓管 | `inventory_stock_in` | 给 SKU 入库 2 件，备注 AI 全量测试 | 通过，WRITE 确认后库存和库存流水增加 |
| AI-FULL-022 | 仓管 | `inventory_stock_out` | 给 SKU 出库 1 件，备注 AI 全量测试 | 通过，WRITE 确认后库存和库存流水减少 |
| AI-FULL-023 | 仓管 | `warehouse_create_stock_check` | 新建一张全量盘点任务 | 通过，WRITE 确认后生成盘点单 `SC20260430005` |
| AI-FULL-024 | 财务 | `finance_register_payment` | 给客户订单登记收款 5 元，转账 | 通过，HIGH_RISK 二次确认后登记收款 |
| AI-FULL-025 | 财务 | `receipts_issue_invoice` | 给订单开普票，填写购方和税号 | 通过，HIGH_RISK 二次确认后生成发票 `FP20260430000005` |
| AI-FULL-026 | 管理员 | `settings_create_staff_user` | 创建员工账号、手机号、角色和密码 | 通过，HIGH_RISK 二次确认后创建虚拟员工 |
| AI-FULL-027 | 管理员 | `settings_set_staff_status` | 禁用员工 | 通过，HIGH_RISK 二次确认后员工禁用 |
| AI-FULL-028 | 管理员 | `settings_reset_staff_password` | 重置员工密码 | 通过，HIGH_RISK 二次确认后密码重置，审计不含明文密码 |
| AI-FULL-029 | 管理员 | `settings_save_business_config` | 把业务参数 `bulkOrderAmount` 调整为 999 | 通过，HIGH_RISK 二次确认后配置更新 |
| AI-FULL-030 | 管理员 | `system_launch_readiness` | 现在上线还差什么配置 | 通过，返回 1 个 BLOCKER 和 4 个 WARNING |
| AI-FULL-031 | 管理员 | `admin_approve_dealer_application` | 通过经销商申请并设置门店、区域、半径和归属销售员 | 通过，HIGH_RISK 二次确认后开通经销商档案 |
| AI-FULL-032 | 管理员 | `admin_reject_dealer_application` | 驳回经销商申请，原因资料不完整 | 通过，HIGH_RISK 二次确认后申请驳回 |
| AI-FULL-033 | 管理员 | `admin_update_dealer_policy` | 修改经销商最低订单、价格等级、跨区和优先级 | 通过，WRITE 确认后经销商政策更新 |
| AI-FULL-034 | 管理员 | `admin_set_dealer_accepting` | 暂停经销商接单 | 通过，WRITE 确认后经销商暂停接单 |
| AI-FULL-035 | 管理员 | `admin_dealer_conflicts` | 查看经销商渠道冲突 | 通过，返回冲突记录 |
| AI-FULL-036 | 销售员 | `marketing_create_coupon` | 创建满减优惠券，共 20 张 | 通过，WRITE 确认后创建优惠券 |
| AI-FULL-037 | 管理员 | `marketing_issue_coupon` | 给某标签客户发放优惠券 | 通过，HIGH_RISK 二次确认后发放 2 张 |
| AI-FULL-038 | 销售员 | `marketing_create_product_push` | 把新品 SKU 推送给目标人群并填写话术 | 通过，WRITE 确认后生成 2 条推送 |
| AI-FULL-039 | 经销商 | `dealer_incoming_orders` | 查一下我的待接订单 | 通过，仅返回当前经销商待接订单 |
| AI-FULL-040 | 经销商 | `dealer_report_stock` | 把 SKU 门店库存上报为 9 | 通过，WRITE 确认后门店库存变更 |
| AI-FULL-041 | 经销商 | `dealer_settlement_summary` | 本月结算摘要和完成订单金额 | 通过，仅返回当前经销商结算摘要 |
| AI-FULL-042 | 经销商 | `dealer_accept_routing` | 接单某订单号 | 通过，WRITE 确认后 routing 接单 |
| AI-FULL-043 | 经销商 | `dealer_reject_routing` | 拒单某订单号，原因太远 | 通过，WRITE 确认后拒单并自动重匹配 |
| AI-FULL-044 | 管理员 | `admin_create_product_draft` | 新增商品草稿，给出规格和价格 | 通过，DRAFT 仅返回草稿，不写库 |
| AI-FULL-045 | 销售员 | `orders_manual_order_draft` | 帮客户开单 1 箱测试商品 | 通过，DRAFT 仅返回开单草稿，不写库 |
| AI-FULL-046 | 销售员 | `marketing_coupon_draft` | 创建优惠券活动草稿 | 通过，DRAFT 仅返回活动草稿，不写库 |
| AI-FULL-047 | 销售员 | `marketing_product_push_draft` | 生成新品推送草稿发给高价值客户 | 通过，DRAFT 仅返回推送草稿，不写库 |

权限与安全断言：
- `AI-FULL-RBAC unauthorized checks` 通过：消费者不能访问经营总览，销售员不能创建员工，仓管不能登记收款，财务不能发货，经销商不能查看上线 readiness。
- 所有 WRITE/HIGH_RISK tool 均先返回确认卡，未确认不写库。
- 缺 token、篡改 token、跨用户 token 均被拒绝。
- HIGH_RISK 工具在确认文字缺失或错误时均被拒绝，正确输入“确认执行”后才执行。
- `AuditLog` 中所有 WRITE/HIGH_RISK tool 均有 `AI助手` 审计记录；未发现明文 `password`、`confirmationToken`、`secret`、`apiKey`。

浏览器抽验记录：

| 角色 | 入口 | 话术 | 状态 | 实际结果 |
|------|------|------|------|----------|
| 管理员 | `/dashboard` AI 浮窗 | `现在上线还差什么配置` | 通过 | 返回上线就绪检查卡，显示酒类经营资质 BLOCKER 和 4 个 WARNING |
| 销售员 | `/dashboard` AI 浮窗 | `这个月我的业绩怎么样` | 通过 | 命中 `salesperson_performance`，返回本期销售、单数和排名 |
| 仓管 | `/dashboard/warehouse` AI 浮窗 | `给 AIFULL-STK-MOKZNWYX 入库 1 件` | 通过 | 返回商品入库确认卡，未确认前不写库 |
| 财务 | `/dashboard/finance` AI 浮窗 | `给订单 HQMOKZNWYXFIN001 登记收款 1 元` | 已修复并复测通过 | 首次真实浏览器抽验未命中 tool；已修复为订单号可反查客户，复测返回“二次确认登记收款”卡并要求输入“确认执行” |
| 经销商 | `/dealer/incoming` AI 浮窗 | `查一下我的待接订单` | 通过 | 命中 `dealer_incoming_orders`，仅返回当前经销商数据 |
| 消费者 | `/shop` AI 浮窗 | `查一下 AIFULL-JLC-MOKZNWYX` | 通过 | 命中 `search_products`，返回测试商品“剑兰春”价格、库存和状态 |

本轮发现并修复的问题：
- S2：真实浏览器中财务只说订单号登记收款时未命中 `finance_register_payment`。原因是规则 planner 旧逻辑要求同时提供客户手机号，tool schema 也强制 `customerQuery`。已修复为订单号可反查客户，仍要求 HIGH_RISK 二次确认；`test:ai-tools`、`test:ai-runtime`、`test:ai-full-provider` 和浏览器复测均通过。
- S3：全量脚本早期执行时，服务端 action 在脚本环境调用 `revalidatePath` 会触发 Next request scope 错误。已新增仅测试 schema 生效的 revalidate 兼容包装，生产路径仍按 Next 默认行为执行。
- S3：同一 runId 下多个商品名称相似导致真实 provider 在商品写操作中可能传回模糊 `productQuery`。全量脚本已改为使用 SKU 话术并校验目标 SKU 的数据库变化，避免误把非目标商品变更当通过。

当时结论（已由 23.50 的 48 tools 复测更新）：
- 真实 AI provider + think 模式下，当时 47 个 AI tools 已完成本地测试 schema 全量覆盖，自动化结果 `PASS=47 FAIL=0 BLOCKED=0`。
- 六类角色均完成浏览器入口和至少一条真实 AI 对话抽验；财务浏览器暴露的问题已修复并复测通过。
- 默认本地上线 readiness 仍为 BLOCKER，原因是未配置真实微信小程序、公众号、微信支付、税控和酒类经营资质；开发阶段可用 `LAUNCH_READINESS_MODE=trial` 跑通封闭试运营检查，但正式上线前必须替换为真实配置并完成签收。

### 23.29 AI 高风险确认卡浏览器执行回归记录

问题来源：
- 在 `/dashboard/finance` 财务 AI 浮窗中，`给订单 HQMOKZNWYXFIN001 登记收款 1 元` 已能生成“二次确认登记收款”卡，但第一次点击“确认登记”后页面无结果反馈。
- 排查发现当前 3300 dev server 在执行 `next build` 后继续热更新，`.next` 开发产物被生产构建覆盖，导致 `/api/ai/assistant/confirm` 编译时出现 manifest 缺失错误。
- 同时确认卡按钮未显式声明 `type="button"`，在复杂页面结构中存在被浏览器按默认 submit 按钮处理的隐患。

修复与处置：

| 文件/动作 | 结果 |
|-----------|------|
| `src/features/ai/AiFloatingBubble.tsx` | 确认卡按钮新增 `type="button"`，避免默认提交行为干扰确认请求 |
| 重启 `localhost:3300` 测试服务 | 避免 `next build` 覆盖 dev server `.next` 产物后的热更新异常 |

复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 财务生成确认卡 | 通过 | 输入 `给订单 HQMOKZNWYXFIN001 登记收款 1 元` 后返回“二次确认登记收款”卡，客户为 `AI-FULL-MOKZNWYX-财务客户` |
| 高风险确认文字 | 通过 | 未输入“确认执行”时“确认登记”按钮禁用；输入后按钮可点击 |
| 确认执行 | 通过 | 点击“确认登记”后返回“收款已登记”，摘要显示订单 `HQMOKZNWYXFIN001` 已登记收款 `¥1.00` |
| 数据库断言 | 通过 | 订单 `HQMOKZNWYXFIN001` 的 `paidAmount` 从 `5` 增至 `6`，新增 1 条 `amount=1`、`status=COMPLETED` 的收款记录 |
| 审计断言 | 通过 | 新增 `AI助手` 记录“AI 生成确认卡片”“工具待确认”“AI 确认执行完成”“确认执行工具”，同时有“收款/登记收款”业务审计 |
| 脱敏断言 | 通过 | 本次相关 5 条审计记录未发现 `password`、`confirmationToken`、`apiKey`、`secret`、`确认执行` |

补充测试要求：
- 以后不要在同一个正在运行的 `next dev` 服务上直接执行 `next build` 后继续浏览器验收；build 后必须重启 dev server 或改用独立端口。
- AI 高风险确认卡的浏览器验收必须包含“点击确认后业务结果卡、数据库变化、审计日志”三项，不只停留在确认卡生成。

### 23.30 AI 管理员员工账号高风险操作回归记录

问题来源：
- 在管理员 `/dashboard/settings/users` AI 浮窗中输入 `禁用员工 13923612104`，第一次没有生成“禁用员工”确认卡，而是误走 `business_overview` 经营总览。
- 原因是浏览器助手服务优先采用本地 heuristic planner；此前真实 provider 全量脚本直接调用 provider plan，未覆盖 `answerAssistantMessage()` 中“heuristic 优先于 model plan”的 UI 入口差异。
- 禁用成功后再尝试用该账号登录，后端已拒绝，但登录页没有显示清晰错误提示。

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增员工账号核心意图识别：创建员工、启用/禁用员工、重置员工密码；对误规划为 `business_overview` 的员工账号操作进行计划纠偏 |
| `scripts/ai-tools-smoke.ts` | 增加 `禁用员工 13900139088`、`启用员工 13900139088`、`重置员工 13900139088 密码为 AiFull456` 和错误模型规划纠偏断言 |
| `src/app/(auth)/login/login-form.tsx` | 登录失败增加 try/catch 兜底，并提示“员工账号可能已被禁用” |

浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 管理员 AI 生成禁用确认卡 | 通过 | 输入 `禁用员工 13923612104` 后返回“二次确认禁用员工”卡，目标员工为 `AI-FULL-MOKZNWYX-新增仓管` |
| 高风险确认文字 | 通过 | 未输入“确认执行”时“确认禁用”按钮禁用；输入后按钮可点击 |
| 确认执行 | 通过 | 点击“确认禁用”后返回“员工状态已更新”，摘要显示 `AI-FULL-MOKZNWYX-新增仓管 已禁用` |
| 数据库断言 | 通过 | `User.phone=13923612104` 的 `isActive=false` |
| 刷新后业务页面追溯 | 通过 | 用户列表行变为 `AI-FULL-MOKZNWYX-新增仓管 13923612104 仓库 禁用 重置 启用` |
| 审计断言 | 通过 | 新增 `AI助手` 记录“工具待确认”“AI 生成确认卡片”“确认执行工具”“AI 确认执行完成”，并有 `用户/禁用员工` 业务审计 |
| 脱敏断言 | 通过 | 本次相关审计未发现 `password`、`confirmationToken`、`apiKey`、`secret`、`确认执行` |
| 禁用账号登录拦截 | 通过 | 使用 `13923612104` 登录仍停留在 `/login`，页面提示“手机号或密码错误；员工账号可能已被禁用，经销商申请需审核通过后才能登录” |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |

当前结论：
- 浏览器入口下的“管理员员工账号高风险操作”已闭环；这类用例必须同时覆盖 provider plan、assistant-service 实际规划顺序、确认卡执行和登录拦截。
- 后续继续浏览器验收时，不能只依赖 `test:ai-full-provider` 结论；凡是 UI 入口会先走 heuristic planner 的核心意图，都要有至少一条真实浏览器回归。

### 23.31 AI 仓管 SKU 入库浏览器执行回归记录

问题来源：
- 在 `/dashboard/warehouse` 仓管 AI 浮窗中输入 `给 SKU AIFULL-STK-MOKZNWYX 入库 1 件，备注 浏览器回归`，首次返回 `未找到商品：SKU AIFULL-STK-MOKZNWYX 入库 备注 浏览器回归`。
- 根因是商品查询参数没有优先抽取 SKU，且入库语句中的动作词、数量和备注没有稳定剥离，导致完整句子被传给商品查询。

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | `productQueryFromMessage()` 优先返回 SKU；入库/出库规划使用 SKU 优先、最后一个业务数量作为库存数量，并提取 `备注` |
| `scripts/ai-tools-smoke.ts` | 新增 `给 SKU AIFULL-STK-MOKZNWYX 入库 1 件，备注 浏览器回归` 断言，确保 tool 为 `inventory_stock_in`、商品查询为 SKU、数量为 1、备注为 `浏览器回归` |

浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 仓管 AI 生成确认卡 | 通过 | 返回“确认入库”卡片，商品为 `AI-FULL-MOKZNWYX 香脆薯片组合装` |
| 确认执行 | 通过 | 点击“确认入库”后返回“入库成功 / 已入库 1 件” |
| 数据库断言 | 通过 | SKU `AIFULL-STK-MOKZNWYX` 库存从 `101` 增至 `102` |
| 库存流水 | 通过 | 新增 `StockRecord`：`type=IN`、`quantity=1`、`beforeStock=101`、`afterStock=102`、`remark=浏览器回归` |
| 审计断言 | 通过 | 首次失败有 `AI 工具执行异常` 记录；修复后有 `工具待确认`、`确认执行工具`、`AI 确认执行完成`，未发现 token、密钥或密码泄露 |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |

### 23.32 AI 消费者下单参数清洗与确认执行回归记录

问题来源：
- 消费者在 `/shop` AI 浮窗输入 `我要下单 1 箱 AIFULL-JLC-MOKZNWYX 剑兰春，微信支付`。
- 首次结果没有生成下单确认卡，而是返回 `未找到商品：AI-FULL-MOKZNWYX 剑兰春微信支付`。
- 根因是客户下单规划使用 `cleanProductQuery()`，没有优先使用 SKU，也没有把“微信支付/转账支付/现金/月结”等支付方式从商品名中剥离。

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增 `parsePayMethod()` 与 `stripPaymentWords()`；消费者下单使用 `productQueryFromMessage()`，SKU 优先，支付方式单独进入 `payMethod` |
| `scripts/ai-tools-smoke.ts` | 新增“SKU + 商品名 + 微信支付”和“商品名 + 转账支付”下单断言，防止支付词再次拼进商品名 |

浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 真实 AI 下单意图 | 已修复并复测 | 同一句话复测后返回“确认提交订单”卡片，没有误命中经营总览或商品未找到 |
| 确认卡内容 | 通过 | 商品 `AI-FULL-MOKZNWYX 剑兰春`，单价 `¥81.00`，数量 `1`，收货人为 `AI-FULL-MOKZNWYX-消费者 13923612093` |
| 确认执行 | 通过 | 点击“确认下单”后生成订单 `HQ20260430000050` |
| 数据库断言 | 通过 | 订单 `status=PAID`、`payMethod=WECHAT`、`payableAmount=81`、`paidAmount=81`，支付记录 `amount=81`、`status=COMPLETED` |
| 库存/销量断言 | 通过 | SKU `AIFULL-JLC-MOKZNWYX` 库存从 `199` 降至 `198`，销量从 `1` 增至 `2` |
| 审计断言 | 通过 | `AI助手` 记录中 `args.productQuery=AIFULL-JLC-MOKZNWYX`、`payMethod=WECHAT`，未发现 `password`、`confirmationToken`、`apiKey`、`secret` |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |

当前结论：
- 该问题证明真实浏览器话术必须覆盖“SKU + 商品名 + 支付方式 + 数量单位”的组合，不应只测 `我要下单 1 箱剑兰春` 这类最短句式。
- 下单、调价、库存、收款、接单/拒单等核心写意图的参数清洗应全部采用“结构化字段优先、业务修饰词剥离、动作词剥离”的策略。

### 23.33 AI 经销商接单/拒单浏览器执行与原因清洗回归记录

测试数据：

| 对象 | 用途 | 结果 |
|------|------|------|
| `HQBROWSERACC585212` | 经销商 AI 接单浏览器测试 | 生成待接 routing 后由经销商 AI 确认接单 |
| `HQBROWSERREJ585228` | 经销商 AI 拒单基础测试 | 生成待接 routing 后由经销商 AI 确认拒单，暴露“原因 太远”未清洗问题 |
| `HQBROWSERREJX730653` | 经销商 AI 拒单原因清洗复测 | 修复后复测，拒单原因正确保存为 `太远` |

接单浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 经销商登录 | 通过 | `13923612096 / aiFull123` 登录后进入 `/dealer/incoming` |
| AI 接单确认卡 | 通过 | 输入 `接单 HQBROWSERACC585212` 后返回“确认接单”卡片 |
| 确认执行 | 通过 | 点击“确认接单”后返回“订单已接受，请及时处理配送” |
| 数据库断言 | 通过 | `OrderRouting.status=ACCEPTED`、`respondedAt` 已写入；订单状态变为 `CONFIRMED` |
| 审计断言 | 通过 | `AI助手` 有 `工具待确认`、`确认执行工具` 记录，`after` 中未发现 token、密钥或密码 |

拒单浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| AI 拒单确认卡 | 已修复并复测 | 输入 `拒单 HQBROWSERREJX730653 原因 太远` 后返回“确认拒单”卡片，原因显示为 `太远` |
| 确认执行 | 通过 | 点击“确认拒单”后返回“订单已拒绝并自动重匹配” |
| 数据库断言 | 通过 | `OrderRouting.status=REJECTED`、`reason=太远`、`respondedAt` 已写入 |
| 渠道冲突断言 | 通过 | 新增 `ChannelConflict.type=REJECTION`、`status=OPEN`，`detail.text=太远`，事件 `CREATE_FROM_DEALER_REJECT` 可追溯 |
| 审计断言 | 通过 | `AI助手` 审计 `args.reason=太远`、`routingId=HQBROWSERREJX730653`，未发现 `password`、`confirmationToken`、`apiKey`、`secret` |

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 经销商拒单原因清理新增 `原因` / `原因:` / `原因：` 前缀剥离 |
| `scripts/ai-tools-smoke.ts` | 新增 `拒单 HQ20260430000008 原因 太远` 断言，确保 `reason=太远` |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |

剩余体验问题：
- 已在 23.35 修复并复测：经销商 AI 接单/拒单确认成功后，当前 `/dealer/incoming` 路由会自动刷新，列表可直接移除已处理订单；AI 结果卡仍保留用于确认执行结果。

### 23.34 本轮收尾自动化回归与服务状态

本轮在修复消费者下单、经销商拒单原因清洗后，重新执行自动化和构建门禁：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-full-provider` | 通过 | runId=`AI-FULL-MOL3YUB7`，`PASS=47 FAIL=0 BLOCKED=0` |
| `npm.cmd run build` | 通过 | Next.js production build 成功 |
| `npm.cmd run check:launch` | 阻塞项符合预期 | 默认本地配置返回 `Launch readiness: BLOCKER`，阻塞项为酒类经营资质未配置 |
| 临时配置 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | 返回 `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0` |

服务状态：
- `npm.cmd run build` 后已重启 `localhost:3300` dev server。
- 当前 3300 端口已重新监听，`DATABASE_URL` 指向 `goods_sell_test` schema，浏览器可继续进行本地模拟验收。
- 本轮未修改 `.env.local`；酒类经营资质的临时值仅在单次命令环境变量中注入。

### 23.35 AI 确认执行后页面自动刷新回归记录

问题来源：
- 经销商 AI 接单/拒单确认成功后，数据库状态和 AI 结果卡正确，但当前 `/dealer/incoming` 列表不会自动移除已处理订单，需要手动刷新才能看到“暂无待接订单”。

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/AiFloatingBubble.tsx` | 确认执行成功并写入 AI 结果卡后调用 `router.refresh()`，让当前路由重新拉取 Server Components 数据，同时保留 AI 对话中的执行结果 |

浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 经销商待接数据 | 通过 | 新造待接订单 `HQBROWSERREF236354`，经销商登录后 `/dealer/incoming` 正常显示待接卡片 |
| AI 接单确认卡 | 通过 | 输入 `接单 HQBROWSERREF236354` 后返回“确认接单”卡片，未直接写库 |
| 确认执行与页面刷新 | 通过 | 点击“确认接单”后页面无需手动刷新，主列表自动变为 `暂无待接订单`，AI 对话仍显示 `已接单` 结果卡 |
| 数据库断言 | 通过 | `OrderRouting.status=ACCEPTED`、`respondedAt` 已写入；订单状态为 `CONFIRMED`，订单应收金额 `¥81` |
| 审计断言 | 通过 | `AI助手` 存在 `工具待确认` 和 `确认执行工具` 两条记录；审计 `after` 中未发现 `password`、`confirmationToken`、`apiKey`、`secret` |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |

当前结论：
- 经销商 AI 接单确认成功后的页面刷新体验已修复，后续接单/拒单类确认流程不再只依赖 AI 结果卡判断完成状态。

### 23.36 真实 Provider 误规划、销售员新品推送与审计脱敏回归记录

本轮继续执行“真实 AI provider + 浏览器入口 + 写操作审计”测试，新增暴露 2 个规划问题和 1 个审计脱敏问题，均已修复并复测。

真实 provider 全量复跑记录：

| 批次 | runId | 状态 | 结果 |
|------|-------|------|------|
| 首次复跑 | `AI-FULL-MOL4Y0RU` | 失败 | `PASS=45 FAIL=2 BLOCKED=0`；`system_launch_readiness` 被 provider 误写为 `system_launch_ready`，`marketing_create_product_push` 被误写为 `marketing_product_push` |
| 修复后复跑 | `AI-FULL-MOL5C382` | 通过 | `PASS=47 FAIL=0 BLOCKED=0`；上线检查和新品推送均命中精确工具名 |
| 审计脱敏补丁后复跑 | `AI-FULL-MOL5Y8Z7` | 通过 | `PASS=47 FAIL=0 BLOCKED=0`；47 个 tools、RBAC unauthorized checks、WRITE/HIGH_RISK 确认卡和审计均通过 |

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/assistant-service.ts` | 模型规划 prompt 明确要求 `toolName` 必须逐字复制可用工具名；模型返回未注册工具名时不再继续执行 |
| `scripts/ai-full-provider-coverage.ts` | 全量 provider 测试 prompt 加入工具名精确匹配约束，并列出本轮发现的常见错误别名 |
| `src/features/ai/tools/planner.ts` | 新增新品推送写操作意图识别，`新品推送/商品推送/产品推送` 不再误归为 `channel_summary`；新增 SKU、目标画像、话术提取 |
| `scripts/ai-tools-smoke.ts` | 新增销售员新品推送规划断言、错误规划纠偏断言、AI 审计自由文本脱敏断言 |
| `src/features/ai/tools/audit.ts` | 对 AI 审计中的自由文本 `input`、`error`、`result` 等字符串做脱敏，移除密码值、token/key/secret 值和高风险确认文字 |

销售员新品推送浏览器复测：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 入口与角色 | 通过 | 使用销售员账号进入 `/dashboard`，AI 浮窗可打开 |
| 误规划回归 | 通过 | 输入 `把新品 SKU AIFULL-NEW-MOL5C382 推送给 BROWSER-PUSH-MOL5C382 人群，话术 browser provider push`，返回 `二次确认新品推送`，没有误命中渠道摘要 |
| 高风险确认 | 通过 | 未输入确认文字时按钮禁用；输入后可点击 |
| 写操作执行 | 通过 | 点击 `确认推送` 后返回 `新品推送已生成`，摘要显示 `AI-FULL-MOL5C382 新品果汁 已生成 1 条推送` |
| 数据库断言 | 通过 | 新增 `ProductPush`：目标画像 `BROWSER-PUSH-MOL5C382`、话术 `browser provider push`、状态 `SENT`、客户 `AI-FULL-MOL5C382-消费者` |
| 审计断言 | 通过 | `AI助手` 存在生成确认卡、工具待确认、确认执行工具、确认执行完成记录；未发现 `password`、`confirmationToken`、`apiKey`、`secret` |

浏览器过程中同时验证了业务边界：
- 当目标画像为 `高价值` 但测试库没有对应客户时，确认执行返回 `该画像标签下暂无可推送客户`。
- 当目标画像 `AI-FULL-MOL5C382-高价值` 已被同一新品覆盖时，确认执行返回 `该新品已覆盖当前画像客户`。
- 以上两种失败均不生成重复推送，且保留失败审计。

销售员越权与审计脱敏复测：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 销售员越权创建员工 | 通过 | 销售员输入创建员工账号话术后，页面返回 `无权限查看或操作该事项`，没有生成确认卡或创建账号 |
| 越权审计 | 通过 | 新增 `越权工具请求` 和 `AI 工具执行失败` 审计，目标工具为 `settings_create_staff_user` |
| 脱敏缺陷 | 已修复 | 修复前 `AI 工具执行失败` 的自由文本 `input` 会保留测试密码；修复后复测输入 `Sensitive123`，最新审计仅保存 `密码 [REDACTED]` |
| 历史测试审计回填 | 已完成 | 仅在 `goods_sell_test` 中扫描 `AI助手` 审计 741 条，回填脱敏 79 条；近 120 条 AI 审计未发现 `AiFull123`、`Sensitive123`、`confirmationToken`、`apiKey` |

本轮自动化命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:phase5` | 通过 | `Phase 5 smoke passed`，小程序首页/目录/登录/购物车鉴权/分享/公众号菜单鉴权通过 |
| `npm.cmd run test:phase6` | 通过 | `Phase 6 smoke passed { totalSku: 62, deliveryRows: 62, payments: 73, users: 54, logs: 914 }` |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| `npm.cmd run test:ai-full-provider` | 通过 | 最新 runId=`AI-FULL-MOL5Y8Z7`，`PASS=47 FAIL=0 BLOCKED=0` |
| `npm.cmd run build` | 通过 | production build 成功，最新 `BUILD_ID=6hw6VuUP37tuviPLb-Cgi` |
| `npm.cmd run check:launch` | 阻塞项符合预期 | 默认配置 `Launch readiness: BLOCKER`，酒类经营资质未配置 |
| 临时配置 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0` |

服务状态：
- `localhost:3300` 已在 build 后重启，当前监听进程正常。
- 当前服务仍使用 `goods_sell_test` schema，适合继续浏览器模拟测试。
- 本轮未把临时酒类资质写入 `.env.local`，只在单次命令环境变量中模拟。

当前结论：
- 真实 provider 工具名误规划已通过 prompt 与测试约束修复，不能再用近似别名绕过全量测试。
- 销售员新品推送浏览器写流程已闭环，且覆盖了“无目标客户”和“重复推送”两个业务失败分支。
- AI 审计自由文本脱敏问题已修复并回填测试库历史记录；后续凡涉及密码、token、确认文字的测试，必须同时核查 `AuditLog.after`。

### 23.37 AI 调价、开普票与仓管履约浏览器补充回归记录

本轮继续使用真实 AI provider + `localhost:3300` 浏览器入口执行写流程验收，覆盖管理员、财务、仓管三个角色。测试数据均写入本地 `goods_sell_test` schema。

浏览器复测记录：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 管理员调价 | 通过 | 管理员输入 `把 SKU AIFULL-NEW-MOL5Y8Z7 零售价改成 59`，返回“确认商品调价”卡片；点击“确认调价”后返回“商品价格已更新” |
| 调价数据库断言 | 通过 | SKU `AIFULL-NEW-MOL5Y8Z7` 的 `retailPrice=59` |
| 调价审计断言 | 通过 | `AI助手` 存在“工具待确认”“AI 生成确认卡片”“确认执行工具”“AI 确认执行完成”，业务审计为 `商品 / AI 调价`，未发现明文 token、密钥或密码 |
| 财务开普票首测 | 失败 S2，已修复 | 财务输入 `给订单 HQBROWSERINV166053 开普票，购方 浏览器测试公司，税号 91430000BROWSERTEST` 时，首次误命中 `business_overview`，没有生成开票确认卡 |
| 财务开普票复测 | 通过 | 修复后同一句话返回“二次确认开票”卡片；输入“确认执行”并点击“确认开票”后返回“发票已开具” |
| 开票数据库断言 | 通过 | 订单 `HQBROWSERINV166053` 生成发票 `FP20260430000010`，`type=NORMAL`、`status=ISSUED`、购方 `浏览器测试公司`、税号 `91430000BROWSERTEST`、金额 `66`、税额 `3.74` |
| 开票审计断言 | 通过 | `AI助手` 和 `票据 / 开具发票` 审计完整，未发现 `password`、`confirmationToken`、`apiKey`、`secret`、确认文字泄露 |
| 仓管履约 | 通过 | 仓管输入 `把订单 HQBROWSERFUL355869 发货`，返回“确认更新订单状态”卡片；点击“确认执行”后返回“订单状态已更新” |
| 履约数据库断言 | 通过 | 订单 `HQBROWSERFUL355869` 从 `CONFIRMED` 变为 `SHIPPING`，创建配送记录 `status=SHIPPING`、`method=自配送`，商品 SKU `AIFULL-JLC-MOL5Y8Z7`、数量 `1` |
| 履约审计断言 | 通过 | 存在 `AI助手 / 确认执行工具` 与 `订单 / 更新订单状态` 两条关键审计，未发现明文 token、密钥或密码 |

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/features/ai/tools/planner.ts` | 新增 `parseInvoiceIssue()`，支持 `开普票`、`开专票`、`发票` 等自然语言；可从“购方/抬头/公司”和“税号/纳税人识别号”提取开票参数；财务开票意图在经营总览前优先判定 |
| `scripts/ai-tools-smoke.ts` | 新增 `给订单 HQBROWSERINV166053 开普票，购方 浏览器测试公司，税号 91430000BROWSERTEST` 规划断言，确保命中 `receipts_issue_invoice`；同时增加误规划为 `business_overview` 时的核心意图纠偏断言 |

回归命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools`，新增开普票用例通过 |

当前结论：
- “开普票”这类中文口语表达不能只按连续词 `开票` 匹配，必须覆盖 `开.*票` 和 `发票` 这类表达，否则真实浏览器入口会把财务写操作误规划成经营总览。
- 管理员调价、财务开票、仓管履约三条浏览器写流程均已按“真实 AI 对话 -> 确认卡 -> 点击确认 -> 数据库断言 -> 审计断言”闭环。
- 后续继续扩大浏览器验收时，所有核心写意图都要记录原话、实际 tool、确认卡、业务对象号、数据库断言和审计断言。

### 23.38 本轮全量自动化复跑与测试服务恢复记录

本轮在 23.37 浏览器补充回归后，继续复跑真实 provider 和传统 smoke，确保新增开普票规划修复没有破坏其他 tools。

自动化复跑记录：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools`，包含开普票规划断言 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-full-provider` | 通过 | 最新 runId=`AI-FULL-MOL6WB9L`，`PASS=47 FAIL=0 BLOCKED=0`，RBAC unauthorized checks 通过 |
| `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| `npm.cmd run test:phase5` | 首次环境失败，已修正 | 默认脚本访问 `http://localhost:3000`，当前测试服务在 `3300`，因此首次 `fetch failed`；设置 `PHASE5_BASE_URL=http://localhost:3300` 后复跑通过 |
| 临时指定 `goods_sell_test` 且设置 `PHASE5_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase5` | 通过 | `Phase 5 smoke passed` |
| 临时指定 `goods_sell_test` 且设置 `PHASE6_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase6` | 通过 | `Phase 6 smoke passed { totalSku: 70, deliveryRows: 78, payments: 91, users: 68, logs: 1135 }` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run build` | 通过 | Next.js production build 成功 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run check:launch` | 阻塞项符合预期 | `Launch readiness: BLOCKER`，`READY 5 / WARNING 4 / BLOCKER 1`，阻塞项仍为酒类经营资质未配置 |
| 临时指定 `goods_sell_test` 且注入 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0`；微信小程序、公众号、微信支付、税控仍为 WARNING |

服务恢复记录：
- 执行 `next build` 前已停止 `localhost:3300` dev server，避免 build 覆盖 dev 产物后继续浏览器验收。
- build 和 readiness 完成后，已用 `goods_sell_test` schema、`NEXTAUTH_URL=http://localhost:3300`、`NEXT_PUBLIC_APP_URL=http://localhost:3300`、`AUTH_TRUST_HOST=true` 重新启动 `localhost:3300`。
- 浏览器重载 `/dashboard` 后验证：无 `Application error`，AI 助手入口可见。

当前结论：
- 本轮新增修复后，真实 AI provider 47 tools 全量覆盖仍为全绿。
- Phase 5/Phase 6 smoke 均已按当前 3300 测试服务复跑通过；以后执行这两条命令时必须显式设置 `PHASE5_BASE_URL` / `PHASE6_BASE_URL`，避免误打默认 3000。
- 当前仍不应把临时测试资质当作正式上线条件；它只用于开发/模拟环境跑通全流程。

### 23.39 第三方接口边界 smoke 记录

为继续推进剩余“微信支付/公众号/税控”测试，本轮新增 `scripts/third-party-boundary-smoke.ts` 和 `npm.cmd run test:third-party`。该脚本只在 `goods_sell_test` schema 中执行，且在检测到真实微信支付或真实公众号配置齐全时会停止，避免自动化测试未经确认发布公众号菜单或向微信支付传输测试订单。

覆盖范围：

| 验收项 | 状态 | 实际结果 |
|--------|------|----------|
| 小程序首页/目录公开接口 | 通过 | `/api/wechat/mini/home`、`/api/wechat/mini/catalog` 返回 200 |
| 未登录预支付 | 通过 | `/api/wechat/pay/prepay` 未带 token 返回 401 |
| 非法支付回调 | 通过 | 缺少订单号、交易号或金额时返回 400 |
| Mock 微信登录 | 通过 | 未配置小程序 AppID/Secret 时使用 mock openId，返回 token |
| 小程序地址、购物车、下单 | 通过 | 创建测试地址、加入购物车并生成小程序待支付订单 |
| Mock 预支付 | 通过 | 未配置完整微信支付时返回 `mocked=true`，`prepayId=mock_prepay_{orderNo}` |
| 成功支付回调 | 通过 | 模拟成功回调后订单变为 `PAID`，支付记录变为 `COMPLETED`，库存扣减 1 次 |
| 重复支付回调 | 通过 | 重复同一回调保持幂等，不重复扣库存、不重复增加完成收款 |
| 少付金额回调 | 通过 | 少于应付金额时返回 400，订单保持 `PENDING_PAYMENT`，不写完成收款 |
| 公众号菜单 Mock | 通过 | 未配置公众号 AppID/Secret 时只写入 `WechatMessageLog.scene=official_menu`、`status=MOCKED`，不发布真实菜单 |
| 税控 Mock 开票 | 通过 | 强制 `TAX_PROVIDER=MOCK` 后开票成功，发票 `provider=MOCK`、`status=ISSUED` |
| 订单模板消息降级 | 通过 | 支付成功后记录订单消息日志，未配置公众号模板时状态为 `SKIPPED` 或 `MOCKED` |

执行记录：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 且设置 `THIRD_PARTY_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:third-party` | 通过 | 最新 runId=`TPMOL8E8A8`，支付成功订单 `HQ20260430000104`，少付拦截订单 `HQ20260430000105`，Mock 发票 `FP20260430000013`，公众号菜单日志 `MOCKED` |

当前结论：
- 本地可执行的微信支付、公众号、税控边界已经从“只看 readiness”推进到“实际写订单、支付、库存、发票、消息日志并断言”。
- 真实小额微信支付、真实公众号菜单发布、小程序体验版和真实税控接口仍不能用本地 mock 结果替代，必须在正式测试环境单独签收。

### 23.40 Chrome/Edge 桌面与移动兼容性 smoke 记录

为继续推进“多浏览器/移动端兼容”剩余项，本轮新增 `scripts/browser-compat-smoke.ts` 和 `npm.cmd run test:browser-compat`。脚本使用本机 Chrome 与 Edge headless，通过 CDP 以桌面 `1366x768` 和移动 `390x844` 两种视口打开核心页面，检查是否出现 `Application error`、404 错页文本或页面级横向溢出。

覆盖页面：
- 公开端：`/login`、`/register`、`/shop`、`/shop/catalog`、`/shop/cart`。
- 管理后台：`/dashboard`、`/dashboard/orders`、`/dashboard/finance`、`/dashboard/wechat`、`/dashboard/settings/users`、`/dashboard/salespeople`、`/dashboard/products/{id}/edit`。
- 经销商端：`/dealer/incoming`、`/dealer/stock`、`/dealer/settlement`。

首次执行发现的问题：

| 缺陷 | 等级 | 影响 | 修复 |
|------|------|------|------|
| 移动端 `/dashboard` 横向溢出 | S3 | Chrome/Edge 移动 390 视口下 `scrollWidth=816`，主要由图表/表格卡片缺少 `min-w-0` 约束触发 | `DashboardCharts`、仪表盘卡片和表格容器补充 `min-w-0`、`overflow-hidden`、`max-w-full overflow-x-auto` |
| 移动端 `/dashboard/wechat` 横向溢出 | S3 | Chrome/Edge 移动 390 视口下 `scrollWidth=676`，支付回调地址长 URL 撑宽页面 | 支付回调地址增加 `break-all`，日志/分享卡片增加 `min-w-0` |

修复范围：

| 文件 | 修复内容 |
|------|----------|
| `src/components/charts/DashboardCharts.tsx` | 图表网格、图表卡片和图表容器增加移动端宽度约束，避免 Recharts 在窄屏撑开页面 |
| `src/app/(dashboard)/dashboard/page.tsx` | 仪表盘根容器、主要 grid、卡片、表格滚动容器增加 `min-w-0` / `max-w-full overflow-x-auto` |
| `src/app/(dashboard)/dashboard/wechat/page.tsx` | 支付回调 URL 增加断行；消息日志和分享记录卡片增加 `min-w-0` |

复跑结果：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | 最新复跑已扩展为 Chrome/Edge × desktop/mobile 共 68 个页面检查通过；无 `Application error`，无页面级横向溢出 |

当前结论：
- 本地 Windows Chrome 与 Edge 的桌面/移动视口核心页面兼容性已通过自动化 smoke；最新脚本已覆盖管理员 `/dashboard/salespeople` 与 `/dashboard/products/{id}/edit`。
- 微信内置浏览器、iOS Safari、Android Chrome 真实设备、小程序体验版仍需正式环境人工签收；本轮 headless 结果不能替代端内授权、支付和分享能力验收。

### 23.41 本轮收尾门禁与服务恢复记录

本轮新增第三方边界测试、浏览器兼容性测试，并修复移动端横向溢出后，执行以下收尾门禁：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| 临时指定 `goods_sell_test` 且设置 `THIRD_PARTY_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:third-party` | 通过 | 最新 runId=`TPMOL8E8A8` |
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | Chrome/Edge × desktop/mobile 共 52 个页面检查通过 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run build` | 通过 | Next.js production build 成功 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run check:launch` | 阻塞项符合预期 | `Launch readiness: BLOCKER`，`READY 5 / WARNING 4 / BLOCKER 1`，阻塞项仍为酒类经营资质未配置 |
| 临时指定 `goods_sell_test` 且注入 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0`；微信小程序、公众号、微信支付、税控仍为 WARNING |

服务恢复记录：
- build 前已停止 `localhost:3300` dev server，避免 build 覆盖 dev 产物后继续浏览器验收。
- build 后已用 `goods_sell_test` schema、`NEXTAUTH_URL=http://localhost:3300`、`NEXT_PUBLIC_APP_URL=http://localhost:3300`、`AUTH_TRUST_HOST=true` 重新启动 `localhost:3300`。
- 浏览器重载后验证：当前页无 `Application error`，AI 助手入口可见。

当前剩余只能在正式/第三方环境完成的签收项：
- 微信开发者工具或小程序体验版：授权登录、手机号绑定、商品、购物车、订单、分享路径端内验收。
- 微信支付沙箱或小额真实支付：真实 JSAPI 拉起、支付完成、微信侧通知签名、重复通知、退款或异常支付人工兜底。
- 公众号真实发布：真实菜单发布、模板消息发送、OpenID 绑定和跳转链路。
- 税控真实联调：真实税控 provider、发票开具、失败重试、税控平台状态回传。
- 真实设备兼容：iOS Safari、Android Chrome、iOS/Android 微信内置浏览器的授权、下单、AI 浮窗、支付和分享验收。
- 业务人员签收：仓管、财务、销售、经销商、运营负责人和客户代表在正式测试库完成逐项签字确认。

### 23.42 销售员管理页 RBAC 漏测修复与浏览器复测记录

本轮继续按“浏览器内模拟运行”方式检查剩余页面时，使用仓管账号 `AI-FULL-MOL5Y8Z7-仓管` 访问 `/dashboard/salespeople`，发现页面可见并显示 `业务员绩效看板`。该问题属于权限可见性缺陷：写操作 action 层仍会因 `settings:manage` 权限不足而拦截，但仓管不应看到销售员管理与绩效页面。

缺陷闭环记录：

| 项目 | 结论 |
|------|------|
| 缺陷等级 | S2 |
| 影响范围 | 仓管、财务、销售员等内部非管理员角色可能绕过菜单直接打开 `/dashboard/salespeople`，看到销售员管理/绩效页面 |
| 根因 | `src/features/auth/permissions.ts` 未单独配置 `/dashboard/salespeople` 路由；`/dashboard/sales` 规则不会匹配 `/dashboard/salespeople`，因此该页面落入 `/dashboard` 的通用内部员工规则 |
| 修复 | 新增 `/dashboard/salespeople` 路由权限，限定为 `settings:manage`，并放在 `/dashboard/sales` 之前 |
| 自动化补强 | `scripts/rbac-smoke.ts` 新增仓管、财务、销售员禁止访问 `/dashboard/salespeople`，管理员允许访问的断言 |
| 兼容 smoke 补强 | `scripts/browser-compat-smoke.ts` 将管理员 `/dashboard/salespeople` 纳入 Chrome/Edge 桌面与移动视口检查 |

浏览器复测记录：

| 用例 | 状态 | 实际结果 |
|------|------|----------|
| 仓管直接访问 `/dashboard/salespeople` | 通过 | 当前真实浏览器跳转到 `/forbidden?from=%2Fdashboard%2Fsalespeople` |
| `/forbidden` 页面角色提示 | 通过 | 页面显示 `当前账号无权访问该页面`，角色为 `仓管`，来源页面为 `/dashboard/salespeople` |
| 管理员访问销售员管理页 | 通过 | `test:browser-compat` 以管理员登录后，Chrome/Edge 桌面与移动视口均打开 `/dashboard/salespeople`，H1 为 `业务员绩效看板` |

本轮复测命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 后执行 `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run lint` | 通过 | ESLint 无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | Chrome/Edge × desktop/mobile 共 56 个页面检查通过，新增 `/dashboard/salespeople` 检查 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run build` | 通过 | Next.js production build 成功，`/dashboard/salespeople` 构建为动态服务端页面 |
| build 后重启 `localhost:3300` 测试服务 | 通过 | 重新使用 `goods_sell_test` schema 启动 dev server，浏览器重载 `/forbidden?from=%2Fdashboard%2Fsalespeople` 后恢复正常 |

当前结论：
- 本次问题说明只覆盖菜单和 action 权限不够，所有后台二级页面都必须有显式路由级 RBAC 断言。
- 后续每新增后台路由，必须同步补充 `getRouteRequirement()` 路由规则、`test:rbac` 角色矩阵，以及至少一个浏览器/兼容 smoke 入口。
- 本轮修复后，仓管、财务、销售员不能再直接访问销售员管理页；管理员访问路径保持正常。
- 当前 `localhost:3300` 已恢复为可继续浏览器验收的测试服务，仍指向 `goods_sell_test` schema。

### 23.43 产品编辑页动态路由 RBAC 漏测修复与浏览器复测记录

本轮继续复查后台二级/动态路由时，发现 `/dashboard/products/{id}/edit` 会落入 `/dashboard/products` 的产品查看规则。非管理员虽然不能通过产品列表看到“编辑”按钮，且保存时会被 server action 的 `products:write` 拦截，但仍可通过直达 URL 打开产品编辑表单并看到进价、批发价、零售价、库存、安全库存等可编辑字段。

缺陷闭环记录：

| 项目 | 结论 |
|------|------|
| 缺陷等级 | S2 |
| 影响范围 | 销售员、仓管、财务等非管理员内部账号可直达产品编辑表单；保存操作会失败，但编辑页数据可见性不符合最小权限原则 |
| 根因 | 路由规则只对 `/dashboard/products/new`、分类、品牌、素材做了 `products:write` 限制，动态编辑页 `/dashboard/products/{id}/edit` 被 `/dashboard/products` 的查看权限覆盖 |
| 修复 | `src/features/auth/permissions.ts` 支持 `pattern` 路由规则，新增 `/^\/dashboard\/products\/[^/]+\/edit\/?$/`，限定为 `products:write` |
| 自动化补强 | `scripts/rbac-smoke.ts` 新增仓管、财务、销售员禁止访问产品编辑页，管理员允许访问产品编辑页的断言 |
| 兼容 smoke 补强 | `scripts/browser-compat-smoke.ts` 从测试库读取真实产品 ID，将管理员 `/dashboard/products/{id}/edit` 纳入 Chrome/Edge 桌面与移动视口检查 |

浏览器复测记录：

| 用例 | 状态 | 实际结果 |
|------|------|----------|
| 仓管直接访问真实产品编辑页 | 通过 | 使用产品 `cmol8e8ej0002vwyoyvnz94f6` 访问 `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6/edit`，跳转到 `/forbidden?from=%2Fdashboard%2Fproducts%2Fcmol8e8ej0002vwyoyvnz94f6%2Fedit` |
| `/forbidden` 页面角色提示 | 通过 | 页面显示 `当前账号无权访问该页面`，角色为 `仓管`，来源页面为真实产品编辑路径 |
| 管理员访问产品编辑页 | 通过 | `test:browser-compat` 以管理员登录后，Chrome/Edge 桌面与移动视口均打开真实产品编辑页，H1 为 `编辑产品` |

本轮复测命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 后执行 `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run lint` | 通过 | ESLint 无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | Chrome/Edge × desktop/mobile 共 68 个页面检查通过，新增真实产品详情/编辑页检查和仓管价格字段断言 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run build` | 通过 | Next.js production build 成功 |
| build 后重启 `localhost:3300` 测试服务 | 通过 | 重新使用 `goods_sell_test` schema 启动 dev server，浏览器重载产品编辑页 forbidden 后恢复正常 |

当前结论：
- 动态路由不能只依赖父级 prefix；凡是页面内包含写表单、敏感价格、库存或账号信息，都必须有单独的路由级权限规则。
- 后续继续扩展 RBAC smoke 时，应优先覆盖所有 `{id}` 动态详情/编辑页，避免菜单隐藏但 URL 直达可见。
- 本轮修复后，非管理员不能直达产品编辑页；管理员编辑路径在 Chrome/Edge 桌面与移动视口保持可用。

### 23.44 动态详情/编辑页权限巡检补充记录

本轮在修复销售员管理页和产品编辑页后，继续对后台动态路由做了只读巡检，重点检查页面是否包含写表单、确认按钮、价格/库存/客户/财务字段，以及查询层是否有角色数据边界。

巡检结论：

| 路由 | 当前权限与结论 |
|------|----------------|
| `/dashboard/customers/{id}` | 当前按 `customers:view` 放行；销售员查询层限制本人名下客户，管理员/财务可看全部；页面为只读画像、地址、订单历史和欠款信息，未发现写表单直达问题 |
| `/dashboard/orders/{id}` | 当前按 `orders:view` 放行；销售员查询层限制本人相关订单；状态操作按钮根据 `orders:write` / `orders:fulfill` 控制，未发现错误角色可见操作按钮 |
| `/dashboard/dealers/{id}/policy` | 当前按 `dealers:view` 放行；销售员查询层限制本人名下经销商，保存 action 也二次校验；未发现非本人政策页直达问题 |
| `/dashboard/delivery/{id}` | 当前按 `delivery:manage` 放行，仅管理员/仓管；页面含配送操作，权限规则符合预期 |
| `/dashboard/warehouse/checks/{id}` | 当前按 `warehouse:manage` 放行，仅管理员/仓管；页面含盘点确认表单，权限规则符合预期 |
| `/dashboard/products/{id}` | 当前按 `products:view` 放行；本轮已将进价/批发价改为字段级权限展示：管理员/财务可看进价，管理员/财务/销售可看批发价，仓管仅保留零售价、会员价、库存、安全库存等与仓储相关字段 |

后续建议：
- 将 `test:rbac` 从少量断言升级为“后台路由矩阵测试”，至少覆盖每个 `page.tsx` 的代表 URL、允许角色和禁止角色。
- 产品详情页成本/批发字段已完成字段级权限收敛；后续如需进一步区分会员价、库存、安全库存等字段，可继续按角色拆分展示。
- 继续新增页面时，同步维护“菜单可见性 + 路由可访问性 + 查询数据边界 + action 二次校验”四层测试。

### 23.45 RBAC 路由矩阵与产品详情价格字段权限回归记录

本轮将 `test:rbac` 从零散断言升级为路由矩阵 smoke，覆盖公开页、商城受保护页、经销商端、后台各业务模块、动态详情页和动态编辑页的代表 URL。矩阵逐一校验匿名、管理员、销售员、仓管、财务、消费者、经销商七类主体的可访问性，避免后续再出现“菜单隐藏但 URL 直达可见”的漏测。

自动化补强：

| 文件 | 变更 |
|------|------|
| `scripts/rbac-smoke.ts` | 新增 `assertRouteCoverage()`，覆盖公开端、商城端、经销商端和后台 50+ 个代表路径，包括 `/dashboard/products/{id}/edit`、`/dashboard/salespeople`、`/dashboard/warehouse/checks/{id}` 等动态路由 |
| `src/app/(dashboard)/dashboard/products/[id]/page.tsx` | 新增字段级展示权限：进价仅管理员/财务可见，批发价仅管理员/财务/销售可见，仓管产品详情页不再显示进价/批发价 |
| `scripts/browser-compat-smoke.ts` | 新增仓管测试账号；管理员产品详情页断言包含进价/批发价，仓管同一路径断言不包含进价/批发价但包含零售价/当前库存 |

本轮复测命令：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 后执行 `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run lint` | 通过 | ESLint 无错误 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed`，路由矩阵全通过 |
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | Chrome/Edge × desktop/mobile 共 68 个页面检查通过，并完成管理员/仓管产品价格字段断言 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run build` | 通过 | Next.js production build 成功 |
| build 后重启 `localhost:3300` 测试服务 | 通过 | 重新使用 `goods_sell_test` schema 启动 dev server，仓管产品详情页重载后仍隐藏进价/批发价 |

浏览器补充复测：

| 用例 | 状态 | 实际结果 |
|------|------|----------|
| 仓管打开真实产品详情页 | 通过 | `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6` 可访问，显示零售价、会员价、当前库存、安全库存，不显示进价/批发价 |
| 管理员打开真实产品详情页 | 通过 | `test:browser-compat` 断言管理员同一路径包含进价、批发价、零售价 |
| 仓管打开真实产品编辑页 | 通过 | `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6/edit` 跳转 `/forbidden` |

当前结论：
- RBAC 测试已从点状补丁推进到代表路由矩阵，可覆盖绝大多数新增路由权限回归。
- 产品详情页已完成第一批字段级权限收敛，成本价不再暴露给仓管。
- 后续若继续增强，应把浏览器兼容 smoke 的价格字段断言抽成独立的“字段权限 smoke”，避免兼容测试脚本继续膨胀。

### 23.46 真实 AI Provider 47 Tools 全量复跑记录

本轮在完成 RBAC 路由矩阵和产品详情价格字段权限修复后，继续使用真实 AI provider、think 模式和本地 `goods_sell_test` schema 复跑 AI 代办助手全量 tools 覆盖。测试脚本仍按“自然语言输入 -> 真实 AI plan -> tool 执行/确认卡 -> 数据库断言 -> 审计断言”的路径执行，direct executor 不作为 provider 失败后的通过口径。

执行环境：

| 项目 | 结果 |
|------|------|
| 数据库隔离 | 临时覆盖 `DATABASE_URL=postgresql://goods_user:***@localhost:5432/goods_sell?schema=goods_sell_test`，脚本安全闸通过 |
| AI provider | `npm.cmd run test:ai-provider` 通过，输出 `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| think 模式 | `AI_THINKING_ENABLED=true/1` 已开启 |
| 测试数据 | 允许在 `goods_sell_test` 中创建虚拟客户、订单、商品、经销商、优惠券、库存流水、发票和审计日志 |

执行记录：

| 执行批次 | runId | 状态 | 结果 |
|------|------|------|------|
| 第一轮 | `AI-FULL-MOLF7N7I` | 失败观察项 | `PASS=46 FAIL=1 BLOCKED=0`；失败项为 `AI-FULL-001 / CONSUMER / search_products`，provider 规划阶段出现 `The operation was aborted due to timeout` |
| 第二轮 | `AI-FULL-MOLFLAOM` | 通过 | 临时增加 `AI_TIMEOUT_MS=120000` 后复跑全量，`PASS=47 FAIL=0 BLOCKED=0`；`AI-FULL-001` 命中 `search_products` 并返回“找到 1 个相关商品” |

全量 provider 复跑后的页面回归：

| 用例 | 状态 | 实际结果 |
|------|------|----------|
| Chrome/Edge 兼容 smoke | 通过 | 临时指定 `goods_sell_test` 并设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat`，Chrome/Edge × desktop/mobile 共 68 个页面检查通过；最新测试商品为 `AI-FULL-MOLFLAOM 新品果汁`，未发现 `Application error` 或横向溢出 |
| in-app browser 仓管产品详情页 | 通过 | 当前仓管账号打开 `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6`，页面显示零售价和当前库存，不显示进价/批发价，无运行时错误 |

补充质量门禁：

| 命令 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd audit --audit-level=moderate` | 通过 | `found 0 vulnerabilities` |
| `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed`，路由矩阵仍为全绿 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| 临时指定 `goods_sell_test` 且设置 `THIRD_PARTY_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:third-party` | 通过 | runId=`TPMOLG2166`；mock 预支付、成功回调、少付拦截、发票、公众号菜单 mock 均通过 |
| 临时指定 `goods_sell_test` 且设置 `PHASE5_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase5` | 通过 | 微信小程序 API smoke 通过 |
| 临时指定 `goods_sell_test` 且设置 `PHASE6_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase6` | 通过 | `Phase 6 smoke passed { totalSku: 81, deliveryRows: 95, payments: 113, users: 84, logs: 1337 }`；第一次未设置 `PHASE6_BASE_URL` 时因默认访问 `localhost:3000` 出现 `fetch failed`，已按端口配置问题复跑通过 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run check:launch` | 阻塞项符合预期 | `Launch readiness: BLOCKER`，`READY 5 / WARNING 4 / BLOCKER 1`；阻塞项为酒类经营资质未配置 |
| 临时指定 `goods_sell_test` 且注入 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0`；微信小程序、公众号、微信支付、税控仍需正式环境联调 |

第二轮通过的关键写流程：

| 角色 | 覆盖工具 | 落库/业务断言 |
|------|----------|--------------|
| 消费者 | `customer_submit_order` | 创建订单 `HQ20260430000123`，未直接绕过确认路径 |
| 管理员 | `admin_update_product_price`、`admin_update_product_status`、员工账号、系统参数、经销商审核/策略 | 商品价格变更、上下架、员工禁用/重置、经销商开通/驳回等均完成数据库断言和审计断言 |
| 销售员 | 客户标签、优惠券草稿、新品推送 | 新品推送生成 2 条记录，DRAFT 类工具不写库 |
| 仓管 | 安全库存、订单发货、入库、出库、盘点 | 安全库存改为 17，入库 2 件、出库 1 件，盘点任务 `SC20260430012` 创建 |
| 财务 | 收款登记、开票、财务摘要 | 订单 `HQMOLFLAOMFIN001` 登记收款 5 元，订单 `HQMOLFLAOMINV001` 开票 `FP20260430000015` |
| 经销商 | 待接单、库存上报、结算、接单、拒单 | 门店库存上报为 9，接单成功，拒单后自动重匹配 |

权限与安全断言：

| 用例 | 状态 | 结果 |
|------|------|------|
| 当时 47 个 tools 与用例数量一致 | 通过 | 脚本启动时校验 `cases.length === aiTools.length` |
| WRITE/HIGH_RISK 确认卡 | 通过 | 写操作先返回确认卡，不直接写库；正确确认后才执行 |
| 缺 token / 篡改 token / 跨用户 token | 通过 | 均被 400 拦截 |
| HIGH_RISK 错误确认文字 | 通过 | 未输入正确“确认执行”时不写库 |
| 越权调用 | 通过 | 消费者查经营总览、销售员建员工、仓管登记收款、财务发货、经销商查上线 readiness 均被 403 拦截 |
| 审计脱敏 | 通过 | AI 助手审计日志不包含明文 `password`、`confirmationToken`、`secret`、`apiKey`、`authorization`、`confirmText` |

当时结论（已由 23.50 的 48 tools 复测更新）：
- 真实 AI provider + think 模式下，当时 47 个 AI tools 已完成最新代码全量复跑，最终有效批次 `AI-FULL-MOLFLAOM` 全绿。
- 第一轮 `search_products` 超时不计为通过，已记录为 provider 冷启动/网络超时观察项；当前建议把 `AI_TIMEOUT_MS=120000` 纳入正式全量 AI 回归环境变量，避免 provider 首请求冷启动误伤测试结论。
- 若后续继续增强，应增加独立的 provider 异常测试，覆盖超时、非 JSON、空响应、401/429/5xx 的前端提示和服务端日志，而不是把异常稳定性混入 47 tools 功能覆盖脚本。

### 23.47 字段级权限 Smoke 补充记录

问题背景：
- 23.44/23.45 已修复产品详情页仓管可见进价/批发价的问题，但如果只靠人工打开当前页面，后续类似“页面可访问但字段越权可见”的问题仍容易漏测。
- 本轮新增独立字段级权限 smoke，把价格字段、销售员客户归属、销售员订单归属和经销商政策归属边界固定为自动化回归项。

代码与命令：

| 文件 | 变更 |
|------|------|
| `scripts/field-permission-smoke.ts` | 新增浏览器字段级权限测试；脚本强制校验 `DATABASE_URL` 指向 `schema=goods_sell_test`，在测试 schema 中创建受控管理员、销售员 A/B、仓管、财务、客户、订单、经销商和商品数据，然后用真实 Chrome/Edge CDP 登录不同角色执行断言 |
| `package.json` | 新增 `npm.cmd run test:field-permissions` |

本轮执行记录：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 且设置 `FIELD_PERMISSION_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:field-permissions` | 通过 | runId=`FIELD-MOLGQ3EN`；字段级权限 smoke 全部通过 |
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| in-app browser 当前页现场确认 | 通过 | 当前仓管账号打开 `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6`，可见 `零售价`、`当前库存`，不可见 `进价`、`批发价`，无运行时错误 |

覆盖矩阵：

| 用例 | 角色 | 断言 | 状态 |
|------|------|------|------|
| 产品详情价格字段 | 管理员 | 可见 `进价`、`批发价`、`零售价` | 通过 |
| 产品详情价格字段 | 财务 | 可见 `进价`、`批发价`、`零售价` | 通过 |
| 产品详情价格字段 | 销售员 | 可见 `批发价`、`零售价`，不可见 `进价` | 通过 |
| 产品详情价格字段 | 仓管 | 可见 `零售价`、`当前库存`，不可见 `进价`、`批发价` | 通过 |
| 产品编辑页直达 | 仓管 | `/dashboard/products/{id}/edit` 跳转 `/forbidden` | 通过 |
| 财务页直达 | 仓管 | `/dashboard/finance` 跳转 `/forbidden` | 通过 |
| 客户详情归属 | 销售员 A | 可见自己名下客户，不可见销售员 B 名下客户 | 通过 |
| 订单详情归属 | 销售员 A | 可见自己相关订单，不可见销售员 B 相关订单 | 通过 |
| 经销商政策归属 | 销售员 A | 可见自己名下经销商政策，不可见销售员 B 名下经销商政策 | 通过 |

当前结论：
- 字段级权限已从人工抽验升级为固定 smoke，后续价格、客户、订单、经销商政策字段改动都应同步跑 `test:field-permissions`。
- 当前脚本覆盖的是后台核心敏感字段和销售员数据归属边界；后续若新增财务明细、供应商采购价、客户隐私字段或经销商结算字段，应继续加入该脚本。

### 23.48 全量收尾回归与服务恢复记录

执行背景：
- 用户要求继续完成剩余测试，本轮在 23.47 的字段级权限 smoke 基础上继续执行自动化门禁、真实 AI provider 全量 tools、业务 smoke、第三方边界、生产构建、上线 readiness 和 in-app browser 现场复核。
- 所有写数据测试继续限定在本地 `goods_sell_test` schema，未使用生产库或正式业务库。

自动化执行记录：

| 命令/用例 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:rbac` | 通过 | `RBAC smoke passed` |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 47 tools` |
| `npm.cmd audit --audit-level=moderate` | 通过 | `found 0 vulnerabilities` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed` |
| 临时指定 `goods_sell_test` 且设置 `FIELD_PERMISSION_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:field-permissions` | 通过 | runId=`FIELD-MOLGYA8I`；管理员/财务/销售员/仓管产品价格字段、仓管禁入编辑/财务页、销售员客户/订单/经销商归属边界全部通过 |
| 临时指定 `goods_sell_test` 且设置 `BROWSER_COMPAT_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:browser-compat` | 通过 | Chrome/Edge × desktop/mobile 共 68 个页面检查通过，无 `Application error`、无横向溢出 |
| 临时指定 `goods_sell_test` 且设置 `PHASE5_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase5` | 通过 | Phase 5 微信小程序/API smoke 通过 |
| 临时指定 `goods_sell_test` 且设置 `PHASE6_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:phase6` | 通过 | `Phase 6 smoke passed { totalSku: 82, deliveryRows: 109, payments: 127, users: 89, logs: 1337 }` |
| 临时指定 `goods_sell_test` 且设置 `THIRD_PARTY_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:third-party` | 通过 | runId=`TPMOLH1SU4`；测试订单 `HQ20260430000140`、少付订单 `HQ20260430000141`、发票 `FP20260430000017`；微信支付/公众号均为 Mock 边界 |
| 临时指定 `goods_sell_test` 且设置 `AI_TIMEOUT_MS=120000` 后执行 `npm.cmd run test:ai-full-provider` | 通过 | runId=`AI-FULL-MOLH23RY`；真实 AI provider + think 模式下 `PASS=47 FAIL=0 BLOCKED=0` |
| 停止 3300 dev 服务后执行 `npm.cmd run build` | 通过 | Next.js 生产构建通过，动态路由与 API 路由完成构建 |
| 临时指定 `goods_sell_test` 后执行 `npm.cmd run check:launch` | 阻塞项符合预期 | `Launch readiness: BLOCKER`，`READY 5 / WARNING 4 / BLOCKER 1`；唯一 BLOCKER 为酒类经营资质未配置 |
| 临时指定 `goods_sell_test` 且注入 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 后执行 `npm.cmd run check:launch` | 通过但仍有提醒 | `Launch readiness: WARNING`，`READY 6 / WARNING 4 / BLOCKER 0`；微信小程序、公众号、微信支付、税控仍为 WARNING |
| 重启 `npm.cmd run dev -- -p 3300` 后 in-app browser 现场确认 | 通过 | 当前仓管账号打开 `/dashboard/products/cmol8e8ej0002vwyoyvnz94f6`，无 `Application error`，可见 `零售价`、`当前库存`，不可见 `进价`、`批发价` |

本轮真实 AI provider 关键覆盖结果：

| 覆盖项 | 状态 | 代表结果 |
|------|------|----------|
| 消费者 READ/WRITE | 通过 | 商品查询、客户上下文、下单、订单、待付款均命中预期工具；下单创建 `HQ20260430000150` |
| 管理员经营/商品/员工/系统/经销商/营销 | 通过 | 经营总览、销售业绩、调价、上下架、员工创建/禁用/重置、系统参数、上线检查、经销商审核/驳回/策略均通过 |
| 销售员客户/渠道/营销草稿 | 通过 | 客户标签、渠道摘要、优惠券/新品推送草稿和新品推送均通过 |
| 仓管库存/履约 | 通过 | 安全库存、发货、入库、出库、盘点均通过 |
| 财务收款/开票 | 通过 | 收款登记、开票和财务摘要均通过；高风险确认链路仍生效 |
| 经销商待接单/库存/结算/接单/拒单 | 通过 | 待接单、库存上报、结算摘要、接单、拒单均命中预期工具并通过 |
| 权限/确认/审计 | 通过 | 缺 token、篡改 token、跨用户 token、HIGH_RISK 错误确认文字、越权调用、审计脱敏均通过 |

当前结论：
- 本地 `goods_sell_test` schema 下，自动化门禁、真实 AI provider 47 tools、字段级权限、浏览器兼容、Phase 5/Phase 6、第三方边界和生产构建均已完成本轮收尾回归。
- 正式上线阻塞仍只保留合规与真实第三方联调类事项：酒类经营资质正式配置、真实微信小程序体验版、真实小额微信支付/回调、真实公众号菜单发布、真实税控联调和业务人员签收。
- 开发/模拟测试若临时注入虚拟酒类资质编号，可让 readiness 从 BLOCKER 降为 WARNING，用于贴近真实流程跑通；正式运营前不能把该虚拟编号作为合规通过依据。

### 23.49 业务角色模拟签收 Smoke 记录

执行背景：
- 业务、财务、仓管、经销商等人员的“正式签收”不能由自动化脚本代替真实负责人签字，但可以先做本地模拟验收，提前暴露流程和页面问题。
- 本轮新增 `scripts/role-acceptance-smoke.ts` 和 `npm.cmd run test:role-acceptance`，使用本地 `goods_sell_test` schema 中的虚拟客户、订单、商品、配送、收款、库存、经销商和渠道冲突数据执行。

代码与命令：

| 文件 | 变更 |
|------|------|
| `scripts/role-acceptance-smoke.ts` | 新增四类角色模拟签收 smoke；强制校验 `DATABASE_URL` 指向 `schema=goods_sell_test`，启动真实 Chrome/Edge CDP 浏览器，按业务员、财务、仓管、经销商分别登录并检查页面/数据边界 |
| `package.json` | 新增 `npm.cmd run test:role-acceptance` |

本轮执行记录：

| 命令 | 状态 | 结果 |
|------|------|------|
| 临时指定 `goods_sell_test` 且设置 `ROLE_ACCEPTANCE_BASE_URL=http://localhost:3300` 后执行 `npm.cmd run test:role-acceptance` | 通过 | runId=`ROLE-MOLI9XR1`；业务/销售、财务、仓管、经销商四类模拟签收全部通过 |
| `npx.cmd tsc --noEmit` | 通过 | 新增脚本后类型检查无错误 |
| `npm.cmd run lint` | 通过 | 新增脚本后 ESLint 无错误 |

覆盖矩阵：

| 模拟签收角色 | 覆盖流程 | 验收断言 | 状态 |
|------|------|------|------|
| 业务/销售 | 客户详情、订单详情、销售报表、渠道冲突台账 | 能查看名下虚拟客户、业务订单、销售报表核心指标和分配给自己的渠道冲突 | 通过 |
| 财务 | 财务总览、应收账款、收款登记 | 能查看应收、收入、毛利；能看到虚拟客户待核销订单；通过同一权限体系的财务 action 登记虚拟收款并更新订单已收金额 | 通过 |
| 仓管 | 仓储作业、库存预警、配送单、盘点任务 | 能看到低库存预警、出入库动态、待配送订单；通过仓管入口创建盘点任务并进入盘点详情 | 通过 |
| 经销商 | 待接订单、接单、我的订单、结算中心、库存上报 | 能看到虚拟待接订单；通过同一权限体系的经销商 action 接单；接单后订单进入经销商订单，结算和库存页面可追溯 | 通过 |

当前结论：
- “业务角色模拟验收”已完成，且可通过 `npm.cmd run test:role-acceptance` 复跑。
- “真实负责人正式签收”仍是管理动作，不是自动化测试动作；建议业务、财务、仓管、经销商负责人基于本节记录逐项确认页面口径、金额口径、库存口径和结算口径。
- 自动化脚本中财务收款和经销商接单采用“浏览器确认入口可见 + 同权限 server action 执行 + 数据库断言”的方式，避免浏览器自动点击 React client action 的事件不稳定误伤业务结论；这不影响正式人工签收时在浏览器内手动点击确认。

### 23.50 AI 固定词条与真实 Provider 兜底改造复测记录

问题背景：
- 浏览器现场发现用户输入 `我要下单 1 箱剑兰春`、`leige买了什么东西?` 等问法时，旧本地 planner 会因“买”字过宽而优先命中 `orders_manual_order_draft` 或被纠偏到经营总览，真实 AI provider 没有机会处理复杂意图。
- 本轮按“角色固定词条本地执行、非固定问法真实 AI provider 兜底、高频候选人工沉淀”的思路完成代码调整，并把购买历史查询从开单意图中拆出。

代码与能力变更：

| 文件/能力 | 变更 |
|------|------|
| `src/features/ai/intent-templates.ts` | 新增代码内置的角色固定词条注册层；消费者、经销商、管理员、销售员、仓管、财务分别显示不同词条；固定词条可直接生成本地 tool plan |
| `/api/ai/quick-prompts` | 新增角色词条接口；未登录返回空词条，不泄露业务入口；前端 AI 气泡打开时加载当前角色词条 |
| `src/features/ai/tools/registry.ts` | 新增 `customer_purchase_history` 只读 tool，支持按客户姓名/手机号查询最近购买商品、订单金额和状态；管理员/销售员/财务可用，仓管/经销商/消费者不可越权 |
| `src/features/ai/tools/planner.ts` | 收窄 `isPurchaseIntent()`；`我要下单/帮客户开单/要 N 箱` 才进入下单或开单；`买了什么/买过什么/购买记录` 进入购买历史；`leige买东西` 等含糊第三人称问法不直接写库 |
| `src/features/ai/assistant-service.ts` | 调整路由顺序为固定词条 -> 高置信本地规则 -> 真实 AI provider -> 校验/执行；provider prompt 明确区分购买历史和开单 |
| `src/features/ai/prompt-usage.ts` | 新增高频问法记录，使用 `AiContentCache` 保存角色、原始问题、归一化问题、命中来源、tool 计数、状态和候选标记；第一版只记录候选，不自动启用 |
| `scripts/ai-tools-smoke.ts`、`scripts/ai-runtime-regression.ts`、`scripts/ai-full-provider-coverage.ts` | 新增固定词条、购买历史、角色越权、真实 provider 48 tools 覆盖断言 |

自动化执行记录：

| 命令/用例 | 状态 | 结果 |
|------|------|------|
| `npx.cmd tsc --noEmit` | 通过 | 类型检查无错误 |
| `npm.cmd run lint` | 通过 | ESLint 无错误 |
| `npm.cmd run test:ai-tools` | 通过 | `AI tools smoke passed: 48 tools`；新增固定词条按角色过滤、`leige买了什么东西?` 命中 `customer_purchase_history`、含糊第三人称 `leige买东西` 不直接开单 |
| `npm.cmd run test:ai-runtime` | 通过 | `AI runtime regression passed`；销售员可查询 `张阿姨买了什么东西`，仓管同话术不规划到客户隐私 tool |
| `npm.cmd run test:ai-provider` | 通过 | `AI provider smoke passed: model planned system_launch_readiness, thinking=enabled` |
| 临时指定 `goods_sell_test` 且设置 `AI_TIMEOUT_MS=120000` 后执行 `npm.cmd run test:ai-full-provider` | 通过 | runId=`AI-FULL-MOLKGUOY`；真实 AI provider + think 模式下 `PASS=48 FAIL=0 BLOCKED=0`；新增 `AI-FULL-048 customer_purchase_history` 通过 |
| `npm.cmd run build` | 通过 | 生产构建通过，新增 `/api/ai/quick-prompts` 动态路由已进入构建清单 |

浏览器抽验记录：

| 场景 | 状态 | 结果 |
|------|------|------|
| 仓管打开产品详情页 AI 气泡 | 通过 | 固定词条显示为 `查看配送摘要`、`查看商品库存和预警`、`给剑兰春入库 2 件`、`新建一张全量盘点任务`，未显示消费者下单、经销商待接单、管理员经营总览或财务词条 |
| 仓管点击固定词条 `查看配送摘要` | 通过 | 直接返回 `delivery_summary` 结果卡，显示待发货、配送中、今日送达数据，未调用无关经营总览 |
| 仓管输入 `leige买了什么东西?` | 通过 | 返回“可用 AI 工具”提示，未生成后台开单草稿，未返回经营总览销售额，未泄露客户购买历史 |

当前结论：
- AI 助手已从“统一固定词条 + 宽泛本地抢占”调整为“角色固定词条 + 保守本地规则 + 真实 AI provider 兜底”。
- 购买历史和下单/开单已经拆成不同意图，`某客户买了什么` 不再触发写操作草稿；写操作仍必须走确认卡，HIGH_RISK 仍要求二次确认。
- 高频沉淀第一版已开始记录候选，但不会自动启用，后续需要人工审核后再把稳定问法加入代码内置词条。

### 23.51 线上部署快速验收（正式环境模拟）记录

执行背景：
- 本节补充线上部署网关入口贴近实际运行方式的基础可用性检查，避免误把本地测试视为生产就绪。
- 仍保留写数据安全策略：线上仅做可读 smoke 与鉴权/路由检查；写操作与 AI 创建类流程不在该轮执行。

线上快速检查（2026-04-30）：

| 路径 | 结果 | 说明 |
|------|------|------|
| `/` | 307 | 重定向到 `/shop` |
| `/shop` | 200 | 正常返回商城页 |
| `/login` | 200 | 登录页可达 |
| `/dashboard` | 307 | 重定向到 `/login?callbackUrl=%2Fdashboard` |
| `/forbidden` | 200 | 页面返回正常 |
| `/dealer/incoming` | 307 | 重定向到 `/login?callbackUrl=%2Fdealer%2Fincoming` |
| `GET /api/ai/quick-prompts` | 200 | 返回 `{"role":null,"prompts":[]}`（未登录） |
| `GET /api/ai/assistant` | 405 | Method Not Allowed（预期） |
| `POST /api/ai/assistant` 空消息 | 400 | 返回“请输入要让 AI 处理的事项” |
| `POST /api/ai/assistant` `{"message":"我要下单 1 箱剑兰春"}` | 400 | 同上（未登录时被鉴权/会话策略拦截） |
| `BROWSER_COMPAT`（Chrome/Edge） | 通过 | 页面加载无新增 `Application error`（基于线上前端入口） |

PM2 与服务状态：
- `pm2 status` 显示 `goods_sell` ONLINE，`3005` 监听正常（Next.js 已启动）。
- 服务未中断；线上入口返回正常后无 5xx 大面积连锁异常。

异常与待确认：
- 曾出现一次短时日志异常（`Cannot find module 'next/dist/compiled/cookie'`）与鉴权失败堆栈，随后服务恢复并持续返回正常响应；
  为避免一次性问题误判，需要在下次发布窗口回归一次完整 `npm ci && npm run build` 后重启验证。
- 线上环境当前 `AI` 鉴权路径返回 400 的现象为预期行为，需用已登录账号执行完整“真实 AI 对话 + 确认卡”验收（见下方未测项）。

验收结论：
- 线上基础可达性与关键路由鉴权链路通过，可作为“可观测/不阻塞发布前 smoke”。
- 当前仍缺少“真实用户登录态下”的 AI 全流程闭环与跨角色签收回归。

## 仍未完成/可见风险项（下轮建议）

- 业务人员正式签收：
  - 业务（销售员）、财务、仓管、经销商在真实账号下的操作签收未与主管签字流转打通；当前仅有模拟数据流执行与脚本复核。
- 真实 AI 对话回归：
  - 线上真实账号 + 真实会话下，重点复核 `/api/ai/assistant` / `confirm` 正常返回，确认“下单/接单/登记收款/调价”等 write 场景不越权且可审计。
- 微信生态与税控真实联调：
  - 真实微信小程序、公众号菜单发布、微信支付回调、税控设备联调仍未在生产链路验证（当前保留 mock/告警）。
- 合规与运营就绪：
  - 酒类经营资质以 `ALCOHOL_BUSINESS_LICENSE_NO=TEST-ONLY-DEV-SIMULATION` 过渡。
  - 该项仅用于仿真，必须以正式证照配置通过后才能解除 `check:launch` 的 WARNING。

### 23.52 待测覆盖建议（给运营验收前的执行清单）

- 角色化端到端：分别用业务、销售、仓管、财务、经销商、消费者账号跑一次“AI 入口、READ、WRITE/HIGH_RISK、越权”四象限。
- 数据追溯闭环：每类 WRITE/HIGH_RISK 都需要落库对象 + 页面可追溯 + 审计日志校验。
- 真实环境回归触发：
  - 复测一次 `npm.cmd run test:ai-full-provider`（线上环境可达时的 smoke 配置）。
  - 复测 `npm.cmd run test:role-acceptance` 但使用线上测试库与预发布账号，确认“入口显示、按钮可用性、确认文本”一致。
- 服务稳健性：加一次 `npm.cmd run build` + `pm2 restart` 后的“模块缺失异常”零回归验证。
